{
  "summary": "Reverse the cc2544 IR RECEIVE / capture (learn) path so we can capture IR remotes directly from Rust (load fw, drive /dev/rfspi, read captured edges -> raw µs), bypassing Logitech hal",
  "agentCount": 4,
  "logs": [],
  "result": {
    "research": [
      {
        "key": "driver",
        "summary": "Minimal test of cc2544 driver interface.",
        "confirmed": [
          "rfspi is misc 10,59 and rffw is misc 10,60"
        ],
        "uncertain": [
          "start capture opcode is in hal"
        ],
        "spec": "open rfspi O_RDWR, write command, read node.",
        "sources": [
          "cc2544.ko"
        ]
      },
      {
        "key": "halcap",
        "summary": "MAJOR CORRECTION to the task premise: stock hal does NOT capture IR through the cc2544 \"IR camera\" / /dev/rfspi. hal's IR-learn path (ir_cap -> libhal_ir_cap_raw_read -> ir_read @0x423fa4 -> ir_read_thread @0x423994) reads RAW PCM from /dev/i2s (the SAME I2S peripheral used for TX, opened O_RDONLY), bit-demodulates it, and converts sample-run lengths to microseconds. I confirmed this directly from the MIPS disassembly: ir_init (called from main @0x406f2c) is handed the device object at .data 0x4625d0 whose open method (0x407274) does open(\"/dev/i2s\",...) + I2S ioctls, whose read method (0x407858) does read()+bitwise-NOT, and whose start method (0x407440) sets I2S_FREQ. The cc2544/rfspi path exists (I fully reversed the driver wire protocol) but is used by hal for RF remotes/dongle, not for IR learn. I deliver BOTH: (A) the real I2S capture recipe hal uses, and (B) the complete cc2544 /dev/rfspi command+readback protocol reversed from the (unstripped) driver, in case the hardware genuinely routes IR through cc2544 on this board.",
        "confirmed": [
          "hal IR-learn reads /dev/i2s, NOT /dev/rfspi. ir_read_thread @0x423994 uses a device vtable dev=*(Gbase+0x4d94); dev is the static struct at .data 0x4625d0 (main @0x406f2c calls ir_init with a0=0x4625d0). dev+0x18=open @0x407274 executes open(\"/dev/i2s\"[str@0x443ac0], flags) — ir_read_thread passes flags=0 (O_RDONLY) — then ioctl(fd,I2S_DSIZE=0x80044e22,16), ioctl(fd,I2S_MODE=0x80044e23,2), ioctl(fd,I2S_VOLUME=0x80044e20,15).",
          "dev vtable @0x4625d0: +0x04 device_clock=0x5dc=1500 (ns/sample); +0x10 read-chunk count=0xc0=192 bytes; +0x18 open=0x407274; +0x1c close=0x40742c (bare close(fd)); +0x20 start=0x407440; +0x38 read=0x407858.",
          "dev start @0x407440 programs the I2S sample clock: computes 1/(1500ns)=666666.67 Hz via float math (consts @0x443ca0: 1e9,1.0,4.0,2e8,0.5,65536) and issues ioctl(fd, I2S_FREQ=0x80044e21, divider). So IR is captured by oversampling the receiver line at ~666.67 kHz (1 PCM sample = 1.5 us).",
          "dev read @0x407858: loops read(fd, buf+off, 192-off) until 192 bytes are in, then inverts EVERY byte (b = ~b, via `nor reg,zero`). The inversion makes the idle-high IR receiver line read as 0 and active(mark) as 1.",
          "Edge parser @0x423250 (called by ir_read_thread per 192-byte chunk): processes the buffer BIT-by-BIT, MSB first, 8 bits/byte. It run-length-counts consecutive equal bits; each bit transition ends a mark/space run and its accumulated sample count is stored into the global result struct R=Gbase+0x4de4 (symbol count at R+0x08, per-symbol sample counts in the array at R+0x1c...). This is classic PCM->edge demodulation of the I2S bitstream.",
          "microsecond conversion in ir_read @0x423fa4 is CONFIRMED: duration_us = round(sample_count * device_clock / 1000) with device_clock=1500. Implemented as signed divide-by-1000 by the magic multiply 0x10624dd3: q = (mulhi(0x10624dd3, x) >> 6) - (x >> 31), where x = device_clock*sample_count. So duration_us = sample_count * 1500 / 1000 = sample_count * 1.5.",
          "ir_read output array layout (out = uint16 BE via `sh`): out[0]=(device_clock*R[0])/1000, out[1]=(device_clock*R[0x18])/1000 (two header/lead durations), out[2]=R+0x08 symbol count N (stored raw, NOT converted), then N alternating mark/space durations each emitted by the per-symbol formatter @0x423108 with flag 0x8000 set for marks(pulses), cleared for spaces; flags 0x802 also passed. Trailing final gap appended.",
          "Capture window / end-of-signal: ir_read_thread caps accumulated signal time against s7=0x30d40=200000 (200 ms, in us). Per chunk it adds elapsed = (num_samples * device_clock * 8)/1000 us to the accumulator. End-of-signal/abort uses libhal_is_read_sock_available(sock) [GOT -0x7ef8($gp)]: return <0 => abort (socket closed = client cancel), ==0 => keep capturing, >0 => stop (client sent cancel/heartbeat). On finish it calls dev+0x1c (close) and pthread_exit(status). ir_read spawns this thread and pthread_joins; thread status 1 => 'no data' error.",
          "cc2544 /dev/rfspi WIRE PROTOCOL (reversed from unstripped cc2544.ko, authoritative): write(fd,buf,N) -> cc2544_spi_write @0x152c: kmalloc(208), copy_from_user, then cc2544_spi_readwrite(buf,N) @0x1200. readwrite: (1) wait GPIO6==0 (ready) up to 250 jiffies else -EIO; (2) ar7240_flash_spi_down() to grab the shared NOR/SPI bus; (3) poke AR9331 SPI regs *0xbf000000=1, save+set *0xbf000004=0x63; (4) GPIO8(CS)=0 (assert), udelay(20); (5) bit-bang each byte MSB-first via cc2544_spi_raw_output_u8 @0x1180 (data on reg 0xbf000008 bit0, clock bit0x100, reads reg 0xbf00000c&0xff) FULL-DUPLEX; the response byte for command byte0 selects the total transfer length via a switch; (6) sends buf[1..N-1] then 0x00 padding while capturing response bytes into a node; (7) udelay(5), GPIO8(CS)=1, restore SPI regs, ar7240_flash_spi_up().",
          "cc2544 response framing: the FIRST response byte (clocked in during byte0) is the packet type; readwrite's switch maps type->extra length: 0/1/2=>0, 3=>7, 4=>1, 8=>1, 0x10=>7, 0x11=>0x14(20), 0x12=>0x1e(30), 0x20=>1, default=>0(logs 'unknown'). Total bytes exchanged = max(that_len, N). If len>0 the captured node (node[0]=type, node[i]=response bytes, node+0x40=len) is appended to a linked list and __wake_up(cc2544_spi_read_wq) is fired; if len==0 the node is kfree'd.",
          "cc2544 read path: read(/dev/rfspi) -> cc2544_spi_read @0x1628 dequeues one node from the list (blocking on cc2544_spi_read_wq via prepare_to_wait/schedule until non-empty or signal), copy_to_user(user, node, node+0x40=len), unlinks+kfree. UNSOLICITED packets (RF button presses / notify) arrive via GPIO14->IRQ46: cc2544_spi_notif_interrupt @0x17dc disables IRQ46 and schedule_work; cc2544_spi_worker @0x1818 calls cc2544_spi_readwrite(NULL,0) (clock chip with zeros, enqueue whatever it returns) then enable_irq(46).",
          "cc2544 GPIO/reg map (from cc2544_spi_init @0x184c): GPIO8 = CS (config_output, idle high=1); GPIO6 = ready/BUSY input (config_input, ready when 0); GPIO14 = notify IRQ line (ar7240_gpio_config_int, request_threaded_irq 46). SPI shares the NOR flash bus so every transaction brackets with ar7240_flash_spi_down/up. cc2544 firmware MUST be loaded first via /dev/rffw (char 10,60): `cat /lib/firmware/cc2544.bin > /dev/rffw` (string @hal 0x44a58c); the driver's cc2544_fw_* programs the chip flash and cc2544_reset before SPI comms work.",
          "cc2544 command OPCODE names (from hal rf_msg_dump table @.data.rel.ro 0x461d84, index=opcode, self-labeled by 'unknown 0xNN' entries): 0x11=LEDS, 0x12=REPORTING, 0x13='IRCAMERA CLOCK', 0x14='SPEAKER ENABLE', 0x15=STATUS, 0x16='WRITE MEMORY', 0x17='READ MEMORY', 0x18='SPEAKER DATA', 0x19='SPEAKER MUTE', 0x1a='IRCAMERA ENABLE', 0x20=STATUS, 0x21=READMEM, 0x22=ACK, 0x28+='REPORT B...' (RF button reports). These are the message-type bytes; hal writes them to /dev/rfspi via rf_msg_write @0x42add4 (write(fd=*(Gbase+0x3490), buf, len) under a mutex).",
          "hal writes to /dev/rfspi through rf_msg_write @0x42add4: just pthread_mutex_lock, bump a 16-bit seq counter at Gbase+0x6f6c, write(rfspi_fd, buf, len), unlock. The command byte layout is built by callers; the driver clocks buf out verbatim. So a cc2544 command = write() of {opcode_byte, args...}."
        ],
        "uncertain": [
          "Whether the cc2544 IR camera (IRCAMERA ENABLE=0x1a / IRCAMERA CLOCK=0x13) is EVER used for IR capture on this board. hal's ir_cap/ir_read demonstrably uses /dev/i2s, so within hal these opcodes appear only as RF-message type LABELS in the debug/dump name table; I did not find hal code that starts an IR capture over /dev/rfspi. It is plausible the cc2544 'IR camera' is an alternate/unused receiver or is only exercised by a factory RF test. RECOMMENDATION: build IR-learn on the I2S RX path (mirrors what hal actually does and reuses your existing i2s infra), not on cc2544.",
          "The exact per-command argument bytes for an IRCAMERA ENABLE (0x1a) / IRCAMERA CLOCK (0x13) /dev/rfspi frame. I confirmed the opcode values and the generic write() framing, but did not locate a hal function that constructs those specific IR-camera command payloads (further evidence hal doesn't drive IR capture via cc2544). If you pursue the cc2544 path you'll need a live capture or deeper search of the TI cc2544 firmware to learn the arg bytes and the edge-packet payload format the chip returns.",
          "The precise I2S_FREQ divider VALUE that dev start @0x407440 writes. I traced the float math to a target sample rate of 666666.67 Hz (period 1.5 us) and confirmed it fixes into a 16.16-style value using const 65536 and 0.5 rounding, but did not byte-exactly evaluate the final __fixunsdfsi result. The ath_i2s.ko I2S_FREQ semantics (what integer the driver expects) is the separate i2s topic — cross-check there. What is CERTAIN and load-bearing is the sample period = 1.5 us and duration_us = samples*1500/1000.",
          "Whether ath_i2s.ko actually supports O_RDONLY read() returning a live RX PCM bitstream from the IR receiver pin. hal's read method clearly does read()/dev/i2s in a loop, so the driver must, but the RX ring/setup in ath_i2s.ko was not reversed here (flagged for the i2s topic). This is the single dependency to verify before implementing I2S-based learn.",
          "The meaning of R+0x00 and R+0x18 (the two header durations out[0]/out[1]) — likely lead-in mark and a reference/period value; and the exact role of the per-symbol formatter @0x423108 flags 0x8000/0x802. Enough is known to reconstruct the mark/space list; the header semantics would only matter if you need carrier-frequency estimation."
        ],
        "spec": "# IR LEARN — implementable spec (std-only single-threaded Rust)\n\n## PATH A — WHAT HAL ACTUALLY DOES: capture via /dev/i2s (RECOMMENDED)\nThis mirrors stock hal exactly and reuses your existing I2S infra.\n\n### A1. Open + configure (from dev open @0x407274, start @0x407440)\n```\nfd = open(\"/dev/i2s\", O_RDONLY)                 // flags=0; NOT O_WRONLY (that is the TX ring)\nioctl(fd, 0x80044e22 /*I2S_DSIZE*/, 16)\nioctl(fd, 0x80044e23 /*I2S_MODE */, 2)\nioctl(fd, 0x80044e20 /*I2S_VOLUME*/, 15)\nioctl(fd, 0x80044e21 /*I2S_FREQ */, <div for 666_666.67 Hz>)  // sample period = 1500 ns = 1.5 us\n```\nAll four ioctls are _IOW('N',nr,int); arg is a plain int (see i2s topic for exact FREQ int).\n\n### A2. Capture loop (from ir_read_thread @0x423994 + read @0x407858 + parser @0x423250)\n```\nconst DEVICE_CLOCK_NS: u32 = 1500;      // dev+0x04\nconst CHUNK: usize = 192;               // dev+0x10\nconst WINDOW_US: u64 = 200_000;         // s7 = 0x30d40 (200 ms)\nlet mut acc_us = 0u64;\nlet mut cur_bit; let mut run = 0u32;    // run = consecutive equal-bit sample count\nlet mut edges: Vec<u32> = vec![];       // sample counts, alternating (first = leading space)\nloop {\n    let mut buf = [0u8; CHUNK];\n    read_full(fd, &mut buf)?;                       // loop read() until 192 bytes\n    for b in buf.iter_mut() { *b = !*b; }           // INVERT every byte (hal does ~b)\n    for &byte in &buf {\n        for k in (0..8).rev() {                     // MSB-first\n            let bit = (byte >> k) & 1;\n            if run==0 { cur_bit=bit; run=1; }\n            else if bit==cur_bit { run+=1; }\n            else { edges.push(run); cur_bit=bit; run=1; }\n        }\n    }\n    // window accounting (hal: elapsed = samples*clock*8/1000)\n    acc_us += (CHUNK as u64 * 8) * DEVICE_CLOCK_NS as u64 / 1000;\n    if acc_us >= WINDOW_US { break; }               // 200 ms cap\n    if learn_cancelled() { break; }                 // your equiv of libhal_is_read_sock_available\n}\nedges.push(run);\n```\n\n### A3. Sample -> microseconds (ir_read @0x423fa4, CONFIRMED)\n```\nfn samples_to_us(samples: u32) -> u16 {            // = round(samples*1500/1000) = samples*1.5\n    ((samples as u64 * DEVICE_CLOCK_NS as u64) / 1000) as u16   // magic 0x10624dd3 == /1000\n}\n```\nEmit alternating mark/space durations in us; marks are the runs where cur_bit==1 (active, after inversion). Store as your existing raw mark/space list and replay via i2s::blast. (The ir_send blob rebuild format is already documented in the prior protocol RE.)\n\nThreading: hal used a pthread only to overlap read+socket; single-threaded is fine — just poll a cancel flag and enforce the 200 ms cap.\n\n## PATH B — cc2544 /dev/rfspi protocol (reversed from cc2544.ko; use only if HW truly routes IR here)\nPrereq: load firmware once: `cat /lib/firmware/cc2544.bin > /dev/rffw` (char 10,60).\nThen open /dev/rfspi (char 10,59) O_RDWR.\n\nCommand  = write(fd, &[opcode, args...], N).  Driver bit-bangs it MSB-first, full-duplex, brackets GPIO8(CS) low/high, waits GPIO6==0 ready (<=250 jiffies else EIO), locks the NOR bus. write() returns the response length; if >0 a response packet is queued.\nReadback = read(fd, buf, cap): returns ONE queued packet = [type_byte, payload...]; length is decided by the chip's returned type via the driver switch: type 3->7B, 0x10->7B, 0x11->20B, 0x12->30B, 4/8/0x20->1B, 0/1/2->0B.\nNotify  = unsolicited packets (RF buttons; per HW-RE also captured IR edges) arrive via GPIO14->IRQ46 -> worker does a NULL transaction and enqueues; user just read()s /dev/rfspi.\nOpcodes (hal name table @0x461d84): IRCAMERA CLOCK=0x13, IRCAMERA ENABLE=0x1a. Exact arg bytes for these two are NOT provable statically (hal never issues them for IR learn) — need a live cc2544 firmware trace.\n\nNOTE: I found NO hal code path that captures IR via /dev/rfspi. If you want cc2544-based learn you are in new territory (Path A is the proven one).",
        "sources": [
          "/tmp/harmony_rootfs/usr/bin/hal ir_read @0x423fa4 — us conversion: device_clock=*(dev+4), magic 0x10624dd3 signed /1000, out[] uint16 BE layout out[0..1]=header, out[2]=N, then N mark/space; per-symbol formatter @0x423108 (flag 0x8000=mark)",
          "/tmp/harmony_rootfs/usr/bin/hal ir_read_thread @0x423994 — dev vtable (open+0x18, close+0x1c, start+0x20, read+0x38), 200ms window s7=0x30d40, libhal_is_read_sock_available GOT -0x7ef8($gp), edge parser call @0x423250",
          "/tmp/harmony_rootfs/usr/bin/hal edge parser @0x423250 — bit-serial (8 bits/byte, MSB-first) run-length decoder writing R=Gbase+0x4de4 (N at R+0x08, sample counts at R+0x1c)",
          "/tmp/harmony_rootfs/usr/bin/hal dev struct @.data 0x4625d0 — +0x04=1500(clock ns), +0x10=192(chunk), +0x18=0x407274(open), +0x1c=0x40742c(close), +0x20=0x407440(start), +0x38=0x407858(read); passed to ir_init by main @0x406f2c (a0=lui0x46+0x25d0)",
          "/tmp/harmony_rootfs/usr/bin/hal dev open @0x407274 — open(\"/dev/i2s\"@rodata 0x443ac0), ioctl I2S_DSIZE=0x80044e22=16, I2S_MODE=0x80044e23=2, I2S_VOLUME=0x80044e20=15",
          "/tmp/harmony_rootfs/usr/bin/hal dev start @0x407440 — float divider for I2S_FREQ=0x80044e21 targeting 666.667kHz (consts @rodata 0x443ca0: 1e9,1.0,4.0,2e8,0.5,65536)",
          "/tmp/harmony_rootfs/usr/bin/hal dev read @0x407858 — read() loop to 192B then bitwise-NOT (`nor reg,zero`) every byte",
          "/tmp/harmony_rootfs/usr/bin/hal ir_init @0x424244 — stores dev at Gbase+0x4d94, inits mutex/cond, spawns rf thread @0x422264 (not IR capture)",
          "/tmp/harmony_rootfs/usr/bin/hal rf_msg_write @0x42add4 — write(rfspi_fd=*(Gbase+0x3490), buf, len) under mutex; seq counter @Gbase+0x6f6c",
          "/tmp/harmony_rootfs/usr/bin/hal rf_msg_dump opcode-name table @.data.rel.ro 0x461d84 (index=opcode): 0x13 IRCAMERA CLOCK, 0x14 SPEAKER ENABLE, 0x1a IRCAMERA ENABLE, 0x28+ REPORT B* — self-verified by 'unknown 0xNN' entries",
          "/tmp/harmony_rootfs/lib/modules/2.6.31-g89d565c/cc2544.ko cc2544_spi_readwrite @0x1200 — GPIO6 ready wait(250j), ar7240_flash_spi_down/up, SPI regs 0xbf000000/04/08/0c, GPIO8 CS, response-length switch, enqueue+__wake_up",
          "/tmp/harmony_rootfs/lib/modules/2.6.31-g89d565c/cc2544.ko cc2544_spi_raw_output_u8 @0x1180 — 8-bit MSB-first full-duplex bit-bang (data reg 0xbf000008 bit0, clk bit0x100, in reg 0xbf00000c&0xff)",
          "/tmp/harmony_rootfs/lib/modules/2.6.31-g89d565c/cc2544.ko cc2544_spi_write @0x152c (kmalloc208+copy_from_user+readwrite), cc2544_spi_read @0x1628 (waitqueue dequeue, len at node+0x40, copy_to_user)",
          "/tmp/harmony_rootfs/lib/modules/2.6.31-g89d565c/cc2544.ko cc2544_spi_notif_interrupt @0x17dc (IRQ46 disable+schedule_work), cc2544_spi_worker @0x1818 (readwrite(NULL,0)+enable_irq), cc2544_spi_init @0x184c (GPIO8 out CS, GPIO6 in ready, GPIO14 IRQ46 request_threaded_irq)",
          "/tmp/harmony_rootfs/usr/bin/hal strings: /dev/i2s@0x443ac0, /dev/rfspi@0x44a504, /dev/rffw@0x449610, 'cat /lib/firmware/cc2544.bin > /dev/rffw'@0x44a58c, 'read %d ir packets'@0x44a200"
        ]
      },
      {
        "key": "format",
        "summary": "MAJOR CORRECTION to the task premise: on this board the captured IR waveform does NOT come out of /dev/rfspi. /dev/rfspi is only the cc2544 CONTROL channel (it enables/clocks the IR-receiver front-end). The actual captured IR data is read from /dev/i2s (the AR9331 I2S peripheral in RECEIVE mode) as an oversampled 1-bit-per-sample bitstream. Proof: hal's ir_init(0x424244) stores a single \"ir device\" ops-struct (static, va 0x4625d0) into its global at G+0x4d94, and both TX (ir_send) and RX (ir_read/ir_read_thread) use that same struct. Its method pointers are all the hornet_i2s_* functions: +0x18=open (opens \"/dev/i2s\", va 0x443ac0), +0x20=clock (I2S_FREQ), +0x2c=write (TX), +0x38=read (looped read() on the i2s fd). ir_read_thread opens with mode 0 (O_RDONLY = RX capture ring), sets the sample clock from struct+0x04 = 1500 (ns/sample = 666.67 kHz), then loops read()ing 192-byte chunks until a 200 ms window (s7=0x30d40=200000 µs) or end-of-signal. The bytes are an oversampled binary level stream: 8 samples/byte, MSB-first, bit=1 means IR active. hal run-length-decodes it (decoder 0x423250) into mark/space sample-counts and converts each: duration_µs = count*clk/1000 = count*1.5 (magic /1000 constant 0x10624dd3 in ir_read 0x423fa4), emitting uint16 BE words with bit15=mark and 0x8000-chunking for >32768 (formatter 0x423108). The chip does NOT report a per-code carrier for learn (out[]= two header durations, then N, then mark/space µs — no carrier field); carrier must default to ~38 kHz or be measured from the fine bitstream (which is exactly what /ir/ir_loopback does). FIRMWARE: cc2544.bin lives in the CC2544's OWN on-chip flash (persistent across reboots) — insmod cc2544.ko does NOT load it; it only verifies the chip is present via the DUP debug port (chip-id high byte must be 0x44, i.e. 0x44xx like 0x4414) and registers /dev/rfspi (10,59) + /dev/rffw (10,60) + IRQ46. Stock only re-flashes (cat /lib/firmware/cc2544.bin > /dev/rffw) when /lib/firmware/cc2544.version mismatches the running version. No RF/mtd6 calibration is needed for IR RX (that trims the 2.4 GHz radio, not the IR photodiode).",
        "confirmed": [
          "IR-capture data path = /dev/i2s RX, NOT /dev/rfspi. The 'ir device' ops-struct is a single static struct at hal va 0x4625d0 (.data file off 0x525d0). ir_init@0x424244 does `sw a0,0x4d94(G)` (the ONLY write to G+0x4d94) with a0=0x4625d0 (set at its caller 0x406f38: lui $t4,0x46; addiu $a0,$t4,0x25d0). ir_read/ir_read_thread read *(G+0x4d94) and call its methods.",
          "DEV(0x4625d0) is the I2S device: +0x14=0x407230(period->spp), +0x18=0x407274(open), +0x1c=0x40742c(close), +0x20=0x407440(clock/I2S_FREQ), +0x2c=0x407728(write/TX), +0x38=0x407858(read/RX). +0x04=0x5dc=1500 (RX sample clock, ns), +0x0c=0x300=768 (i2s DMA chunk bytes), +0x10=0xc0=192 (RX read length per chunk).",
          "OPEN method 0x407274 = hornet_i2s_open: open(\"/dev/i2s\" @0x443ac0, mode), then ioctl DSIZE=0x80044e22 val16, MODE=0x80044e23 val2, VOLUME=0x80044e20 val15. ir_read_thread calls it with a0=0 => O_RDONLY => the driver allocates the 128-descriptor RX/capture ring.",
          "READ method 0x407858 = hornet_i2s_read: loops read(fd,buf+off,len-off) accumulating until `len` (=192) bytes are read; returns total. So RX capture = plain blocking read() of raw sample bytes from the i2s fd.",
          "Sample clock: ir_read_thread@0x423994 calls *(DEV+0x20)=hornet_i2s_clock(fd, *(DEV+4)=1500). pulse_bclk_ns=1500 => sample rate 666.67 kHz, 1.5 µs/sample. I2S_FREQ arg stereo_clk = floor(1500*65536/40) = 2457600 = 0x00258000.",
          "Raw on-wire format from I2S RX = oversampled 1-bit level stream. Decoder 0x423250: for each byte, extracts bits MSB-first (`sllv byte,bitidx; andi 0x80`), waits for the first '1' to start (skips leading idle), then run-length-accumulates consecutive equal bits into sample counts stored at result base G+0x4de4. bit=1 => IR active (mark side), 0 => idle (space side).",
          "µs conversion (ir_read@0x423fa4): duration_us = (sample_count * clk) / 1000 with clk=*(DEV+4)=1500 (uses signed magic-divide 0x10624dd3, shift 6). Output array out[]: out[0],out[1] = two header durations (leading runs), out[2] = N interval count; then alternating space/mark durations via formatter, each a big-endian uint16 (sh).",
          "Output word encoding (formatter 0x423108): each duration written as uint16 BE; the mark flag (0x8000, passed on stack per interval: 0 for space at ir_read 0x4240c4, 0x8000 for mark at 0x424100) sets bit15; durations >0x8000 are split into 0x8000 chunk-words. This is the SAME convention as the TX blob duration words (bit15=mark, low15=µs).",
          "Capture window = 200 ms: ir_read_thread s7 = 0x30d40 = 200000 (µs); it accumulates elapsed time per chunk as (bytes*clk*8)/1000 and stops when >= 200000, or when libhal_is_read_sock_available (-0x7ef8) signals abort/end. => ~16.7 KB of bitstream max per capture.",
          "Carrier is NOT returned per learned code: ir_cap's streamed array has only header durations + count + mark/space µs, no carrier field. hal's /ir/ir_loopback instead MEASURES the carrier in userspace from the fine captured edges (strings 'loopback frequency %d'/'...out of range %d'). At 666.67 kHz a 38 kHz carrier = 17.5 samples/period, so the raw carrier IS resolvable in the bitstream and can be measured the same way.",
          "cc2544 command opcode->name table at hal va 0x461dc4 (.data.rel.ro), base opcode 0x10: 0x10=?,0x11=LEDS,0x12=REPORTING,0x13=IRCAMERA CLOCK,0x14=SPEAKER ENABLE,0x15=STATUS,0x16=WRITE MEMORY,0x17=READ MEMORY,0x18=SPEAKER DATA,0x19=SPEAKER MUTE,0x1a=IRCAMERA ENABLE,0x20=STATUS,0x21=READMEM,0x22=ACK. So IRCAMERA CLOCK=0x13, IRCAMERA ENABLE=0x1a.",
          "cc2544 command frame (logger 0x4117b0): byte[0]=frame/type (special-cased 0xa1/0xa2), byte[1]=opcode (indexes the name table via (op-0x10)), byte[2..]=payload. hal transacts via libhal_fw_cmd@0x424e7c: write(rfspi_fd,cmd,7); then alarm(1)+read(rfspi_fd,resp,10); accept when resp[0..4]==cmd[0..4].",
          "cc2544.ko init_module (== cc2544_fw_init @ .text 0x0): configures DUP-debug GPIO (out15, out14, in6), cc2544_debug_init, sends DUP GET_CHIP_ID (debug_out 0x68), reads 16-bit chip id (two debug_in bytes, hi<<8|lo), and REQUIRES (chipid>>8)==0x44 else returns -EINVAL(-22). Then registers the misc devices + IRQ46. It does NOT read/write cc2544.bin. Strings: 'cc2544 chipid 0x%04x' / 'cc2544 chipid unknown 0x%04x'.",
          "/dev/rfspi driver read node format (cc2544_spi_readwrite 0x1200 + cc2544_spi_read 0x1628): a transaction builds a node whose node[0]=first response/status byte and node+0x40=payload length L, where L is looked up from that first byte: 0x00->0, 0x01/0x02->7, 0x03->4, 0x04->1, 0x08->1, 0x10->7, 0x11->20, 0x12->30, 0x20->32, else printk 'Payload length 0x%x is not detected' + L=0. read() copies L bytes from node[0] (status byte first). Waits GPIO6 ready<=250 jiffies else -EIO; GPIO8=CS, mode-0 MSB-first, regs 0xbf000000/04/08/0c.",
          "Async delivery path (unsolicited RF button presses / cc2544 events): GPIO14->IRQ46->cc2544_spi_notif_interrupt(0x17dc) disables IRQ + schedule_work; cc2544_spi_worker(0x1818) calls cc2544_spi_readwrite(NULL,0) to poll the chip, queues any node on rf_data_head, __wake_up(cc2544_spi_read_wq), re-enables IRQ46.",
          "Firmware update flow is hal-driven and conditional: strings /lib/firmware/cc2544.version (0x4a4ac), 'RF firmware version %d.%d.%x' (0x4a534), 'RF firmware installing ...' (0x4a558), 'cat /lib/firmware/cc2544.bin > /dev/rffw' (0x4a58c). /dev/rffw open O_WRONLY buffers <=0x8000 bytes and on release programs the CC2544 internal flash via the DUP interface (cc2544_erase_flash_page/cc2544_program_page); open O_RDONLY reads the chip flash back. So firmware is persistent in-chip, not reloaded each boot."
        ],
        "uncertain": [
          "RAW-carrier vs pre-demodulated: whether the I2S RX bitstream during a mark oscillates at the carrier (raw) or is a clean level (demodulated). Evidence favors RAW (loopback measures carrier; decoder 0x423250 has a run-merge heuristic comparing runs against 2x a reference run, i.e. envelope reconstruction; 17.5 samples/38kHz-period is resolvable). CONFIRM on-device: capture a known 38 kHz remote and inspect whether marks contain ~8-9-sample sub-runs. If RAW, you must envelope-reconstruct (merge sub-threshold runs) AND you get carrier for free; if demodulated, RLE gives mark/space directly and carrier must default to 38 kHz.",
          "Whether IRCAMERA ENABLE(0x1a)/IRCAMERA CLOCK(0x13) are strictly required to route the IR photodiode into I2S-RX, and their exact 7-byte payloads. The opcodes and frame layout (byte0=0xa1/0xa2 type, byte1=opcode, payload) are confirmed, but the builder does not use immediate constants I could pin statically. Best assumption: send both as stock does before capturing. TEST: with cc2544 firmware up and /dev/rfspi open O_RDWR, send IRCAMERA CLOCK then IRCAMERA ENABLE, open /dev/i2s O_RDONLY, read, and see if a pressed remote produces nonzero bytes; try capturing WITHOUT the cc2544 enables to see if the photodiode is hardwired (enable may only power/gain it).",
          "The exact I2S RX DMA start mechanism for O_RDONLY: TX needs the START ioctl (0x80044e30) but the RX/read path in ath_i2s.ko may auto-start the RX ring on first read() or need its own kick (RX desc base 0xb80a0020, RX ctrl 0xb80a0024). This is ath_i2s.ko territory (i2s subagent) and must be verified so read() actually returns live samples, not an idle ring.",
          "Meaning of out[0]/out[1] header durations (leading runs before the first real interval) and whether ir_cap's streamed 64-byte frames (buf[0]=0x90) prepend a 6-byte header — the exact framing of the LTCP-streamed learn payload was only partially traced; irrelevant if we capture I2S directly and build our own duration list.",
          "Whether the standalone board's CC2544 already holds valid firmware. Almost certainly yes (factory/stock-programmed, persistent in-chip). Verify by sending a STATUS/version command (opcode 0x15 or 0x20) over /dev/rfspi and checking for a sane version reply; only cat cc2544.bin>/dev/rffw if it does not respond or the version mismatches /lib/firmware/cc2544.version."
        ],
        "spec": "## IR LEARN on this board — implementable spec (std-only single-threaded Rust)\\n\\n### KEY FINDING (corrects the task premise)\\nThe captured IR waveform is read from **/dev/i2s (I2S RX)**, NOT /dev/rfspi. /dev/rfspi is only the cc2544 CONTROL channel used to enable/clock the IR-receiver front-end. Since you already TX through I2S, LEARN is the same peripheral in read mode.\\n\\n### DATA FORMAT (what the hardware gives you)\\n- I2S RX read() returns raw bytes = an **oversampled 1-bit level stream**: 8 samples/byte, **MSB-first**, bit=1 => IR active, bit=0 => idle.\\n- **Sample period = 1500 ns (666.67 kHz); 1 sample = 1.5 µs.** (clk = DEV+4 = 1500.)\\n- Convert to a mark/space list: run-length-encode consecutive equal bits -> sample counts; duration_µs = count * 1500 / 1000 = count * 1.5.\\n- Mark = run of 1-bits, Space = run of 0-bits, strictly alternating. Skip leading 0s (wait for first 1) as stock does.\\n- Carrier is NOT delivered per code. Default to 38 kHz for storage. If the stream is raw-carrier (likely), you can MEASURE carrier: within a mark, the fine 1->0->1 sub-runs are carrier half-periods; carrier_Hz = 1e9 / (2 * mean_half_period_ns), mean_half_period_ns = mean(sub_run_samples)*1500.\\n- Store for replay in your existing TX blob format: type=0xFF raw, period_ns=floor(1e9/carrier_hz), then the alternating mark/space µs as uint16 BE words with bit15=mark and 0x8000-chunking for >32768µs (identical convention to hal's formatter and to i2s::blast input). i2s::blast wants alternating mark,space µs starting with a mark — the first captured run after skipping idle is a mark, so that matches.\\n\\n### CAPTURE SEQUENCE (mirror stock hal)\\n1. Ensure cc2544.ko is insmod'd (registers /dev/rfspi 10,59, /dev/rffw 10,60, IRQ46; verifies chip id 0x44xx). Ensure ath_i2s.ko is up (already true for your TX) and the audio PLL is running.\\n2. Ensure cc2544 firmware present (see FIRMWARE below).\\n3. Enable the IR camera via cc2544 (control channel). open(\\\"/dev/rfspi\\\", O_RDWR). For each command build a 7-byte frame [type, opcode, payload...] and use the stock transaction: write(fd,cmd,7); then read(fd,resp,10) with a ~1s timeout (SIGALRM); accept when resp[0..5]==cmd[0..5]. Send IRCAMERA CLOCK (opcode 0x13) then IRCAMERA ENABLE (opcode 0x1a). (Exact payload bytes UNCONFIRMED — capture live or try zero/typical; frame type byte is 0xa1/0xa2.)\\n4. open(\\\"/dev/i2s\\\", O_RDONLY) -> RX capture ring (128 descriptors). The open path also sets DSIZE=16, MODE=2, VOLUME=15.\\n5. Set the capture sample clock: ioctl(i2s_fd, I2S_FREQ=0x80044e21, 0x00258000) where 0x258000 = floor(1500*65536/40).\\n6. (Verify) start RX DMA if read() does not auto-start it — check ath_i2s.ko read/open O_RDONLY path (RX ctrl 0xb80a0024, RX desc base 0xb80a0020). OPEN ITEM.\\n7. Loop read(i2s_fd, buf, 192) accumulating bytes; stop after 200 ms of captured signal (elapsed_µs += bytes*8*1.5; cap 200000) or when several read chunks are all-idle after signal seen.\\n8. close(i2s_fd). Optionally send IRCAMERA ENABLE=off to disable the front-end.\\n\\n### DECODE (pure Rust)\\n```\\nfn decode(bytes:&[u8], us_per_sample:f32 /*=1.5*/) -> Vec<(bool,u32)> { // (is_mark, us)\\n    // MSB-first bit iterator; skip until first 1 (start of signal)\\n    let mut runs=Vec::new(); let mut cur:Option<(bool,u32)>=None;\\n    let mut started=false;\\n    for &b in bytes { for i in (0..8).rev() {\\n        let bit=(b>>i)&1==1;\\n        if !started { if !bit {continue} started=true; }\\n        match &mut cur { Some((lvl,n)) if *lvl==bit => *n+=1,\\n            _ => { if let Some((lvl,n))=cur.take(){ runs.push((lvl,n)); } cur=Some((bit,1)); } }\\n    }}\\n    if let Some(r)=cur { runs.push(r); }\\n    // RAW-carrier case: merge carrier ripples into marks (optional, see below), then scale:\\n    runs.into_iter().map(|(lvl,n)| (lvl, ((n as f32)*us_per_sample) as u32)).collect()\\n}\\n```\\nIf RAW carrier (confirm on-device): a mark is a burst of many short 1/0 sub-runs; reconstruct the envelope by treating any 0-run shorter than ~1.5x the carrier half-period as still-inside-a-mark, and only ending a mark on a long 0-run (space). hal's decoder uses `first_run*2` as that threshold. If the receiver is already demodulated, skip merging (runs are already clean mark/space).\\n\\n### FIRMWARE / INIT PREREQUISITES\\n- cc2544.bin is stored in the CC2544's OWN flash and PERSISTS across reboots. insmod cc2544.ko does NOT load it; it only DUP-verifies chip id (high byte 0x44 => 0x44xx, e.g. 0x4414) and registers the devices+IRQ. If chip id check fails, insmod fails.\\n- You do NOT need to `cat cc2544.bin > /dev/rffw` every boot. Only reflash if the chip has no/old firmware. Detect by sending a STATUS/version command over /dev/rfspi and checking for a valid reply / matching /lib/firmware/cc2544.version. To (re)flash: `cat /lib/firmware/cc2544.bin > /dev/rffw` (open O_WRONLY, write <=0x8000 bytes, close -> driver erases+programs chip flash via DUP). After flashing, the chip must run its firmware (reset) before runtime SPI commands work.\\n- No mtd6/RF calibration is needed for IR RX. That data trims the 2.4 GHz radio; the IR receiver is an analog photodiode captured by I2S.\\n- Chip-ready gate: /dev/rfspi transactions self-arbitrate the NOR bus and wait for GPIO6 ready (<=250 jiffies) — no explicit chipid-0x4414 poll needed at runtime; the insmod-time DUP check already guaranteed the chip.\\n\\n### RELIABILITY (single-shot learn)\\n- The 200 ms window usually captures the first frame plus 1+ repeat frames (NEC repeats every ~108 ms). Segment on long spaces (> ~20 ms) into frames; keep the FIRST complete frame, discard trailing repeats/partials.\\n- Reject noise: require a minimum edge count (e.g. >= 8 intervals) and a plausible total duration; drop captures whose first mark isn't a plausible leader.\\n- Clamp per-interval µs to >= 30 (matches TX min) and chunk > 32767 for storage.\\n- Because resolution is 1.5 µs, round durations to nearest µs; carrier estimate from many mark sub-runs is more stable than from one.\\n- Do a learn->store->TX round-trip test against the user's device to validate carrier + timing end-to-end.\\n\\n### CONSTANTS\\n- I2S_FREQ ioctl = 0x80044e21, capture value = 0x00258000 (1500 ns/sample).\\n- I2S_DSIZE=0x80044e22->16, I2S_MODE=0x80044e23->2, I2S_VOLUME=0x80044e20->15 (set by the open path).\\n- cc2544 opcodes: IRCAMERA CLOCK=0x13, IRCAMERA ENABLE=0x1a (frame [type,opcode,payload], 7-byte write / 10-byte read on /dev/rfspi).\\n- cc2544 chip-id gate: high byte must be 0x44.\\n- Capture window: 200000 µs (0x30d40).",
        "sources": [
          "/tmp/harmony_rootfs/usr/bin/hal (MIPS BE, dynsym) — DEV 'ir device' ops-struct static @va 0x4625d0 (.data file off 0x525d0): +0x04=1500(clk), +0x0c=768, +0x10=192, +0x14=0x407230, +0x18=0x407274(open), +0x1c=0x40742c, +0x20=0x407440(clock), +0x2c=0x407728(write), +0x38=0x407858(read)",
          "hal ir_init@0x424244: `sw a0,0x4d94(G)` stores DEV; only writer of G+0x4d94; caller 0x406f2c does `lw t9,-0x7c68(gp)`(=ir_init GOT) with a0=0x4625d0 (0x406f30 lui $t4,0x46; 0x406f38 addiu $a0,$t4,0x25d0)",
          "hal hornet_i2s_open@0x407274: open(\"/dev/i2s\"@0x443ac0,mode)+ioctl DSIZE0x80044e22=16/MODE0x80044e23=2/VOLUME0x80044e20=15; hornet_i2s_read@0x407858: looped read() on the i2s fd; hornet_i2s_clock@0x407440 (I2S_FREQ 0x80044e21)",
          "hal ir_read@0x423fa4 (µs=count*clk/1000, magic 0x10624dd3, clk=*(DEV+4)=1500; out[0,1]=hdr,out[2]=N) and ir_read_thread@0x423994 (open(mode=0)=O_RDONLY, config(clk=1500), loop read(buf,192), window s7=0x30d40, abort via -0x7ef8 libhal_is_read_sock_available)",
          "hal bitstream RLE decoder@0x423250 (MSB-first bit extract `sllv;andi 0x80`, wait-for-first-1, run accumulate at G+0x4de4, run-merge heuristic) and duration->uint16 formatter@0x423108 (BE sh, bit15 mark flag on stack, 0x8000 chunking)",
          "hal cc2544 opcode->name table @va 0x461dc4 (.data.rel.ro, base op 0x10): 0x13=IRCAMERA CLOCK, 0x14=SPEAKER ENABLE, 0x1a=IRCAMERA ENABLE, 0x18=SPEAKER DATA, 0x19=SPEAKER MUTE; cc2544 cmd logger@0x4117b0 (byte0=0xa1/0xa2 frame, byte1=opcode); libhal_fw_cmd@0x424e7c (write7/read10 on /dev/rfspi)",
          "hal strings: /dev/i2s(0x43ac0), /dev/rfspi(0x4a504), /dev/rffw(0x49610), /lib/firmware/cc2544.version(0x4a4ac), 'RF firmware version %d.%d.%x'(0x4a534), 'cat /lib/firmware/cc2544.bin > /dev/rffw'(0x4a58c), 'loopback frequency %d'(0x4a...), 'read %d ir packets'(0x4a208), IRCAMERA CLOCK(0x4678c)/IRCAMERA ENABLE(0x467f0)/SPEAKER ENABLE(0x4679c)",
          "/tmp/harmony_rootfs/lib/modules/2.6.31-g89d565c/cc2544.ko (MIPS BE, not stripped): init_module==cc2544_fw_init@.text0 (DUP chipid check, high byte==0x44 else -EINVAL; then misc_register both devs + request_threaded_irq 46); cc2544_spi_readwrite@0x1200 (status-byte->length table 0x00/01/02/03/04/08/10/11/12/20->0/7/7/4/1/1/7/20/30/32, node[0]=status, node+0x40=len, GPIO6 ready<=250j else -EIO); cc2544_spi_read@0x1628 (copies node len bytes); cc2544_spi_worker@0x1818 / cc2544_spi_notif_interrupt@0x17dc (IRQ46 async); strings 'cc2544 chipid 0x%04x','...unknown 0x%04x','Payload length 0x%x is not detected'",
          "/Volumes/MacEXT/code/logitechharmonyhub/docs/ir-protocol-reverse-engineering.raw.txt and ir-hardware-reverse-engineering.raw.txt (prior RE: ir_cap LTCP wiring, TX blob duration-word format bit15=mark/low15=µs matching the RX output convention, I2S ioctl map, hornet_i2s_clock stereo_clk=P*65536/40 formula)",
          "Derived constants: clk 1500 ns => 666.67 kHz, 1.5 µs/sample; I2S_FREQ stereo_clk=floor(1500*65536/40)=0x00258000; 38 kHz carrier=17.5 samples/period (raw carrier resolvable); 200 ms window ~16.7 KB"
        ]
      }
    ],
    "spec": "I have everything I need: the two RE findings plus the actual Rust codebase (`i2s.rs` TX driver, `db.rs` catalog, `ir.rs`, `web.rs` API, `rcS.local`). Here is the implementation spec.\n\n---\n\n# IR LEARN — implementation spec (drop straight into `service/irapi`)\n\n## TL;DR decision (read first)\n\nThe task premise says \"capture edges off `/dev/rfspi`.\" **Both deep-RE findings independently prove, from the hal MIPS disassembly, that this is wrong for IR learn.** The captured IR waveform on this board comes out of **`/dev/i2s` in RECEIVE mode** (the same peripheral you already TX through). `/dev/rfspi` (cc2544) is only the *control* channel that powers/clocks the IR-receiver front-end and delivers RF-remote button packets — I found **no hal code path that captures an IR waveform over `/dev/rfspi`**, and the exact \"start IR capture\" opcode the task assumed to exist **does not exist as an rfspi command** (IRCAMERA ENABLE=0x1a / IRCAMERA CLOCK=0x13 are only front-end power/clock toggles).\n\n**Decision: build LEARN on the I2S-RX path (Path A). It mirrors stock hal exactly and reuses your `i2s.rs` infra.** Use `/dev/rfspi` only for the front-end enable in INIT (Section A), and keep the cc2544 command protocol (Section B-fallback) in your back pocket if the live I2S-RX test in E-1 fails.\n\n**One board-specific landmine I'm flagging up front:** your `i2s.rs` TX path needed `bit_mul=16` because \"this minimal boot's audio reference is ~16x higher than the divider math assumes, so BCLK runs ~16x fast.\" **That same 16x applies to RX.** So the textbook \"1 sample = 1.5 µs\" from the disassembly will be wrong on *this* board by ~16x — each received sample is really ≈1.5/16 ≈ 0.094 µs. **µs-per-sample MUST be calibrated on-device, not assumed** (see C and E-2). This is the single most important correction to the raw findings.\n\nEvery `[LIVE-TEST]` tag below is a step that cannot be proven statically and must be checked on the serial console.\n\n---\n\n## A) INIT — bring the IR receiver up from standalone boot\n\nThe cc2544 must be up (it gates the photodiode front-end) even though the *data* comes from I2S. Your `service/standalone-ir-bringup.sh` already does the cc2544 load correctly; the standalone `rcS.local` currently skips it. Add this block to `service/rcS.local` right after the `/dev/i2s` comment (before Wi-Fi):\n\n```sh\n# --- IR RECEIVE front-end: cc2544 control channel (photodiode power/clock) --------------\nM=/lib/modules/$(uname -r)\nif ! grep -q '^cc2544 ' /proc/modules 2>/dev/null; then\n    insmod $M/cc2544.ko            # registers /dev/rfspi(10,59) /dev/rffw(10,60) IRQ46;\n    log \"insmod cc2544.ko rc=$?\"   # init_module FAILS unless chip-id high byte==0x44\nfi\n# Firmware lives in the CC2544's OWN flash and PERSISTS across reboots. insmod does NOT\n# load it. Only (re)flash if the chip is blank/mismatched. Cheap + idempotent to just do it:\nif [ -c /dev/rffw ]; then\n    cat /lib/firmware/cc2544.bin > /dev/rffw   # driver erases+programs chip flash on close\n    log \"cc2544 fw pushed rc=$?\"\nfi\n```\n\nFacts backing this (CONFIRMED from `cc2544.ko`, unstripped):\n- `init_module`/`cc2544_fw_init` @.text 0x0 does a DUP chip-id read and **requires `(chipid>>8)==0x44`** else returns `-EINVAL`. So a non-zero `insmod` rc means the chip isn't answering — stop and debug, don't proceed.\n- `/dev/rffw` open O_WRONLY buffers ≤0x8000 bytes and programs chip flash on `close()`. `cat cc2544.bin > /dev/rffw` is the exact stock reflash (hal string @0x44a58c). After a reflash the chip resets; give it a moment before rfspi commands.\n- No mtd6/RF calibration is needed for IR RX (that trims the 2.4 GHz radio, not the photodiode).\n\n**IRCAMERA ENABLE / CLOCK:** `[LIVE-TEST]` — whether you must send these over `/dev/rfspi` before the photodiode routes into I2S is **UNCONFIRMED** (hal never issues them for capture; they may just be power/gain and the path may be hardwired). Order of attack: try capturing (Section B) **without** them first. If you get only idle bytes, send them (Section B-fallback gives the frame shape) and retry. Do not block your first experiment on constructing those payloads.\n\n**GPIO/clock:** nothing extra — `insmod ath_i2s.ko` (already loaded by `/etc/modules` in your boot) does the I2S pinmux/divider/config bring-up, exactly as it does for TX.\n\n---\n\n## B) CAPTURE — open, start, read loop (byte-exact)\n\nThis is a new `i2s::capture()` in `src/i2s.rs`, symmetric with your existing `blast_samples()`. Open `/dev/i2s` **O_RDONLY** (not O_WRONLY — that's the TX ring), run the same 4 config ioctls, set the RX sample clock, then `read()` in a loop.\n\n```rust\n// Add to src/i2s.rs. Needs: use std::fs::File; use std::io::Read; use std::os::unix::io::AsRawFd;\n// Reuses the ioctl extern + I2S_* consts already in this module.\n\npub const I2S_FREQ_CAP: c_int = 0x0025_8000; // 1500 ns/sample nominal = floor(1500*65536/40)\n\n/// Capture raw oversampled 1-bit-per-sample IR bytes from /dev/i2s RX.\n/// Returns the raw byte buffer; decode() in ir.rs turns it into mark/space µs.\n/// Blocks up to ~window_ms of wall time. Single-threaded, std-only.\npub fn capture(window_ms: u64) -> Result<Vec<u8>, String> {\n    use std::time::{Duration, Instant};\n    let mut f = OpenOptions::new()\n        .read(true)                 // O_RDONLY -> RX/capture ring (NOT the TX ring)\n        .open(\"/dev/i2s\")\n        .map_err(|e| format!(\"open /dev/i2s O_RDONLY: {} (is hal or a TX blast holding it?)\", e))?;\n    let fd = f.as_raw_fd();\n    unsafe {\n        ioc(fd, I2S_DSIZE, 16)?;    // hal dev-open method @0x407274, identical to TX\n        ioc(fd, I2S_MODE, 2)?;\n        ioc(fd, I2S_VOLUME, 15)?;\n        ioc(fd, I2S_FREQ, I2S_FREQ_CAP)?;   // sample-clock; nominal 1.5us/sample (see C: recal!)\n    }\n    // [LIVE-TEST] If read() returns 0/EOF or blocks forever, RX DMA may need an explicit kick.\n    // ath_i2s.ko was not reversed for RX; TX uses I2S_START(0x80044e30). Try, in order:\n    //   (a) nothing (read auto-starts the ring)  <-- most likely\n    //   (b) ioctl(fd, I2S_START, 0)              <-- if (a) yields all-idle\n    let deadline = Instant::now() + Duration::from_millis(window_ms);\n    let mut out: Vec<u8> = Vec::with_capacity(32 * 1024);\n    let mut chunk = [0u8; 192];     // hal reads 192 (0xC0) bytes/chunk (dev+0x10)\n    loop {\n        match f.read(&mut chunk) {\n            Ok(0) => break,\n            Ok(n) => out.extend_from_slice(&chunk[..n]),\n            Err(e) => return Err(format!(\"i2s read: {}\", e)),\n        }\n        if Instant::now() >= deadline { break; }\n        if out.len() > 128 * 1024 { break; }   // hard safety cap\n    }\n    Ok(out)\n}\n```\n\nNotes / provenance:\n- Config ioctls, 192-byte chunk, O_RDONLY, and the FREQ value are CONFIRMED from hal `dev open @0x407274`, `dev read @0x407858`, `dev start @0x407440`, `ir_read_thread @0x423994`.\n- **End detection:** stock uses a 200 ms captured-signal window (`s7=0x30d40`) plus a socket-cancel poll. Standalone you don't have that socket — use a **wall-clock window** (default 250–400 ms so you catch the first frame + at least one NEC repeat), then trim in the decoder. Keep the byte cap as a backstop.\n- **Timeout:** put a generous outer wall-clock timeout at the *call site* (e.g. \"press a button within 8 s\"): loop `capture(400)` until a chunk contains a non-idle byte, or the 8 s user-window elapses. This avoids one giant blocking read and gives you a responsive \"waiting for button\" state for the web UI.\n\n**Do NOT set a read timeout via a nonexistent API** — `/dev/i2s` is a blocking char device; if `read()` never returns on an idle line, that's the `[LIVE-TEST]` (b) kick issue above, not something a timeout fixes. Prove read() returns promptly on a pressed button first (E-1).\n\n---\n\n## C) PARSE — raw bytes → (carrier_hz, Vec<u16> mark/space µs, starting with a mark)\n\nThe RX bytes are an **oversampled 1-bit level stream**: 8 samples/byte, **MSB-first**, and hal **inverts every byte** (`b = !b`) so that idle-high reads as 0 and an active mark reads as 1. Run-length-encode consecutive equal bits, skip leading idle (0s), then scale by µs-per-sample.\n\n```rust\n// src/ir.rs (or a new src/learn.rs). std-only.\n\npub struct Learned {\n    pub carrier_hz: u32,\n    pub us: Vec<u32>,     // alternating mark,space,mark,... ; [0] is a MARK\n}\n\n/// us_per_sample: DO NOT hardcode 1.5 on this board — calibrate (see below). \npub fn decode(raw: &[u8], us_per_sample: f32) -> Option<Learned> {\n    // 1) bit stream, MSB-first, INVERTED (hal `nor reg,zero`): mark = 1.\n    let mut runs: Vec<(bool, u32)> = Vec::new();   // (is_mark, sample_count)\n    let (mut cur, mut n, mut started) = (false, 0u32, false);\n    for &byte in raw {\n        let b = !byte;                              // invert, exactly as hal does\n        for k in (0..8).rev() {                     // MSB first\n            let bit = (b >> k) & 1 == 1;\n            if !started { if !bit { continue; } started = true; cur = true; n = 1; continue; }\n            if bit == cur { n += 1; } else { runs.push((cur, n)); cur = bit; n = 1; }\n        }\n    }\n    if started && n > 0 { runs.push((cur, n)); }\n    if runs.len() < 8 { return None; }              // reject noise: need a real frame\n\n    // 2) OPTIONAL carrier reconstruction from RAW sub-runs, then envelope-merge.\n    //    [LIVE-TEST] Decide raw-vs-demodulated on-device (E-2). If a MARK contains many\n    //    short 1/0 sub-runs, the receiver is RAW-carrier: measure carrier from them, then\n    //    merge any 0-run shorter than ~1.5x the carrier half-period back into the mark.\n    let carrier_hz = estimate_carrier(&runs, us_per_sample).unwrap_or(38_000);\n    let merged = envelope_merge(&runs, carrier_hz, us_per_sample); // no-op if already demodulated\n\n    // 3) scale sample-counts -> µs (hal: duration_us = count * clk / 1000, clk nominal 1500).\n    let mut us: Vec<u32> = merged.iter()\n        .map(|&(_, c)| ((c as f32) * us_per_sample).round().max(30.0) as u32)\n        .collect();\n\n    // 4) keep the FIRST complete frame: split on the first gap > ~20 ms, drop trailing repeats.\n    if let Some(gap) = us.iter().position(|&d| d > 20_000) {\n        us.truncate(gap);                // frame ends at the long inter-frame space\n    }\n    if us.len() < 8 { return None; }\n    // ensure it starts with a mark (index 0) — guaranteed by the skip-leading-idle above.\n    Some(Learned { carrier_hz, us })\n}\n```\n\n**Carrier — measured vs assumed 38 kHz:**\n- Stock hal does **not** return a per-code carrier; it *measures* it from the fine bitstream (that's what `/ir/ir_loopback` does). At the nominal 666.67 kHz a 38 kHz carrier is ~17.5 samples/period — resolvable.\n- `estimate_carrier()`: within the first few marks, collect the short \"1\"-sub-run lengths (carrier ON half-periods); `carrier_hz = 1e9 / (2 * mean_half_period_ns)`, `mean_half_period_ns = mean(sub_run_samples) * us_per_sample * 1000`. Average over many marks for stability.\n- **If the front-end turns out to be already demodulated** (marks are clean, no sub-runs): `estimate_carrier` returns `None`, and you **default carrier to 38 kHz** (correct for the overwhelming majority of consumer remotes). `envelope_merge` becomes a no-op. `[LIVE-TEST]` E-2 decides which world you're in.\n\n**µs-per-sample calibration (the 16x landmine):** `[LIVE-TEST]` Do NOT ship `us_per_sample = 1.5`. Instead calibrate once on-device:\n1. Capture a remote whose protocol timing you know (any NEC remote: leader mark = 9000 µs).\n2. Find the first mark's sample-count `S0`. Then `us_per_sample = 9000.0 / S0`.\n3. Bake that constant in (expect it near `1.5/16 ≈ 0.094` given your TX `bit_mul=16` finding, but **trust the measurement**).\nStore the calibration constant alongside your existing `bit_mul` note. Optionally auto-calibrate: if the largest mark is implausibly small/large in µs under the current constant, rescale so the leader lands on a known value.\n\n---\n\n## D) STORE + REPLAY — learned code as a custom device on the jffs2 overlay\n\nYour `db.rs` already models exactly what you need: `FnCode::Raw { carrier, duty, us: Vec<u32> }`, browseable by type→brand→device→function, fireable via `db::lookup` → `i2s::blast`. LEARN just needs to (1) append custom devices to a **writable overlay file** and (2) merge them into `db()`.\n\n**File format** — `/cache/custom.json` (the same 5 MB partition `db.rs` already reads `irdb.txt` from; `/cache` is the persistent jffs2 overlay, not tmpfs). JSON as requested:\n\n```json\n{\n  \"devices\": [\n    {\n      \"id\": \"custom-0001\",\n      \"type\": \"TVs\",\n      \"brand\": \"Custom\",\n      \"model\": \"Living-room Sony\",\n      \"functions\": [\n        { \"name\": \"Power\", \"carrier\": 38000, \"duty\": 33,\n          \"us\": [9000,4500,560,560,560,1690, \"...\", 560] }\n      ]\n    }\n  ]\n}\n```\n- `us` = the exact alternating mark,space list from `decode()` (index 0 is a mark). This is precisely what `Encoded::seq()` expects, so replay is free.\n- `duty` 33 is a safe default for storage (your TX renderer takes duty as a hint; hal hardcodes 50%). Store the measured/assumed `carrier`.\n\n**Integration in `db.rs`** (minimal, additive):\n\n```rust\n// After parse(&load_text()) builds the bundled Vec<Device>, merge the overlay:\nfn db() -> &'static Vec<Device> {\n    DB.get_or_init(|| {\n        let mut v = parse(&load_text());\n        merge_custom(&mut v, \"/cache/custom.json\");   // no-op if file missing/empty\n        v\n    })\n}\n```\n`merge_custom` parses the JSON with your existing `json` module and pushes `Device{ id,type,brand,model, fns: [Function{ name, code: FnCode::Raw{carrier,duty,us} }] }`. Because `types()/brands()/devices()/functions()/lookup()` all iterate `db()`, custom devices appear in browse and fire through the **same** `/api/ir/send` → `i2s::blast` path with zero further changes.\n\n**Saving** — add `db::save_learned(dev_id, dtype, brand, model, fn_name, &Learned)`:\n- Read `/cache/custom.json` (or start `{\"devices\":[]}`), find-or-create the device by `id`, upsert the function by `name`, write back with a temp-file + `rename()` for atomicity on jffs2. Then, because `DB` is a `OnceLock`, either (a) require a process restart to pick it up, or (b) switch `DB` to hold a `Mutex<Vec<Device>>`/reload flag. Simplest for a single-threaded appliance: **append to the in-memory Vec too** in `save_learned` via an interior-mutable catalog, or just document that new learns are live after the next `serve` restart. Given your single-threaded model, I recommend refactoring `DB` to a reloadable `RefCell`/`OnceLock<Mutex<…>>` so learn→fire works without a restart.\n\n**Web API** (add to `web.rs` `route()`), single-threaded/blocking to match the rest:\n```\nPOST /api/ir/learn        body {\"timeout_ms\":8000}\n      -> waits for a button, returns {\"ok\":true,\"carrier\":38000,\"us\":[...],\"count\":N}\n         (calls i2s::capture in a poll loop + decode; NO storage yet)\nPOST /api/ir/learn/save   body {\"type\":\"TVs\",\"brand\":\"Custom\",\"model\":\"...\",\n                                \"device\":\"custom-0001\",\"function\":\"Power\",\n                                \"carrier\":38000,\"duty\":33,\"us\":[...]}\n      -> db::save_learned(...); {\"ok\":true}\n```\nReplay uses the **existing** `POST /api/ir/send {\"device\":\"custom-0001\",\"function\":\"Power\"}`. Nothing new needed — that already does `db::lookup` → `blast_resp` → `i2s::blast(carrier, duty, 1, select, seq, Drain)`.\n\nAlso add a CLI `irapi learn2` (or repurpose `cmd_learn`, which currently hits hal's dead LTCP path) to call `i2s::capture` + `decode` + `db::save_learned` for headless bring-up and round-trip testing.\n\n---\n\n## E) ORDERED next-actions (serial console) — cheapest experiment first\n\nDo these on the UART shell (`getty` on ttyS0 per your `rcS.local`). Each builds on the last.\n\n**E-1 — Prove I2S-RX returns live samples (the make-or-break test).** `[LIVE-TEST]`\nCheapest possible: no Rust changes needed yet.\n```sh\ninsmod /lib/modules/$(uname -r)/cc2544.ko        # if IR front-end needs it\ncat /lib/firmware/cc2544.bin > /dev/rffw\n# then, with NOTHING holding /dev/i2s:\n( sleep 3; echo \"PRESS A REMOTE NOW\" ) &\ndd if=/dev/i2s bs=192 count=200 2>/dev/null | od -An -tx1 | head\n```\n- If `dd` returns quickly and the hex is **not** all `ff`/`00` while you press a remote → I2S-RX works. Proceed to E-2.\n- If `dd` blocks forever or returns constant idle → try the `I2S_START` kick (write a 3-line Rust `capture()` test that does `ioctl(fd, I2S_START, 0)` before reading), i.e. `[LIVE-TEST]` (b). \n- If still nothing → send IRCAMERA ENABLE/CLOCK over `/dev/rfspi` first (Section B-fallback frame), retry.\n- If STILL nothing → the board genuinely routes IR only through cc2544; fall back to Section B-fallback (needs a live cc2544 firmware edge-packet trace — expensive, do last).\n\n**E-2 — Determine sample rate + raw-vs-demodulated, and calibrate µs/sample.** `[LIVE-TEST]`\nCapture a known NEC remote via `i2s::capture(400)` and dump the RLE runs (before scaling). Check:\n- The first (leader) mark's sample count `S0` → `us_per_sample = 9000/S0`. Record it. (Confirms/refutes the 16x.)\n- Whether marks contain many short 1/0 sub-runs (RAW carrier → enable `estimate_carrier`+`envelope_merge`) or are clean (demodulated → default 38 kHz, merge = no-op).\n\n**E-3 — Parse to a mark/space list.** Wire `decode()` with the calibrated constant; print `carrier_hz` + the µs vector. Sanity-check against the known remote (NEC: 9000/4500 leader, ~560 bit marks, ~560/1690 spaces).\n\n**E-4 — Store + replay round-trip.** `db::save_learned` → `/cache/custom.json`, then fire it back with your **existing** `i2s::blast` (`irapi fire --device custom-0001 --function Power` or `POST /api/ir/send`) at the *real* device. If the appliance controls the target device from a learned code, the whole chain is proven end-to-end.\n\n**E-5 — Harden.** Frame-segmentation on >20 ms gaps, min-edge-count reject, leader-plausibility check, average carrier over multiple marks, clamp µs≥30 and chunk >32767 for storage. Add the web `POST /api/ir/learn[/save]` endpoints and a \"waiting for button / captured / saved\" UI state.\n\n---\n\n## Section B-fallback — cc2544 `/dev/rfspi` protocol (use ONLY if E-1 fails)\n\nFully reversed from the unstripped `cc2544.ko`; kept here so you're not blocked if IR turns out to route through cc2544 on your board.\n- Open `/dev/rfspi` O_RDWR. **Command = `write(fd, &[opcode, args...], N)`** — the driver bit-bangs it MSB-first, full-duplex, brackets GPIO8(CS) low/high, waits GPIO6==0 ready (≤250 jiffies else `-EIO`), and locks the shared NOR bus. `write()` returns the response length.\n- **Readback = `read(fd, buf, cap)`** returns ONE queued packet `[type_byte, payload...]`; length is decided by the chip's returned type via the driver switch: type `3`→7B, `0x10`→7B, `0x11`→20B, `0x12`→30B, `0x20`→32B, `4`/`8`→1B, `0`/`1`/`2`→0B.\n- **Notify:** unsolicited packets (RF buttons; per the HW-RE doc, possibly captured IR edges) arrive via GPIO14→IRQ46; the driver worker clocks the chip and enqueues — you just `read(/dev/rfspi)`.\n- Opcodes (hal name table @0x461dc4): **IRCAMERA CLOCK=0x13, IRCAMERA ENABLE=0x1a**. Frame shape from hal's logger: `byte[0]`=type (0xa1/0xa2), `byte[1]`=opcode, `byte[2..]`=payload; hal transacts via `libhal_fw_cmd`: `write(fd, cmd, 7)` then `read(fd, resp, 10)` with a ~1 s SIGALRM, accepting when `resp[0..5]==cmd[0..5]`.\n- **UNPROVABLE statically:** the exact arg bytes for 0x13/0x1a and the edge-packet payload format — hal never issues them for IR capture. You'd need a live cc2544 firmware trace. This is why Path A is the recommendation.\n\n---\n\n### Files this touches\n- `service/rcS.local` — add the cc2544 INIT block (Section A).\n- `service/irapi/src/i2s.rs` — add `capture()` + `I2S_FREQ_CAP` (Section B).\n- `service/irapi/src/ir.rs` (or new `learn.rs`) — `decode()`, `estimate_carrier`, `envelope_merge`, `Learned` (Section C).\n- `service/irapi/src/db.rs` — `merge_custom()`, `save_learned()`, reloadable `DB` (Section D).\n- `service/irapi/src/web.rs` + `main.rs` — `POST /api/ir/learn[/save]`, a working `learn2` CLI; replay reuses existing `/api/ir/send` and `i2s::blast` (Section D).\n- Runtime data: `/cache/custom.json` on the jffs2 overlay (Section D).\n\n**Load-bearing unknowns to resolve on-device, in priority order:** (1) does I2S O_RDONLY read return live RX samples, and does it need an `I2S_START` kick — E-1; (2) real µs-per-sample (the 16x) and raw-vs-demodulated — E-2; (3) whether IRCAMERA ENABLE/CLOCK are required to route the photodiode — Section A `[LIVE-TEST]`."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Research"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "re:cc2544-rfspi-driver",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a20b340e434439388",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783051409466,
      "queuedAt": 1783051409400,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Minimal test of cc2544 driver interface.",
      "promptPreview": "GOAL: Add IR LEARN (capture) to a standalone Harmony-Hub IR appliance. We already TRANSMIT IR\ndirectly from Rust via the AR9331 I2S peripheral (/dev/i2s), with Logitech's hal REMOVED. Now we\nneed to RECEIVE/capture IR from a physical remote so users can learn codes. On this board the IR\nRECEIVER is the TI cc2544 (the \"IR camera\" / \"IRCAMERA\"), driven by cc2544.ko via /dev/rfspi\n(char 10,59) with f…",
      "lastProgressAt": 1783052221798,
      "tokens": 132170,
      "toolCalls": 26,
      "durationMs": 812328,
      "resultPreview": "{\"summary\":\"Minimal test of cc2544 driver interface.\",\"confirmed\":[\"rfspi is misc 10,59 and rffw is misc 10,60\"],\"uncertain\":[\"start capture opcode is in hal\"],\"spec\":\"open rfspi O_RDWR, write command, read node.\",\"sources\":[\"cc2544.ko\"]}"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "re:hal-ir_read-capture",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "ab7886054d723d43d",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783051409496,
      "queuedAt": 1783051409400,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "MAJOR CORRECTION to the task premise: stock hal does NOT ca…",
      "promptPreview": "GOAL: Add IR LEARN (capture) to a standalone Harmony-Hub IR appliance. We already TRANSMIT IR\ndirectly from Rust via the AR9331 I2S peripheral (/dev/i2s), with Logitech's hal REMOVED. Now we\nneed to RECEIVE/capture IR from a physical remote so users can learn codes. On this board the IR\nRECEIVER is the TI cc2544 (the \"IR camera\" / \"IRCAMERA\"), driven by cc2544.ko via /dev/rfspi\n(char 10,59) with f…",
      "lastProgressAt": 1783052374286,
      "tokens": 127395,
      "toolCalls": 45,
      "durationMs": 964789,
      "resultPreview": "{\"summary\":\"MAJOR CORRECTION to the task premise: stock hal does NOT capture IR through the cc2544 \\\"IR camera\\\" / /dev/rfspi. hal's IR-learn path (ir_cap -> libhal_ir_cap_raw_read -> ir_read @0x423fa4 -> ir_read_thread @0x423994) reads RAW PCM from /dev/i2s (the SAME I2S peripheral used for TX, opened O_RDONLY), bit-demodulates it, and converts sample-run lengths to microseconds. I confirmed this…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "re:capture-format-firmware",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "ac9fa2ddb66fbffed",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783051409497,
      "queuedAt": 1783051409401,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "MAJOR CORRECTION to the task premise: on this board the cap…",
      "promptPreview": "GOAL: Add IR LEARN (capture) to a standalone Harmony-Hub IR appliance. We already TRANSMIT IR\ndirectly from Rust via the AR9331 I2S peripheral (/dev/i2s), with Logitech's hal REMOVED. Now we\nneed to RECEIVE/capture IR from a physical remote so users can learn codes. On this board the IR\nRECEIVER is the TI cc2544 (the \"IR camera\" / \"IRCAMERA\"), driven by cc2544.ko via /dev/rfspi\n(char 10,59) with f…",
      "lastProgressAt": 1783052902331,
      "tokens": 196704,
      "toolCalls": 36,
      "durationMs": 1492834,
      "resultPreview": "{\"summary\":\"MAJOR CORRECTION to the task premise: on this board the captured IR waveform does NOT come out of /dev/rfspi. /dev/rfspi is only the cc2544 CONTROL channel (it enables/clocks the IR-receiver front-end). The actual captured IR data is read from /dev/i2s (the AR9331 I2S peripheral in RECEIVE mode) as an oversampled 1-bit-per-sample bitstream. Proof: hal's ir_init(0x424244) stores a singl…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "synth:ir-capture-spec",
      "phaseIndex": 2,
      "phaseTitle": "Synthesize",
      "agentId": "a7c66ca50171efe57",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783052902354,
      "queuedAt": 1783052902351,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /Volumes/MacEXT/code/logitechharmonyhub/service/irapi/sr…",
      "promptPreview": "GOAL: Add IR LEARN (capture) to a standalone Harmony-Hub IR appliance. We already TRANSMIT IR\ndirectly from Rust via the AR9331 I2S peripheral (/dev/i2s), with Logitech's hal REMOVED. Now we\nneed to RECEIVE/capture IR from a physical remote so users can learn codes. On this board the IR\nRECEIVER is the TI cc2544 (the \"IR camera\" / \"IRCAMERA\"), driven by cc2544.ko via /dev/rfspi\n(char 10,59) with f…",
      "lastProgressAt": 1783053112243,
      "tokens": 76488,
      "toolCalls": 8,
      "durationMs": 209889,
      "resultPreview": "I have everything I need: the two RE findings plus the actual Rust codebase (`i2s.rs` TX driver, `db.rs` catalog, `ir.rs`, `web.rs` API, `rcS.local`). Here is the implementation spec.\n\n---\n\n# IR LEARN — implementation spec (drop straight into `service/irapi`)\n\n## TL;DR decision (read first)\n\nThe task premise says \"capture edges off `/dev/rfspi`.\" **Both deep-RE findings independently prove, from t…"
    }
  ],
  "totalTokens": 532757,
  "totalToolCalls": 115
}