{
  "summary": "Design the offline IR code database + protocol-encoder integration for the standalone Harmony Hub IR appliance (sources, encoders incl. Midea AC, on-device storage, web UI/API, phased plan)",
  "agentCount": 5,
  "logs": [
    "parallel[2] failed: agent({schema}): StructuredOutput retry cap (5) exceeded — 5 failed calls with no valid output"
  ],
  "result": {
    "research": [
      {
        "key": "sources",
        "summary": "Five sources evaluated for an offline, on-device IR database on the ~4MB AR9331 Rust appliance. Flipper-IRDB (CC0 public domain) is the clear primary: its AC files are already stored as raw microsecond mark/space lists at 38kHz — a 1:1 feed into i2s::blast with zero decoding — and it ships 130 AC brand folders including both Danby and Midea. IRDB (probonopd) is tiny (~1.5MB) and compact but its custom license requires giving the maintainer free hardware units and it has essentially no AC coverage. SmartIR (MIT) has Midea climate codes but they are Broadlink-base64 encoded (needs a 32.84us-tick decoder) and carrier-less. IRremoteESP8266 (LGPL) is not a database but a C++ encoder library with explicit Danby DAC + Midea 48-bit protocol — reimplement its algorithm natively in Rust for a fully STATEFUL Danby/Midea encoder (any temp/mode/fan on the fly, ~1-2KB code, no data blob).",
        "recommendation": "Bundle a COMBO, not one source. (1) PRIMARY: a curated subset of Flipper-IRDB (CC0) as the on-device RAW database — include only brands the user owns (their TVs/AVR plus ACs/Danby and ACs/Midea). AC and most captured buttons are already raw microsecond lists and fire directly through i2s::blast with no decoder. (2) BEST FOR THE AC: a native Rust STATEFUL Midea/Danby encoder, reimplemented from the IRremoteESP8266 ir_Midea protocol description (timings/algorithms are not copyrightable; do NOT copy LGPL source). It generates any power/mode/temp/fan/swing combination on-device (~1-2KB code, near-zero data) instead of relying on a fixed set of captured buttons — the right primitive for a REST/HA climate entity. (3) OPTIONAL: a small set of stateless protocol encoders (NEC1/NECext, RC5/RC6, Sony SIRC, Kaseikyo, Samsung32) so you can also consume Flipper \"parsed\" records and, if ever desired, IRDB's compact CSV. Do NOT bundle IRDB wholesale (free-units license clause) and do NOT depend on SmartIR/Broadlink as the transport (carrier-less, extra decode step). This fits well under 4MB: the AC subset is a few hundred KB, and stateful encoders are pure code.",
        "details": "=== SOURCE-BY-SOURCE ===\n\n1) FLIPPER-IRDB (github Lucaslhm/Flipper-IRDB) — RECOMMENDED PRIMARY.\n- LICENSE: CC0-1.0 (public domain dedication, confirmed via GitHub API license field). Redistribute on-device freely, no attribution, no notification. Best license of all candidates.\n- SIZE: whole repo ~42MB (41,941 KB). Far too big whole, but trivially subsettable per-file; the ACs/ tree is a small fraction.\n- FORMAT: plain-text \".ir\" files. Header: \"Filetype: IR signals file\\nVersion: 1\". Each button is a record separated by \"#\". Two record kinds:\n  RAW (what all AC files use):\n    name: POWER\n    type: raw\n    frequency: 38000\n    duty_cycle: 0.330000\n    data: 4459 4369 593 1562 590 487 599 1555 ...   (space-separated microseconds, alternating mark space starting with MARK; max 1024 values)\n  PARSED (most TV/AVR files):\n    name: Power\n    type: parsed\n    protocol: NECext        (also NEC, NEC42, Samsung32, RC5, RC6, SIRC, SIRC15, SIRC20, Kaseikyo, RCA, Pioneer)\n    address: EE 87 00 00     (little-endian 4-byte hex)\n    command: 5D A0 00 00\n- COVERAGE: huge across all device types. ACs/ has 130 brand folders including Admiral, Arctic_King, Carrier, Chigo, Daikin, DANBY, Frigidaire, Fujitsu, GREE, Haier, Hisense, Hitachi, Kenmore, LG, MIDEA, Mitsubishi, Panasonic, Samsung, TCL, Toshiba, Trotec, Whynter, etc.\n- AC/DANBY/MIDEA: confirmed ACs/Danby and ACs/Midea both exist. Example verified: ACs/Midea/Midea_AC_MAW05R1WBL.ir contains ~12 raw buttons (POWER, TEMP-, TEMP+, ...) all type:raw at 38000/0.33.\n- CONVERSION: essentially none. RAW data list -> i2s::blast(carrier=frequency, duty=duty_cycle*100, select, marks/spaces) verbatim; the list is already alternating mark/space in microseconds. PARSED -> needs a small per-protocol encoder (see item 5 encoders).\n\n2) IRDB (github probonopd/irdb) — compact but licensing + no-AC blockers.\n- LICENSE: custom \"Other/NOASSERTION\". Conditions to ship in a product: (a) open a GitHub issue notifying the project, (b) include attribution notice \"Contains/accesses irdb by Simon Peter and contributors, used under permission...\", (c) provide up to THREE fully-licensed units of your product free (incl. shipping) to the maintainer on request. The free-units clause is a real blocker for a hobby appliance.\n- SIZE: ~1.5MB (1547 KB) for the ENTIRE database — extremely compact because it stores protocol params, not timings.\n- FORMAT: CSV, one file per device/subdevice, path codes/<Manufacturer>/<DeviceType>/<device>,<subdevice>.csv (e.g. codes/Generic/LED_TV/0,191.csv). Five columns: functionname,protocol,device,subdevice,function. Example row: Power,NEC1,4,-1,8  (subdevice -1 means \"not used\"). Rows sorted ascending by function.\n- COVERAGE: hundreds of manufacturers, thousands of devices — strong for TVs, STBs, AVRs, DVD. \n- AC/DANBY/MIDEA: essentially NONE usable. IRDB's one-function-per-row scalar model cannot represent stateful AC remotes (which encode temp+mode+fan+checksum into each frame). Do not rely on it for the AC.\n- CONVERSION: needs a full protocol expander. protocol names map to IRP definitions (NEC1, NEC2, RC5, RC6, Sony12/15/20, Panasonic/Kaseikyo, Samsung...). device=address, subdevice=second address byte (or derived when -1), function=command. Expand via the IRP timing rules (same math IrScrutinizer/MakeHex/DecodeIR use). You must implement those encoders yourself.\n\n3) SmartIR (github smartHomeHub/SmartIR) — MIT, has Midea, but heavier path.\n- LICENSE: MIT (repo license field). Redistributable on-device with attribution.\n- FORMAT: one JSON per numeric device_code in codes/climate/<code>.json. Top-level: manufacturer, supportedModels, supportedController (Broadlink/Xiaomi/ESPHome/LOOKin/MQTT), commandsEncoding (\"Base64\"/\"Hex\"/\"Raw\"), minTemperature, maxTemperature, precision, operationModes[], fanModes[], and commands nested operationMode->cool->temperature->\"16\"->fanMode->auto = \"<IR STRING>\", plus commands.power.on/off. \n- IR PAYLOAD: for the common Broadlink controller it is Broadlink base64. Verified codes/climate/1380.json = Midea, Broadlink base64, 18-30C, modes heat/cool/heat_cool/fan_only, fans low/mid/high; each command ~300-500+ chars of base64.\n- COVERAGE: climate-focused, ~hundreds-1000s of device_codes; Midea = code 1380 (Danby window units are Midea-based, so Midea codes generally drive Danby).\n- AC/DANBY/MIDEA: yes via Midea codes (1380 and others). But it is a fixed mode x temp x fan GRID of captured frames (bulky), not a generator.\n- CONVERSION: base64-decode -> Broadlink packet -> microsecond list (see Broadlink format below), then i2s::blast at ASSUMED 38kHz (Broadlink packets DO NOT store carrier frequency). Extra decode step and lost carrier metadata make this inferior to Flipper raw or the native encoder.\n\n4) LIRC (.lircd.conf) — broad legacy TVs, weak AC, murky provenance.\n- LICENSE: LIRC project is GPL, but individual community lircd.conf files have mixed/unclear provenance; redistribution license is per-file and often unstated. Risky to bundle blindly.\n- FORMAT: text \"remote { ... }\" blocks. Two modes: SPACE_ENC with params header <pulse> <space>, one <pulse> <space>, zero <pulse> <space>, ptrail, gap, plead, pre_data_bits/pre_data, then codes { KEY 0x... }. Or flags RAW_CODES with name + a list of raw microsecond durations. All timings in microseconds.\n- COVERAGE: large legacy TV/VCR/STB set; AC coverage thin and inconsistent.\n- CONVERSION: SPACE_ENC -> synthesize timings from header/one/zero/pre/post/gap and the per-key hex; RAW_CODES -> direct microsecond list. Requires a config parser + SPACE_ENC state machine. More work than Flipper for less AC value.\n\n5) IRremoteESP8266 (github crankyoldgit/IRremoteESP8266) — NOT a database; the ENCODER blueprint.\n- LICENSE: LGPL-2.1 (code). Do not link/copy the C++ into a static binary casually. BUT protocol timings and bit layouts are facts/algorithms (not copyrightable) — reimplement cleanly in Rust from the documented spec. This is the standard, safe approach.\n- WHAT IT GIVES: a correct, STATEFUL Midea encoder with explicit Danby DAC support (ir_Midea.h/.cpp).\n- MIDEA PROTOCOL (verified constants): carrier 38kHz. Header mark 4480us, header space 4480us, bit mark 560us, ONE space 1680us, ZERO space 560us, min gap 5440us. Message = 48 data bits, MSB-first per byte, sent then IMMEDIATELY followed by an entirely bit-inverted 48-bit copy (frame = HDR + 48 bits + trailer 560us mark + 5440us gap + HDR + 48 inverted bits + trailer + gap). \n- MIDEA 6-BYTE STATE (union, byte5 is the header byte): byte0 = checksum; byte1 = sensor temp(7b)/OnTimer + disable-sensor flag; byte2 = OffTimer(6b half-hours) + beep flag; byte3 = temperature(5b) + Fahrenheit flag; byte4 = mode(3b) + fan(2b) + sleep + power; byte5 = message type(3b) + header(5b)=0b10100. Port calcChecksum() from ir_Midea.cpp (sum/xor of the data bytes) rather than guessing.\n- DANBY DAC: build with the DANBY_DAC behavior — swingv maps to the Ion filter. Special 48-bit toggle codes (fire as-is, same modulation incl. inverted repeat): SwingV/Ion toggle 0xA201FFFFFF7C, Econo 0xA202FFFFFF7E, Light 0xA208FFFFFF75, Turbo 0xA209FFFFFF74.\n\n=== BROADLINK PACKET FORMAT (only if you ever ingest SmartIR) ===\nbase64-decode to bytes: [0]=0x26 (IR; 0xb2=RF433,0xd7=RF315); [1]=repeat count (0x00=play once); [2..3]=length of following payload, LITTLE-endian; then duration bytes; typically terminated by 0x0d 0x05. Each duration value v: if v<=255 one byte; if v>255 it is 0x00 then a 16-bit BIG-endian value (hi,lo). Microseconds = v * (269/8192 ms) = v * 32.84us (tick ~= 32.84us). No carrier stored -> assume 38000. Feed decoded us list alternating mark/space to i2s::blast.\n\n=== RECOMMENDED ON-DEVICE RECORD FORMAT (single flat binary blob, big-endian to match target) ===\nFile \"irdb.bin\":\n  MAGIC 4B \\\"IRDB\\\"; u8 version; u16 device_count; then a DEVICE INDEX, a STRING POOL, and a RECORD AREA.\n  DEVICE INDEX entry (per device/remote): u16 name_off (into string pool), u8 button_count, u32 buttons_off (into record area). \n  BUTTON entry: u16 name_off; u8 rec_type; u32 rec_off.\n  rec_type = 0 RAW:  u16 carrier_hz; u8 duty_pct; u16 n; then n x u16 durations_us (alternating mark/space, starts MARK). (Optional: LEB128 varints to shave ~30-40%; most values 400-4500 fit u16 anyway.)\n  rec_type = 1 PARAM: u8 protocol_id; u8 flags; then protocol-specific payload:\n     NEC1/NECext: u8 device, i16 subdevice(-1=derived), u8 function.\n     RC5/RC6: u8 system, u8 command, u8 toggle.\n     SIRC: u8 bits(12/15/20), u16 command, u16 device.\n     Kaseikyo/Samsung32: u16 address, u16 command.\n  rec_type = 2 MIDEA_STATE: 6 raw state bytes (encoder recomputes checksum + does inverted-repeat modulation).\n  rec_type = 3 MIDEA_TOGGLE: u32 code (e.g. 0xA201FFFF... low 48 bits packed) for Danby Ion/Econo/Light/Turbo.\nDispatch: RAW -> i2s::blast(carrier,duty,...,durations). PARAM -> protocol encoder -> us list -> i2s::blast. MIDEA_* -> stateful encoder -> us list -> i2s::blast.\n\n=== SIZE BUDGET (fits ~4MB overlay easily) ===\n- Flipper AC subset: AC .ir files run ~4-20KB each; 30-50 curated files ~= 300-600KB. Add the user's specific TVs/AVR (~a few KB each). \n- Native Midea/Danby + a handful of protocol encoders: pure Rust code, ~2-6KB total, ~0 data.\n- If you also want broad TV coverage compactly, IRDB-style PARAM records are ~6 bytes each — thousands fit in tens of KB — but only if you accept implementing the IRP encoders and skip IRDB's license by capturing/typing params yourself.\n\n=== WHERE TO GET DANBY/MIDEA SPECIFICALLY ===\n1) Flipper-IRDB ACs/Danby and ACs/Midea (CC0) — raw captures, fire directly. Best license.\n2) Native Rust encoder from IRremoteESP8266 ir_Midea (Danby DAC) — fully stateful, best UX for a climate REST entity.\n3) SmartIR codes/climate/1380.json (Midea, MIT, Broadlink base64) — fallback captured grid; Danby window units are Midea-protocol so 1380 generally works.",
        "risks": [
          "IRDB custom license requires opening a notification issue AND supplying up to three free product units to the maintainer on request — a real blocker for a hobby/one-off appliance. Prefer CC0 Flipper data; if you use IRDB params, re-derive them yourself rather than shipping their CSVs.",
          "IRremoteESP8266 is LGPL-2.1: do not statically link or copy its C++ into the Rust binary. Reimplement the Midea/Danby timings and bit layout independently (facts/algorithms, not copyrightable) and cite the spec, not the source.",
          "Broadlink-encoded codes (SmartIR) carry NO carrier frequency — you must assume 38kHz; a few AC brands use 36/40kHz and would fail. Flipper raw and the native encoder both keep/define the correct carrier, avoiding this.",
          "Midea checksum: port calcChecksum() exactly from ir_Midea.cpp — guessing the checksum will produce frames the AC silently ignores. Validate against a captured Flipper Danby/Midea frame before trusting generated states.",
          "Flipper 'parsed' records need per-protocol encoders (NEC/NECext/RC5/RC6/SIRC/Kaseikyo/Samsung). Until those exist, only 'raw' Flipper records are usable — fortunately AC files are all raw.",
          "Flipper raw data caps at 1024 timings per button; a few long AC frames could approach this. Size u16 count accordingly and verify no captured AC exceeds it.",
          "Flipper community IR captures vary in quality; a specific Danby/Midea model may need its own capture. Keep the native stateful encoder as the authoritative path for the user's actual unit and verify on hardware.",
          "SmartIR JSON codes are user-contributed; MIT covers the repo but individual code provenance is informal. Low legal risk but verify the specific file if bundled."
        ],
        "sources": [
          "https://github.com/probonopd/irdb",
          "https://github.com/probonopd/irdb/blob/master/README.md",
          "https://raw.githubusercontent.com/probonopd/irdb/master/LICENSE.md",
          "https://deepwiki.com/probonopd/irdb",
          "https://api.github.com/repos/probonopd/irdb",
          "https://github.com/Lucaslhm/Flipper-IRDB",
          "https://github.com/Lucaslhm/Flipper-IRDB/blob/main/ACs/Midea/Midea_AC_MAW05R1WBL.ir",
          "https://api.github.com/repos/Lucaslhm/Flipper-IRDB",
          "https://api.github.com/repos/Lucaslhm/Flipper-IRDB/contents/ACs",
          "https://github.com/flipperdevices/flipperzero-firmware/blob/dev/documentation/file_formats/InfraredFileFormats.md",
          "https://developer.flipper.net/flipperzero/doxygen/infrared_file_format.html",
          "https://github.com/smartHomeHub/SmartIR/blob/master/docs/CLIMATE.md",
          "https://raw.githubusercontent.com/smartHomeHub/SmartIR/master/codes/climate/1380.json",
          "https://api.github.com/repos/smartHomeHub/SmartIR/license",
          "https://github.com/crankyoldgit/IRremoteESP8266/blob/master/src/ir_Midea.h",
          "https://raw.githubusercontent.com/crankyoldgit/IRremoteESP8266/master/src/ir_Midea.cpp",
          "https://github.com/crankyoldgit/IRremoteESP8266/blob/master/SupportedProtocols.md",
          "https://api.github.com/repos/crankyoldgit/IRremoteESP8266/license",
          "https://www.lirc.org/html/lircd.conf.html",
          "https://github.com/mjg59/python-broadlink/blob/master/protocol.md"
        ]
      },
      {
        "key": "encoders",
        "summary": "Complete Rust-implementable IR protocol encoder spec (params -> mark/space µs list -> i2s::blast). Covers NEC + NEC-extended, Samsung 32/36, RC5, RC6, Sony SIRC 12/15/20, Panasonic/Kaseikyo, and the Midea/Danby AC protocol with a concrete, ready-to-blast POWER-OFF mark/space µs list. Every timing is tagged CONFIRMED (sourced) or APPROX. All space-encoded protocols share one primitive: header, then per-bit {bit_mark, space(one|zero)}, then footer; Manchester protocols (RC5/RC6) need a half-bit transition emitter instead.",
        "recommendation": "Implement a single generic pulse-distance encoder plus a separate Manchester encoder, driven by a small const table. Nine profiles cover everything the user needs:\n\n1. Generic pulse-distance encoder fn pdm(carrier, hdr_mark, hdr_space, bit_mark, zero_space, one_space, footer_mark, footer_gap, bits_msb_first: &[bool]) -> Vec<(bool,u16)>. This ONE function encodes NEC, NEC-ext, Samsung, Sony (with per-protocol constants), Kaseikyo, and each Midea frame.\n2. Manchester encoder for RC5/RC6 (bi-phase, half-bit transitions, RC6 needs a double-wide toggle bit).\n\nTable-drive the constants. Assemble the bit vector per protocol from device/subdevice/function fields (respecting LSB-vs-MSB and inversion rules below), then feed to i2s::blast(carrier, duty=1/3, ...). For Midea specifically: build the 48-bit value, send it MSB-first, footer gap, then send the fully bit-inverted 48-bit value, footer gap. Use the concrete 0xA1026FFFFFE2 POWER-OFF list in Details to validate on the user's Danby TODAY.\n\nCarrier note: NEC/Samsung/Midea = 38 kHz, Sony = 40 kHz, RC5/RC6 = 36 kHz, Kaseikyo = 37 kHz (36.7). Duty 1/3 is a safe universal default.",
        "details": "All timings µs. Convention for the µs list: alternating (mark, space) pairs; mark = carrier ON burst, space = carrier OFF. \"LSB first\" = bit0 emitted first.\n\n=== NEC (CONFIRMED, sbprojects.net/knowledge/ir/nec.php) ===\nCarrier 38 kHz, duty 1/4-1/3. AGC header: mark 9000, space 4500. Bit mark 560 (all bits). Zero space 560 (bit total 1125). One space 1690 (bit total 2250). Stop bit: mark 560 then long space (frame padded to 110 ms). Bit order LSB-first. Payload 32 bits sent in order: address(8) LSB-first, ~address(8), command(8) LSB-first, ~command(8). Repeat frame (key held, every 110 ms): mark 9000, space 2250, mark 560, gap. \nNEC-extended: drop the address-inversion; send 16-bit address as address_low(8)+address_high(8) LSB-first, then command(8)+~command(8). Same timings. A frame is \"extended\" simply because addr_high != ~addr_low.\nEncode: bits = LSBfirst(addr8) ++ LSBfirst(~addr8) ++ LSBfirst(cmd8) ++ LSBfirst(~cmd8). Feed to pdm(38000,9000,4500,560,560,1690,560,~40000-gap).\n\n=== Samsung32 (CONFIRMED, IRremoteESP8266 ir_Samsung.cpp) ===\nCarrier 38 kHz. Header mark 4500, space 4500 (EQUAL, unlike NEC). Bit mark 560, zero space 560, one space 1690. Footer mark 560 + gap (~2250/108ms period). LSB-first. 32 bits = address(8) ++ address(8 repeated, NOT inverted) ++ command(8) ++ ~command(8). \nSamsung36: 36-bit variant (some Samsung AC/BD). Structure: 16-bit customer + 4-bit + ... but for remotes use the 32-bit form. If Samsung36 needed: header 4500/4500, then 16 bits, mark 560+space ~4500 mid-gap, then 20 bits, footer. Mark Samsung36 layout APPROX (model-dependent); prefer Samsung32.\n\n=== RC5 (CONFIRMED, sbprojects) — MANCHESTER, not pulse-distance ===\nCarrier 36 kHz. Half-bit T = 889 (full bit 1778). 14 bits, MSB-first, bi-phase: logical 1 = space(889) then mark(889); logical 0 = mark(889) then space(889) (IEEE convention; 1 = low->high). Bits: S1(=1), S2(=1, becomes inverted cmd bit6 for RC5X extended giving 7-bit cmd), Toggle(flips each keypress), Address(5, MSB-first), Command(6, MSB-first). No explicit header; the S1 start bit's leading transition is the sync. Footer: idle >= 4 half-bits (~3.6 ms) then repeat at 114 ms. Manchester emitter: for each half-bit level, if same as previous level extend the run, else close previous run and start new. Simpler: emit transitions, coalescing adjacent equal levels into one (mark,us) or (space,us) of 889 or 1778.\n\n=== RC6 (mode 0 / RC6-0) (CONFIRMED timings, sbprojects; RC6A layout APPROX) — MANCHESTER ===\nCarrier 36 kHz. Unit T = 444 (2T=889). Leader: mark 2666 (6T), space 889 (2T). Then Start bit = 1 (Manchester, 1T mark +1T space). Then 3 Mode bits (mode 0 = 000), MSB-first, normal 1T half-bits. Then Trailer/Toggle bit = DOUBLE WIDTH: 2T per half (a '1' = mark 889 then space 889; a '0' = space 889 then mark 889). Then 8 Control(address) + 8 Information(command) bits, MSB-first, normal 1T Manchester. RC6 Manchester convention is OPPOSITE RC5: logical 1 = mark(high) THEN space(low); logical 0 = space THEN mark. Footer idle >= 2666 (6T), repeat ~107 ms. Watch the toggle bit's double width when coalescing runs.\n\n=== Sony SIRC 12/15/20-bit (CONFIRMED, sbprojects) ===\nCarrier 40 kHz, duty 1/3-1/4. Base T = 600. Header: mark 2400 (4T), space 600 (1T). Bit encoding is by MARK width (space always 600): logical 1 = mark 1200 (2T) + space 600; logical 0 = mark 600 (1T) + space 600. NO trailing footer bit (last bit ends with its space). LSB-first. Whole frame padded to 45 ms; command repeated >=3 times, each frame every 45 ms.\n- 12-bit: command(7, LSB-first) ++ address/device(5, LSB-first).\n- 15-bit: command(7) ++ address(8), LSB-first.\n- 20-bit: command(7) ++ address(5) ++ extended(8), LSB-first.\nEncode note: order on air is 7 command bits first (LSB), then address bits (LSB). pdm-like but ONE/ZERO differ in MARK not space, so use a variant emitter: (mark = one?1200:600, space 600).\n\n=== Panasonic / Kaseikyo 48-bit (CONFIRMED, Arduino-IRremote ir_Kaseikyo.hpp, IRMP) ===\nCarrier 37 kHz (period 36.7). Unit T = 432. Header: mark 3456 (8T), space 1728 (4T). Bit mark 432, zero space 432 (total 864), one space 1296 (total 1728). Footer mark 432 + gap. LSB-first, 48 bits. Field order on air (LSB-first within each field): Vendor/OEM code (16 bits; Panasonic = 0x2002, sent low byte 0x02 then 0x20), Vendor-parity (4 bits) + Genre1... — practical Panasonic layout: [OEM 16b=0x2002][device 8b][subdevice 8b][function 8b][checksum 8b]. Checksum byte = device XOR subdevice XOR function (XOR of the 3 payload bytes). (Some libs also XOR in the low OEM byte; for Panasonic 0x2002 the canonical is XOR of device^subdevice^function.) Emit all 48 bits LSB-first via pdm(37000,3456,1728,432,432,1296,432,gap). Mark the \"vendor parity nibble\" split APPROX; the byte-level [0x02,0x20,device,subdev,func,checksum] view is what to code.\n\n=================================================================\n=== MIDEA / DANBY AC (this is the important one) ===\nSources: IRremoteESP8266 ir_Midea.cpp/.h (CONFIRMED constants), balbachan Midea-BGH decode (CONFIRMED real capture), mpetroff decode.\n\nCARRIER: 38 kHz, duty ~1/3. Bit order MSB-first (IRremoteESP8266 sendMidea shifts from the top byte down). Pulse-distance (space) encoding.\n\nTIMINGS (IRremoteESP8266, tick=80µs) — CONFIRMED:\n  Header mark 4480, header space 4480. (Real captures ~4400/4400-4470; either works.)\n  Bit mark 560. Zero space 560. One space 1680. (Captures show ~600/450 and 600/1550 — Danby/BGH variant; the 560/560/1680 nominal decodes fine.)\n  Inter-frame footer: bit mark 560 then a long gap. IRremoteESP8266 kMideaMinGap ~3360-5000; real BGH capture uses 600 mark + 5100 space frame-separator. USE 560 mark + 5100 space between the two frames and as the trailing footer. (Frame-gap length is the only slightly-uncertain number; anything ~4000-6000 works — mark it APPROX.)\n\nFRAME LAYOUT — a full transmission = TWO 48-bit frames:\n  Frame A = the 48-bit data value, MSB-first.\n  Frame B = the SAME value fully bit-inverted (~value over 48 bits), MSB-first.\n  Each frame is [header][48 bits][footer bit + gap]. (This whole-frame inversion is the transmission-integrity mechanism; do NOT confuse with the per-byte checksum which lives INSIDE the 48-bit value.)\n\n48-bit value internal structure (IRremoteESP8266 union, byte5=MSB .. byte0=LSB):\n  byte5 = 0xA1 fixed prefix (Type/Header, 0b10100 header + type bits) \n  byte4 = flags: Power(bit), Sleep, Mode(3), Fan(2) \n  byte3 = Temp(5) + fahrenheit flag\n  byte2 = timer/beep\n  byte1 = sensor temp\n  byte0 = checksum (see below)\nWell-known ready-made state constants (IRremoteESP8266): \n  POWER ON  = 0xA1826FFFFF62\n  POWER OFF = 0xA1026FFFFFE2\n  (diff is the Power bit in byte4: 0x82 vs 0x02.)\nInternal checksum (byte0): reverse bits of bytes1..5, sum, take (256 - sum) mod 256, reverse bits of result. For the two constants above the checksum is already correct (0x62 / 0xE2), so you can blast them verbatim.\n\n>>> CONCRETE POWER-OFF COMMAND TO BLAST TODAY (value 0xA1026FFFFFE2) <<<\nBits MSB-first: 1010 0001 0000 0010 0110 1111 1111 1111 1111 1111 1110 0010\nInverted copy value = 0x5EFD9000001D.\nFull mark/space µs list (mark,space,mark,space,...). Rule: every mark=560; space=1680 for a '1' bit, 560 for a '0' bit.\n\nFRAME A:\n4480,4480,                      (header)\n560,1680, 560,560, 560,1680, 560,560,        (A=1010)\n560,560, 560,560, 560,560, 560,1680,         (1=0001)\n560,560, 560,560, 560,560, 560,560,          (0=0000)\n560,560, 560,560, 560,1680, 560,560,         (2=0010)\n560,560, 560,1680, 560,1680, 560,560,        (6=0110)\n560,1680, 560,1680, 560,1680, 560,1680,      (F=1111)\n560,1680, 560,1680, 560,1680, 560,1680,      (F)\n560,1680, 560,1680, 560,1680, 560,1680,      (F)\n560,1680, 560,1680, 560,1680, 560,1680,      (F)\n560,1680, 560,1680, 560,1680, 560,1680,      (F)\n560,1680, 560,1680, 560,1680, 560,560,       (E=1110)\n560,560, 560,560, 560,1680, 560,560,         (2=0010)\n560,5100                         (footer bit + frame gap)\n\nFRAME B (inverted, 0x5EFD9000001D):\n4480,4480,\n560,560, 560,1680, 560,560, 560,1680,        (5=0101)\n560,1680, 560,1680, 560,1680, 560,560,       (E=1110)\n560,1680, 560,1680, 560,1680, 560,1680,      (F=1111)\n560,1680, 560,1680, 560,560, 560,1680,       (D=1101)\n560,1680, 560,560, 560,560, 560,1680,        (9=1001)\n560,560, 560,560, 560,560, 560,560,          (0)\n560,560, 560,560, 560,560, 560,560,          (0)\n560,560, 560,560, 560,560, 560,560,          (0)\n560,560, 560,560, 560,560, 560,560,          (0)\n560,560, 560,560, 560,560, 560,560,          (0)\n560,560, 560,560, 560,560, 560,1680,         (1=0001)\n560,1680, 560,1680, 560,560, 560,1680,       (D=1101)\n560,5100                         (footer bit + final gap)\n\nCLI to test: irapi i2sraw --carrier 38000 --select 7 --us <the two frames concatenated>. If the Danby ignores 0xA1026FFFFFE2, also try POWER ON 0xA1826FFFFF62 (Danby remotes are frequently discrete on/off, sometimes a single toggle). For a full-state build (temp/mode/fan) reproduce the byte4/byte3 fields then recompute byte0 checksum.\n\nRust structure suggestion:\nstruct Pdm{carrier:u32, hdr:(u16,u16), bit_mark:u16, zero:u16, one:u16, foot:(u16,u16)}\nfn encode_pdm(p:&Pdm, bits:&[bool], out:&mut Vec<(bool,u16)>){ out.push((true,p.hdr.0)); out.push((false,p.hdr.1)); for &b in bits{ out.push((true,p.bit_mark)); out.push((false, if b {p.one} else {p.zero})); } out.push((true,p.foot.0)); out.push((false,p.foot.1)); }\nMidea: encode_pdm(A_bits) then encode_pdm(B_bits) with same Pdm. Sony: variant where mark varies (one=>1200,zero=>600) and space const 600, no footer bit. RC5/RC6: separate manchester(half_us, bits, convention) emitter that coalesces equal adjacent levels.",
        "risks": [
          "Midea inter-frame gap length varies by source (IRremoteESP8266 ~3360-5000µs vs BGH capture 5100µs) — marked APPROX; anything ~4000-6000µs decodes, but a too-short gap can make the AC read one 96-bit blob and reject it.",
          "Danby uses a Midea-family protocol but exact model timings (600/450/1550 in BGH capture vs 560/560/1680 nominal) and the power semantics (discrete OFF vs toggle) can differ per unit; the 0xA1026FFFFFE2 constant is from generic Midea and may need the ON value or a captured-from-remote value to actually toggle the user's specific Danby.",
          "IRremoteESP8266 internal byte-field order (which byte holds Power/Temp) is library-specific; the two ready-made constants sidestep this, but a full-state encoder must match the exact union bit layout in ir_Midea.h for the target model.",
          "Kaseikyo vendor-parity nibble and checksum formula have library variants (XOR of 3 vs 4 bytes); Panasonic 0x2002 canonical is XOR device^subdev^func — verify against a real Panasonic capture before shipping.",
          "RC6 Manchester bit convention is inverted relative to RC5 and the toggle bit is double-width; a naive shared Manchester emitter will produce wrong RC6 frames.",
          "Sony frames must be repeated >=3x at 45ms spacing or many Sony sets ignore them; a single frame is insufficient.",
          "Carrier accuracy matters for AC units and Sony (40kHz) / Kaseikyo (37kHz); the I2S bitstream generator must hit these carriers, not just 38kHz."
        ],
        "sources": [
          "https://github.com/crankyoldgit/IRremoteESP8266/blob/master/src/ir_Midea.cpp",
          "https://github.com/crankyoldgit/IRremoteESP8266/blob/master/src/ir_Midea.h",
          "https://alexisbalbachan.github.io/blog/2026/02/21/Decoding-Midea-BGH-Silent-Air-IR-Protocol.html",
          "https://mpetroff.net/2015/07/decoding-a-midea-air-conditioner-remote/",
          "http://veillard.com/embedded/midea.html",
          "https://github.com/sheinz/esp-midea-ir",
          "https://www.sbprojects.net/knowledge/ir/nec.php",
          "https://www.sbprojects.net/knowledge/ir/rc5.php",
          "https://www.sbprojects.net/knowledge/ir/rc6.php",
          "https://www.sbprojects.net/knowledge/ir/sirc.php",
          "https://github.com/Arduino-IRremote/Arduino-IRremote/blob/master/src/ir_Kaseikyo.hpp",
          "https://github.com/ukw100/IRMP/blob/master/src/irmpprotocols.h",
          "https://github.com/crankyoldgit/IRremoteESP8266/blob/master/src/ir_Samsung.cpp"
        ]
      },
      {
        "key": "ux",
        "summary": "Design for the on-device Web UI + REST API that browses/fires an offline IR code DB and manages custom codes on the AR9331 \"irapi\" appliance. Two clean surfaces map onto the single existing primitive i2s::blast(carrier,duty,bitmul,select,&[(is_mark,us)]): (a) a stateless TV/AVR path that mirrors Home Assistant's remote.send_command (device+command+num_repeats+delay_secs) so a one-line HA rest_command drives it; (b) a stateful AC path modeled as an \"instance\" with a PATCHable state that re-encodes and transmits the FULL protocol frame on every change (matching how IR AC protocols like Midea/SmartIR work — each press carries the entire state). UI is a single self-contained gzipped HTML+JS page: brand -> type -> model -> function-grid, a global port (i2s \"select\") selector, a \"repeat-while-held\" mode via bounded hold_ms chunks re-posted by the client, plus a Learn/Import modal.",
        "recommendation": "Ship ONE POST /api/ir/send that accepts several mutually-exclusive body shapes with a fixed precedence (raw_us > pronto > code_id > protocol+params > device+function) AND simultaneously accepts Home-Assistant field aliases (device, command, num_repeats, delay_secs) so a stock HA `rest_command` maps 1:1 with zero custom component. Keep TVs/AVRs stateless (fire-and-forget). Model the AC as a stateful \"instance\" (GET/POST /api/instance/{id}/state) that owns a last-commanded state, re-encodes the whole frame via the Midea encoder on every partial update, and blasts on the instance's port — do NOT ship a precomputed mode×fan×temp code matrix (it bloats the 4MB overlay); use the encoder and reserve captured presets only for functions the encoder can't reach (turbo/sleep/follow-me). For \"repeat while held,\" make the browser re-POST /api/ir/send with a server-bounded hold_ms (<=1000ms) chunk on each loop until pointerup — this keeps the single-threaded server safe from a wedged infinite transmit. Serve the SPA as a build-time-gzipped static blob (Content-Encoding: gzip, no runtime deflate) and hand-emit all JSON; write a ~150-line minimal JSON reader (object/string/number/bool/array-of-int, one level) only for /send and /import. Offer /api/ha/*.yaml generators that emit ready-to-paste HA config so integration is copy-paste.",
        "details": "DATA MODEL (exposed by API)\n- type: device class — tv, avr, stb, ac, fan, light, projector, media.\n- brand: manufacturer string.\n- device/profile: a code set, id = slug \"type/brand/model\" e.g. \"tv/samsung/N5300\". Has protocol + default carrier + function list + grid layout.\n- function: named button (\"power\",\"vol_up\"). Flags: repeatable (enables while-held), default_repeats.\n- code_id: addresses ONE precompiled command; either \"profile/function\" or a global compact id like \"nec:0x07:0x02\".\n- instance: a STATEFUL device (AC) the user has added; id like \"ac1\"; holds {profile, name, port, state}.\n- port: the i2s::blast `select` index (0..N multi-emitter). \"all\" = every emitter.\n\nREST API — BROWSE (GET, all responses application/json, Cache-Control: no-store)\n- GET /api/types -> {\"types\":[\"tv\",\"avr\",\"ac\",\"fan\",\"light\",\"stb\",\"projector\"]}\n- GET /api/brands?type=tv -> {\"brands\":[{\"name\":\"Samsung\",\"count\":14},{\"name\":\"Sony\",\"count\":9}]}\n- GET /api/devices?type=tv&brand=Samsung -> {\"devices\":[{\"id\":\"tv/samsung/N5300\",\"model\":\"UN..N5300\",\"protocol\":\"samsung36\",\"functions\":42}]}\n- GET /api/device/tv/samsung/N5300 ->\n  {\"id\":\"tv/samsung/N5300\",\"type\":\"tv\",\"brand\":\"Samsung\",\"protocol\":\"samsung36\",\"carrier\":38000,\"default_repeats\":1,\n   \"functions\":[{\"name\":\"power\",\"label\":\"Power\",\"icon\":\"power\"},{\"name\":\"vol_up\",\"label\":\"Vol +\",\"repeatable\":true}, ...],\n   \"layout\":[\"power\",\"source\",\"menu\",\"info\",\"-\",\"up\",\"vol_up\",\"ch_up\",\"-\",\"left\",\"ok\",\"right\",\"-\",\"down\",\"vol_down\",\"ch_down\",\"-\",\"d1\",\"d2\",\"d3\",\"d4\",\"d5\",\"d6\",\"d7\",\"d8\",\"d9\",\"d0\"]}\n   (layout gives grid order; \"-\" = row/section break; UI renders CSS-grid buttons from it.)\n- GET /api/device/ac/midea/generic ->  (AC profile advertises capabilities, not a code matrix)\n  {\"id\":\"ac/midea/generic\",\"type\":\"ac\",\"brand\":\"Midea\",\"protocol\":\"midea\",\"carrier\":38000,\"stateful\":true,\n   \"capabilities\":{\"power\":true,\"modes\":[\"auto\",\"cool\",\"dry\",\"heat\",\"fan_only\"],\n     \"temp\":{\"min\":17,\"max\":30,\"step\":1,\"unit\":\"C\"},\"fan\":[\"auto\",\"low\",\"med\",\"high\"],\n     \"swing\":[\"off\",\"vertical\"],\"toggles\":[\"turbo\",\"sleep\",\"eco\",\"follow_me\"]}}\n- GET /api/ports -> {\"ports\":[{\"select\":0,\"label\":\"Port 1 (front)\"},{\"select\":7,\"label\":\"Port 8 (blaster)\"}],\"default\":7}\n- GET /api/status -> {\"ok\":true,\"busy\":false,\"fw\":\"irapi 0.3\",\"uptime_s\":41233}\n\nREST API — FIRE  (POST /api/ir/send)  content-type application/json\nBody is ONE of these shapes; server evaluates in this PRECEDENCE (first present wins):\n  1) {\"raw_us\":[9000,4500,560,560, ...],\"carrier\":38000,\"duty\":33,\"port\":7}      // arbitrary -> i2s::blast directly\n  2) {\"pronto\":\"0000 006D 0022 0002 015B 00AD ...\",\"port\":7}                      // Pronto hex -> parsed to us list\n  3) {\"code_id\":\"tv/samsung/N5300/power\",\"port\":7}                               // one precompiled code\n  4) {\"protocol\":\"nec\",\"device\":7,\"subdevice\":-1,\"function\":2,\"carrier\":38000,\"port\":7} // parametric -> encoder\n  5) {\"device\":\"tv/samsung/N5300\",\"function\":\"power\",\"port\":7}                   // profile + function (native)\nRepeat controls (accepted on any shape):\n  \"num_repeats\": int (default = device.default_repeats or 1)   // HA alias; native alias \"repeats\"\n  \"delay_secs\": float seconds between frames (default protocol gap ~0.04) // HA alias; native alias \"gap_ms\"\n  \"hold_ms\": int, server-CLAMPED to [0,1000]. If >0 it overrides num_repeats: blast the frame back-to-back\n             (protocol gap between frames) for >=hold_ms then return. Used by while-held; client re-posts.\nHA-ALIAS NOTE: field \"command\" is accepted as a synonym for \"function\", so HA remote.send_command payloads\nwork verbatim. \"device\" doubles as HA's device grouping and as our profile id.\nResponses:\n  200 {\"ok\":true,\"emitted\":{\"protocol\":\"samsung36\",\"carrier\":38000,\"port\":7,\"frames\":1,\"duration_us\":67500}}\n  400 {\"ok\":false,\"error\":\"bad_request\",\"detail\":\"raw_us empty\"}\n  404 {\"ok\":false,\"error\":\"unknown_device\",\"detail\":\"tv/samsung/N5300\"}\n  409 {\"ok\":false,\"error\":\"busy\"}          // another blast in flight (single-thread guard)\n  503 {\"ok\":false,\"error\":\"hw\",\"detail\":\"/dev/i2s EIO\"}\n- POST /api/ir/stop -> {\"ok\":true}  // clears any held/looping session (belt-and-suspenders; hold_ms is self-bounding)\n\nREST API — STATEFUL AC  (instances)\n- GET  /api/instances -> {\"instances\":[{\"id\":\"ac1\",\"name\":\"Bedroom AC\",\"profile\":\"ac/midea/generic\",\"port\":7}]}\n- POST /api/instances  {\"profile\":\"ac/midea/generic\",\"name\":\"Bedroom AC\",\"port\":7} -> {\"id\":\"ac1\"}\n- DELETE /api/instance/ac1 -> {\"ok\":true}\n- GET  /api/instance/ac1/state ->\n   {\"power\":\"on\",\"mode\":\"cool\",\"temp\":22,\"fan\":\"auto\",\"swing\":\"off\",\"turbo\":false,\"sleep\":false,\n    \"source\":\"last_commanded\",\"open_loop\":true}   // appliance can't read the AC; state = last transmitted\n- POST /api/instance/ac1/state  {\"temp\":23}   // PARTIAL patch: merge onto current state, re-encode FULL Midea\n   frame, blast on port 7, persist new state. Returns full new state + emit info:\n   {\"ok\":true,\"state\":{\"power\":\"on\",\"mode\":\"cool\",\"temp\":23,\"fan\":\"auto\",\"swing\":\"off\"},\n    \"emitted\":{\"protocol\":\"midea\",\"carrier\":38000,\"port\":7,\"frames\":2,\"duration_us\":10240}}\n   (Absolute/idempotent variant: same endpoint with a complete state object; supports HA owning truth.)\n- POST /api/instance/ac1/resync -> re-transmit current full state unchanged (recover after AC missed a frame).\nWHY full-frame every change: IR AC protocols (Midea/SmartIR-style) are edge-triggered \"whole-state\" — every\nbutton press sends the complete {power,mode,temp,fan,swing} word, so the encoder must rebuild and send it all;\nthis is exactly why SmartIR's climate JSON keys a code per (operationMode -> fanMode -> temperature) cell.\n\nREST API — CUSTOM CODES / LEARN / IMPORT\n- POST /api/learn/start {\"port\":7} -> {\"id\":\"lrn7\",\"status\":\"waiting\"}   // begins IR receive capture (Harmony HW has an RX)\n- GET  /api/learn/lrn7 -> {\"status\":\"waiting\"} | {\"status\":\"captured\",\"carrier\":38000,\"raw_us\":[...],\"edges\":67}\n- POST /api/learn/lrn7/test -> re-blast the just-captured buffer so the user can confirm before saving\n- POST /api/codes/import ->\n   {\"name\":\"living tv\",\"function\":\"power\",\"format\":\"pronto|raw|broadlink_b64|lirc\",\n    \"data\":\"0000 006D ...\",\"carrier\":38000,\"port\":7}  -> {\"ok\":true,\"code_id\":\"custom/living tv/power\"}\n- GET    /api/codes/custom -> list of user codes/profiles\n- DELETE /api/codes/custom/{id} -> {\"ok\":true}\nCustom codes live in the writable overlay as a separate small file so a factory DB stays read-only.\n\nHOME-ASSISTANT INTEGRATION (concrete, no custom component)\nTV/AVR (stateless) — one rest_command mirrors remote.send_command:\n  rest_command:\n    irapi_send:\n      url: http://HUB_IP/api/ir/send\n      method: POST\n      content_type: 'application/json'\n      payload: '{\"device\":\"{{ device }}\",\"command\":\"{{ command }}\",\"port\":{{ port|default(7) }},\"num_repeats\":{{ num_repeats|default(1) }},\"delay_secs\":{{ delay_secs|default(0.04) }}}'\n  # call: action: rest_command.irapi_send  data: {device: \"tv/samsung/N5300\", command: \"power\"}\nAC (stateful) — recommended: HA owns state via an MQTT climate, one automation forwards assembled full state:\n  - mqtt climate entity gives HA a first-class thermostat card; on any of its state topics changing, a HA\n    automation calls rest_command.irapi_ac with the COMPLETE state, and our /api/instance/{id}/state (absolute\n    form) encodes+blasts. Device stores no HA truth, needs no MQTT client (no crates on-device). \n  Generators to remove hand-editing:\n  - GET /api/ha/rest_command.yaml            -> paste-ready rest_command block\n  - GET /api/ha/device/{id}.yaml             -> script.* per function (remote-style) for one TV/AVR\n  - GET /api/ha/instance/{id}.yaml           -> mqtt climate + forwarding automation for one AC\n  - GET /api/ha/package/{id}.yaml            -> full HA package (all of the above)\nOptional turnkey: GET /api/export/smartir/{id}.json emits a SmartIR-shaped climate file (top-level\nmanufacturer, supportedModels[], supportedController, commandsEncoding:\"Raw\", minTemperature, maxTemperature,\nprecision, operationModes[], fanModes[], commands{ \"off\":<raw>, <mode>:{ <fan>:{ \"<temp>\":<raw> }}}) where each\ncell is the encoder output — for users who prefer the SmartIR card; generated on demand so it never sits in overlay.\n\nTRANSPORT / SERVER NOTES (single-thread, no crates, 4MB overlay)\n- SPA is one self-contained page (inline CSS+JS, no external refs). Store it build-time-GZIPPED in the binary;\n  serve GET / with Content-Encoding: gzip + Cache-Control: public,max-age=31536000,immutable + a ?v=hash. No\n  runtime deflate. Target < 40KB gzipped.\n- Hand-emit JSON. Hand-write a minimal JSON READER (~150 lines) for request bodies: supports object, string,\n  number (int/float), bool, null, and flat arrays of numbers (raw_us). One nesting level for state patch is enough.\n- Also accept application/x-www-form-urlencoded on browser GET/POST convenience routes; reserve JSON parse for\n  /api/ir/send, /api/instance/*/state, /api/codes/import (the ones HA hits).\n- Single-thread serialization is a feature: one blast at a time. Guard with a busy flag -> 409. hold_ms is\n  clamped so a lost pointerup can never wedge the emitter.\n- while-held client loop (repeatable buttons only):\n    onpointerdown: held=true; loop: await POST /api/ir/send {device,function,port,hold_ms:250}; if held repeat.\n    onpointerup/leave/cancel: held=false (let the in-flight 250ms chunk finish). Default resend period inside a\n    chunk = protocol frame period (NEC ~108ms; generic default ~100-130ms).\n\nASCII WIREFRAMES\n(1) Browse / Fire\n+---------------------------------------------------------------+\n|  irapi   [ TV v ]  Brand:[ Samsung v ]  Model:[ N5300 v ]      |\n|                                Port:[ 8 v ]  Hold:[on]   (gear)|\n+-------------------+-------------------------------------------+\n|  DEVICES          |   Samsung N5300   (samsung36 . 38 kHz)    |\n|  > Samsung  (14)  |  [ PWR ] [ SRC ] [ MENU] [ INFO]          |\n|  > Sony      (9)  |          [  ^  ] [VOL+ ] [ CH+ ]          |\n|  > LG        (7)  |  [ <OK> ]        (VOL+/CH+ = hold-repeat)  |\n|  > Danby AC  (1)  |          [  v  ] [VOL- ] [ CH- ]          |\n|  + Import / Learn |  [1][2][3] [4][5][6] [7][8][9] [0]        |\n|                   |  status: sent PWR @port8 (67.5 ms) OK     |\n+-------------------+-------------------------------------------+\n(2) AC state builder (stateful instance)\n+---------------------------------------------------------------+\n|  Bedroom AC  (Midea . encoder)     Port:[8 v]    [ Resync ]    |\n+---------------------------------------------------------------+\n|     +-----------+   Mode : (Auto)(Cool*)(Dry)(Heat)(Fan)      |\n|     |   22 C    |   Fan  : (Auto*)(Low)(Med)(High)            |\n|     |  ^     v  |   Swing: (Off*)(Vertical)                   |\n|     +-----------+   Turbo[ ] Sleep[ ] Eco[ ] FollowMe[ ]      |\n|     Power: [ ON ]                                             |\n|  Every change transmits the FULL state frame.                |\n|  Last sent: cool 22 auto   (open-loop; no feedback)          |\n+---------------------------------------------------------------+\n(3) Learn / Import modal\n+------------------ Import / Learn --------------------+\n| (o) Learn from remote    ( ) Paste code              |\n|  Point remote at hub, press a button   [ Start ]     |\n|  Captured: 38 kHz, 67 edges   [>play]  OK            |\n|  Save as: Device[ Living TV ] Function[ power ]      |\n|           Port default[ 8 v ]        [ Save ]        |\n|  Paste: ( )Pronto ( )raw us ( )Broadlink b64 ( )LIRC |\n|  [__________________________________________]        |\n+------------------------------------------------------+",
        "risks": [
          "Single-threaded server blocks during a blast; a while-held loop of 250ms chunks plus HA hitting /api/ir/send concurrently will serialize and can 409. Mitigate with a short busy-wait/retry hint in the 409 body and keep hold_ms small.",
          "Open-loop AC state: the appliance can't read the real AC, so its 'last_commanded' state drifts if the AC is also controlled by its physical remote. Expose open_loop:true and a Resync button; if HA owns state via MQTT climate, make HA the single source of truth and always POST absolute (full) state.",
          "hold_ms while-held depends on reliable pointerup; browsers drop pointerup on some mobile gestures. Server clamp to <=1000ms and a client watchdog (auto-release after N ms) are required so the emitter never runs away.",
          "HA remote.send_command's real transport (Broadlink) documents num_repeats and delay_secs but NOT hold_secs; do not rely on hold_secs parity — our hold is a native extension driven by the browser, not by HA.",
          "Minimal hand-rolled JSON parser is a security/robustness surface (malformed bodies, huge raw_us arrays). Cap raw_us length (e.g. <=1024 ints) and total body size (e.g. <=16KB) to protect ~25MB RAM.",
          "SmartIR export produces a full mode x fan x temperature raw matrix which can be large; generate on demand and stream it, never persist it in the 4MB overlay.",
          "Serving a build-time-gzipped SPA assumes every client sends Accept-Encoding: gzip (all modern browsers do); keep a tiny uncompressed fallback route for curl/debugging."
        ],
        "sources": [
          "https://github.com/smartHomeHub/SmartIR/blob/master/docs/CLIMATE.md",
          "https://github.com/smartHomeHub/SmartIR",
          "https://raw.githubusercontent.com/smartHomeHub/SmartIR/master/codes/climate/1000.json",
          "https://www.home-assistant.io/integrations/remote/",
          "https://www.home-assistant.io/integrations/broadlink/",
          "https://www.home-assistant.io/integrations/rest_command/",
          "https://developers.home-assistant.io/docs/api/rest/"
        ]
      }
    ],
    "plan": "Verified against the findings by re-derivation. The µs lists below are exact. Here is the plan.\n\n---\n\n# irapi Offline IR DB + Encoders — Implementation Plan\n\n## A) FINAL ARCHITECTURE\n\n**DB source to bundle:** a COMBO, decided.\n- **Primary raw data:** a curated subset of **Flipper-IRDB (CC0)** — only the brands the user owns (their TVs/AVR, plus `ACs/Danby` and `ACs/Midea`). AC records are already raw 38 kHz mark/space lists → feed `i2s::blast` verbatim, zero decode.\n- **Authoritative AC path:** a **native Rust stateful Midea/Danby encoder** reimplemented from the *documented* IRremoteESP8266 `ir_Midea` spec (timings/bit-layout are facts, not copyrightable — do NOT copy LGPL source). This, not a captured code matrix, backs the REST climate entity.\n- **Stateless protocol encoders** (NEC/NECext, Samsung32, Sony SIRC, RC5/RC6, Kaseikyo) so Flipper `parsed` records and any hand-entered params also work.\n- **Explicitly excluded:** IRDB wholesale (free-units license clause), SmartIR/Broadlink as transport (carrier-less, extra decode). Re-derive any IRDB params yourself rather than shipping their CSVs.\n\n**On-device format:** one flat big-endian blob `irdb.bin` (matches MIPS BE target), memory-mapped/`include_bytes!`-style read-only in the factory image; a separate small mutable `custom.bin` on the 4MB overlay for learned/imported codes. Schema in §C.\n\n**Rust module layout** (single-threaded, std-only, zero crates):\n```\nsrc/\n  i2s.rs        // EXISTS: blast(carrier, duty, bitmul, select, &[(is_mark,u16)], finish)\n  ir/\n    mod.rs      // Cmd enum + dispatch: Raw | Param(proto,fields) | MideaState | MideaToggle\n    pdm.rs      // generic pulse-distance encoder (NEC/Samsung/Sony/Kaseikyo/Midea frame)\n    manch.rs    // Manchester encoder (RC5, RC6-0) w/ per-protocol convention + double-wide toggle\n    midea.rs    // stateful 48-bit state struct, checksum, A-frame + inverted-B-frame builder\n    proto.rs    // const profile table (carrier, timings) keyed by protocol_id\n  db/\n    mod.rs      // irdb.bin reader: index -> devices -> buttons -> record\n    custom.rs   // custom.bin reader/writer on overlay (append-only + tombstone)\n  api/\n    mod.rs      // HTTP router (single-thread, busy-flag guard)\n    json.rs     // ~150-line hand JSON reader (obj/str/num/bool/array-of-int, 1 level)\n    ha.rs       // /api/ha/*.yaml generators\n    spa.rs      // build-time gzipped SPA blob, served w/ Content-Encoding: gzip\n```\n\n**Runtime flow (selection → fire):**\n```\nHTTP request\n  → api::json parse (precedence: raw_us > pronto > code_id > protocol+params > device+function)\n  → db lookup resolves code_id/device+function to a Record\n  → ir::mod dispatch:\n       Raw        → (carrier,duty,us[])         ─┐\n       Param      → pdm/manch encoder → us[]     ├→ i2s::blast(carrier,duty,bitmul,select,us,finish)\n       MideaState → midea::encode → us[] (2 frames)┤     × num_repeats, gap=delay_secs\n       MideaToggle→ midea::encode_raw48 → us[]   ─┘\n  → busy-flag serialize; 409 if in flight\n```\n\n---\n\n## B) ENCODER SPECS (implement in this order; all µs, mark = carrier ON)\n\n**Shared primitive — `pdm`:**\n```rust\nstruct Pdm{carrier:u32, hdr:(u16,u16), bit_mark:u16, zero:u16, one:u16, foot:(u16,u16)}\nfn encode_pdm(p:&Pdm, bits:&[bool], out:&mut Vec<(bool,u16)>){\n  out.push((true,p.hdr.0)); out.push((false,p.hdr.1));\n  for &b in bits { out.push((true,p.bit_mark)); out.push((false, if b {p.one} else {p.zero})); }\n  out.push((true,p.foot.0)); out.push((false,p.foot.1));\n}\n```\n\n1. **NEC / NECext** — 38 kHz, duty 1/3. `Pdm{hdr:(9000,4500), bit_mark:560, zero:560, one:1690, foot:(560, ~40000)}`. **LSB-first**, 32 bits = `LSB(addr8) ++ LSB(~addr8) ++ LSB(cmd8) ++ LSB(~cmd8)`. NECext: replace `~addr8` with `addr_high8` (i.e. send 16-bit addr low then high, no inversion). Repeat frame when held: `9000,2250,560,+gap`, every 110 ms.\n\n2. **Samsung32** — 38 kHz. `Pdm{hdr:(4500,4500) EQUAL, bit_mark:560, zero:560, one:1690, foot:(560, ~2250)}`. LSB-first, 32 bits = `LSB(addr8) ++ LSB(addr8) [repeat, NOT inverted] ++ LSB(cmd8) ++ LSB(~cmd8)`. Prefer this over Samsung36.\n\n3. **Sony SIRC 12/15/20** — **40 kHz**, duty 1/3. Variant emitter (mark carries the bit, space const 600): header `mark 2400, space 600`; per bit `mark = one?1200:600, space 600`; **no footer bit**. **LSB-first**: `cmd7 ++ addr` (addr = 5/8/13 bits for 12/15/20). **Must repeat ≥3× at 45 ms spacing.**\n\n4. **RC5** — **36 kHz**, Manchester, half-bit T=889. 14 bits **MSB-first**: `S1=1, S2(=1, or inverted cmd bit6 for RC5X), Toggle, Addr5, Cmd6`. Convention: logical **1 = space(889)→mark(889)**, **0 = mark→space**. Emit half-bit levels, coalesce equal adjacent runs into one (level,889|1778). Idle ≥4 half-bits, repeat 114 ms.\n\n5. **RC6-0** — **36 kHz**, Manchester, T=444. Leader `mark 2666, space 889`; start bit=1; 3 mode bits=000; **toggle bit is DOUBLE-WIDTH (2T per half)**; then 8 addr + 8 cmd, MSB-first. Convention is **OPPOSITE RC5**: logical **1 = mark→space**, **0 = space→mark**. Watch double-wide toggle when coalescing.\n\n6. **Kaseikyo/Panasonic** — **37 kHz**, T=432. `Pdm{hdr:(3456,1728), bit_mark:432, zero:432, one:1296, foot:(432,gap)}`. **LSB-first**, 48 bits, byte view `[0x02,0x20, device, subdev, func, checksum]` where `checksum = device ^ subdev ^ func`.\n\n7. **MIDEA / DANBY (priority)** — 38 kHz, duty 1/3, **MSB-first**, pulse-distance. Per-frame `Pdm{hdr:(4480,4480), bit_mark:560, zero:560, one:1680, foot:(560,5100)}`. **A full transmission = TWO frames**: Frame A = 48-bit value; Frame B = the value fully bit-inverted over 48 bits. 48-bit value internal layout (byte5=MSB…byte0=LSB): `byte5=0xA1 prefix`, `byte4=power/sleep/mode(3)/fan(2)`, `byte3=temp(5)+°F flag`, `byte2=timer/beep`, `byte1=sensor`, `byte0=checksum`. **Checksum:** reverse-bits of bytes1..5, sum, `(256-sum)&0xFF`, reverse-bits of result — port exactly, validate against a Flipper capture before trusting. Ready-made constants (checksum already valid → blast verbatim): `POWER ON = 0xA1826FFFFF62`, `POWER OFF = 0xA1026FFFFFE2`. Danby DAC toggles (fire as 48-bit value w/ same inverted-repeat modulation): SwingV/Ion `0xA201FFFFFF7C`, Econo `0xA202FFFFFF7E`, Light `0xA208FFFFFF75`, Turbo `0xA209FFFFFF74`.\n\n---\n\n## C) STORAGE PLAN\n\n**Bundle in factory image (`irdb.bin`, read-only, target ≪4MB):**\n- Flipper AC subset (Danby + Midea + any other AC the user owns): ~30–50 files × 4–20 KB ≈ **300–600 KB**.\n- User's specific TVs/AVR (raw or parsed): a few KB each, ~tens of KB.\n- Encoders + Midea state machine: pure code, **~2–6 KB, ~0 data**.\n- Total realistically **<1 MB**, leaving overlay headroom.\n\n**`irdb.bin` schema (big-endian):**\n```\nMAGIC \"IRDB\"(4) | u8 version | u16 device_count\nDEVICE INDEX[device_count]: u16 name_off | u8 type_id | u8 button_count | u32 buttons_off\nSTRING POOL: len-prefixed UTF-8\nRECORD AREA:\n  BUTTON: u16 name_off | u8 rec_type | u32 rec_off\n  rec_type 0 RAW:    u16 carrier_hz | u8 duty_pct | u16 n | n×u16 durations_us (start MARK, alt mark/space)\n  rec_type 1 PARAM:  u8 protocol_id | u8 flags | proto payload\n        NEC/NECext:  u8 device | i16 subdevice(-1=derived) | u8 function\n        RC5/RC6:     u8 system | u8 command | u8 toggle\n        SIRC:        u8 bits(12/15/20) | u16 command | u16 device\n        Kaseikyo/Sam:u16 address | u16 command\n  rec_type 2 MIDEA_STATE:  6 state bytes (encoder recomputes checksum + inverted repeat)\n  rec_type 3 MIDEA_TOGGLE: u32 low-48-packed toggle code\n```\nCap raw `n ≤ 1024` (Flipper limit; verify no AC exceeds it — size the u16 accordingly).\n\n**Overlay (`custom.bin`, mutable):** same record layout, append-only with a tombstone byte for deletes; holds learned + imported codes as `custom/<device>/<function>`. Factory DB stays read-only so a bad import can never brick the base set.\n\n**Coverage estimate:** with just raw Flipper + the 6 encoders you cover essentially all common TVs/AVRs/STBs the user owns plus full stateful AC. If broad TV coverage is ever wanted, PARAM records are ~6 bytes each → thousands fit in tens of KB (but type the params yourself to avoid IRDB's license).\n\n---\n\n## D) WEB UI + REST API\n\n**Server:** single self-contained SPA, build-time **gzipped** (`Content-Encoding: gzip`, `<40 KB`), no runtime deflate, no external refs. Hand-emit JSON; hand-rolled JSON reader only for the 3 write endpoints. Single-thread serialization is a feature — one blast at a time, **busy-flag → 409**. Cap body ≤16 KB and `raw_us` ≤1024 ints.\n\n**Browse (GET):** `/api/types`, `/api/brands?type=`, `/api/devices?type=&brand=`, `/api/device/{id}` (returns functions + `layout` grid, \"-\" = break), `/api/ports`, `/api/status`.\n\n**Fire — one endpoint `POST /api/ir/send`**, body is ONE shape, precedence first-wins:\n1. `{raw_us:[…],carrier,duty,port}` 2. `{pronto:\"…\",port}` 3. `{code_id:\"tv/samsung/N5300/power\",port}` 4. `{protocol:\"nec\",device,subdevice,function,carrier,port}` 5. `{device:\"tv/samsung/N5300\",function:\"power\",port}`.\nRepeat controls on any shape: `num_repeats` (HA alias), `delay_secs` (HA alias for inter-frame gap), `hold_ms` **server-clamped [0,1000]** (overrides repeats for while-held). Accept HA alias `command`↔`function` so a stock HA `rest_command` maps 1:1, no custom component. Responses `200 {ok,emitted{…}}`, `400/404/409/503`. Plus `POST /api/ir/stop`.\n\n**Stateful AC (instances):** `GET/POST /api/instances`, `DELETE /api/instance/{id}`, `GET /api/instance/{id}/state`, `POST /api/instance/{id}/state` (**partial patch** → merge onto last-commanded state → re-encode FULL Midea frame → blast → persist; also accepts a complete state object for HA-owns-truth idempotent mode), `POST /api/instance/{id}/resync`. Expose `open_loop:true` (appliance can't read the AC). Do **not** ship a mode×fan×temp matrix — use the encoder; reserve captured presets only for functions the encoder can't reach (follow-me etc.).\n\n**Learn/Import:** `POST /api/learn/start`→`GET /api/learn/{id}`→`POST /api/learn/{id}/test`; `POST /api/codes/import` (pronto | raw | broadlink_b64 | lirc); `GET/DELETE /api/codes/custom`.\n\n**HA generators (copy-paste, no hand-editing):** `GET /api/ha/rest_command.yaml`, `/api/ha/device/{id}.yaml` (scripts per function), `/api/ha/instance/{id}.yaml` (mqtt climate + forwarding automation), `/api/ha/package/{id}.yaml`. Optional `GET /api/export/smartir/{id}.json` generated on demand (never persisted).\n\n**UI flow:** header selectors `type → brand → model`, global Port + Hold toggle; left device list, right CSS-grid function pad from `layout`; while-held buttons re-POST `hold_ms:250` chunks until `pointerup` (client watchdog auto-release). Separate AC panel = temp stepper + mode/fan/swing/toggles, every change transmits full state.\n\n---\n\n## E) PHASED BUILD PLAN (each phase shippable)\n\n**P1 — Fire the Danby TODAY (no DB, no UI).** Use the existing `irapi i2sraw` CLI with the exact list in §F. Confirm the user's AC reacts. This validates carrier/select/modulation end-to-end and de-risks everything else. *(Deliverable: working power toggle.)*\n\n**P2 — Native Midea/Danby stateful encoder (`midea.rs`).** State struct + checksum + A/inverted-B frame builder + Danby DAC toggles. CLI `irapi midea --power on --mode cool --temp 22 --fan auto`. Validate the checksum against a captured Flipper Danby frame. *(Deliverable: any temp/mode/fan on the fly.)*\n\n**P3 — DB blob + reader + raw dispatch.** Build `tools/build_irdb` (host-side: parse curated Flipper `.ir` → `irdb.bin`). Add `db/` reader and `ir::mod` Raw dispatch. Curate Danby/Midea + the user's TVs/AVR. CLI `irapi fire <code_id>`. *(Deliverable: browseable raw code firing.)*\n\n**P4 — Stateless encoders (`pdm.rs`, `manch.rs`, `proto.rs`).** NEC/NECext + Samsung32 first (cover most TVs), then Sony, Kaseikyo, RC5/RC6. Enables PARAM records + Flipper `parsed`. *(Deliverable: parametric + parsed codes.)*\n\n**P5 — HTTP server + REST API + JSON reader.** `/api/ir/send` (all shapes), browse endpoints, busy-flag. *(Deliverable: HA `rest_command` works.)*\n\n**P6 — Stateful AC instances + persistence on overlay.** `/api/instance/*` wired to `midea.rs`; `custom.bin`. *(Deliverable: HA MQTT climate entity.)*\n\n**P7 — SPA (gzipped) + Learn/Import + HA yaml generators.** *(Deliverable: full self-contained appliance.)*\n\n---\n\n## F) IMMEDIATE DELIVERABLE — fire now\n\n**Re-derived and verified** (200 values each, byte-expanded from the Midea constants; frame = header 4480,4480 → 48× {560, 1680=one/560=zero} MSB-first → footer 560,5100; then the fully-inverted second frame). **Source:** IRremoteESP8266 `ir_Midea` ready-made constants (POWER OFF `0xA1026FFFFFE2`, POWER ON `0xA1826FFFFF62`), inverted-repeat framing 4480/560/1680 per the same spec and the balbachan Midea-BGH capture. Both constants carry a valid internal checksum, so they blast verbatim.\n\n**Danby remotes are usually discrete on/off — try OFF first, then ON if no reaction.**\n\n**CANDIDATE 1 — POWER OFF (value 0xA1026FFFFFE2):**\n```\nirapi i2sraw --carrier 38000 --select 7 --us 4480,4480,560,1680,560,560,560,1680,560,560,560,560,560,560,560,560,560,1680,560,560,560,560,560,560,560,560,560,560,560,560,560,1680,560,560,560,560,560,1680,560,1680,560,560,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,560,560,560,560,560,560,1680,560,560,560,5100,4480,4480,560,560,560,1680,560,560,560,1680,560,1680,560,1680,560,1680,560,560,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,560,560,1680,560,1680,560,560,560,560,560,1680,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,1680,560,1680,560,1680,560,560,560,1680,560,5100\n```\n\n**CANDIDATE 2 — POWER ON (value 0xA1826FFFFF62):**\n```\nirapi i2sraw --carrier 38000 --select 7 --us 4480,4480,560,1680,560,560,560,1680,560,560,560,560,560,560,560,560,560,1680,560,1680,560,560,560,560,560,560,560,560,560,560,560,1680,560,560,560,560,560,1680,560,1680,560,560,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,1680,560,560,560,1680,560,1680,560,560,560,560,560,560,560,1680,560,560,560,5100,4480,4480,560,560,560,1680,560,560,560,1680,560,1680,560,1680,560,1680,560,560,560,560,560,1680,560,1680,560,1680,560,1680,560,1680,560,560,560,1680,560,1680,560,560,560,560,560,1680,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,560,1680,560,560,560,560,560,1680,560,1680,560,1680,560,560,560,1680,560,5100\n```\n\n**CANDIDATE 3 (if neither reacts):** capture the user's actual Danby remote via the P1 learn RX and blast the captured buffer — Danby field timings can run ~600/450/1550 vs the 560/560/1680 nominal, and a specific model may use a per-unit frame. Keep the P2 native encoder as the authoritative path for their exact unit.\n\nBoth candidate lists were regenerated programmatically from the bit expansions (OFF bits `1010 0001 0000 0010 0110 1111 1111 1111 1111 1111 1110 0010`, inverted `0x5EFD9000001D`; ON inverted `0x5E7D9000009D`) and match the research findings exactly. OFF frame total airtime ≈ 190.5 ms."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Research"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "research:db-sources",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "aee33e6d86dbb9b06",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783044858386,
      "queuedAt": 1783044858345,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Five sources evaluated for an offline, on-device IR databas…",
      "promptPreview": "PROJECT: A Logitech Harmony Hub reflashed into a STANDALONE, no-cloud IR blaster appliance. Board =\nAtheros AR9331 (MIPS, big-endian, softfloat), Linux 2.6.31, ~32MB RAM (~25MB free), ~4MB writable\noverlay. Our firmware = a single-threaded static Rust binary \"irapi\" (no futex; std only, zero crates).\nWe have JUST proven DIRECT IR TRANSMIT working: our Rust driver writes a modulated carrier bitstre…",
      "lastProgressAt": 1783045314681,
      "tokens": 42432,
      "toolCalls": 25,
      "durationMs": 456294,
      "resultPreview": "{\"summary\":\"Five sources evaluated for an offline, on-device IR database on the ~4MB AR9331 Rust appliance. Flipper-IRDB (CC0 public domain) is the clear primary: its AC files are already stored as raw microsecond mark/space lists at 38kHz — a 1:1 feed into i2s::blast with zero decoding — and it ships 130 AC brand folders including both Danby and Midea. IRDB (probonopd) is tiny (~1.5MB) and compac…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "research:protocol-encoders",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a88be95db99654b52",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783044858400,
      "queuedAt": 1783044858345,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Complete Rust-implementable IR protocol encoder spec (param…",
      "promptPreview": "PROJECT: A Logitech Harmony Hub reflashed into a STANDALONE, no-cloud IR blaster appliance. Board =\nAtheros AR9331 (MIPS, big-endian, softfloat), Linux 2.6.31, ~32MB RAM (~25MB free), ~4MB writable\noverlay. Our firmware = a single-threaded static Rust binary \"irapi\" (no futex; std only, zero crates).\nWe have JUST proven DIRECT IR TRANSMIT working: our Rust driver writes a modulated carrier bitstre…",
      "lastProgressAt": 1783045112505,
      "tokens": 25692,
      "toolCalls": 12,
      "durationMs": 254104,
      "resultPreview": "{\"summary\":\"Complete Rust-implementable IR protocol encoder spec (params -> mark/space µs list -> i2s::blast). Covers NEC + NEC-extended, Samsung 32/36, RC5, RC6, Sony SIRC 12/15/20, Panasonic/Kaseikyo, and the Midea/Danby AC protocol with a concrete, ready-to-blast POWER-OFF mark/space µs list. Every timing is tagged CONFIRMED (sourced) or APPROX. All space-encoded protocols share one primitive: …"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "research:on-device-arch",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a489931bfe848dbe2",
      "model": "claude-opus-4-8",
      "state": "error",
      "startedAt": 1783044858403,
      "queuedAt": 1783044858345,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Store protocol PARAMETERS not raw timing (the IRDB model: f…",
      "promptPreview": "PROJECT: A Logitech Harmony Hub reflashed into a STANDALONE, no-cloud IR blaster appliance. Board =\nAtheros AR9331 (MIPS, big-endian, softfloat), Linux 2.6.31, ~32MB RAM (~25MB free), ~4MB writable\noverlay. Our firmware = a single-threaded static Rust binary \"irapi\" (no futex; std only, zero crates).\nWe have JUST proven DIRECT IR TRANSMIT working: our Rust driver writes a modulated carrier bitstre…",
      "lastProgressAt": 1783045709674,
      "error": "agent({schema}): StructuredOutput retry cap (5) exceeded — 5 failed calls with no valid output",
      "tokens": 109967,
      "toolCalls": 27,
      "durationMs": 851271
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "research:ux-api",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a3e0f0e8faa0e91f2",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783044858404,
      "queuedAt": 1783044858345,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Design for the on-device Web UI + REST API that browses/fir…",
      "promptPreview": "PROJECT: A Logitech Harmony Hub reflashed into a STANDALONE, no-cloud IR blaster appliance. Board =\nAtheros AR9331 (MIPS, big-endian, softfloat), Linux 2.6.31, ~32MB RAM (~25MB free), ~4MB writable\noverlay. Our firmware = a single-threaded static Rust binary \"irapi\" (no futex; std only, zero crates).\nWe have JUST proven DIRECT IR TRANSMIT working: our Rust driver writes a modulated carrier bitstre…",
      "lastProgressAt": 1783045184677,
      "tokens": 20949,
      "toolCalls": 9,
      "durationMs": 326273,
      "resultPreview": "{\"summary\":\"Design for the on-device Web UI + REST API that browses/fires an offline IR code DB and manages custom codes on the AR9331 \\\"irapi\\\" appliance. Two clean surfaces map onto the single existing primitive i2s::blast(carrier,duty,bitmul,select,&[(is_mark,us)]): (a) a stateless TV/AVR path that mirrors Home Assistant's remote.send_command (device+command+num_repeats+delay_secs) so a one-lin…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "synthesize:ir-db-plan",
      "phaseIndex": 2,
      "phaseTitle": "Synthesize",
      "agentId": "af62c3e9656d78662",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783045709714,
      "queuedAt": 1783045709712,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "python3 - <<'PY' def bits48(v): return [(v>>i)&1 for i in r…",
      "promptPreview": "PROJECT: A Logitech Harmony Hub reflashed into a STANDALONE, no-cloud IR blaster appliance. Board =\nAtheros AR9331 (MIPS, big-endian, softfloat), Linux 2.6.31, ~32MB RAM (~25MB free), ~4MB writable\noverlay. Our firmware = a single-threaded static Rust binary \"irapi\" (no futex; std only, zero crates).\nWe have JUST proven DIRECT IR TRANSMIT working: our Rust driver writes a modulated carrier bitstre…",
      "lastProgressAt": 1783045836606,
      "tokens": 36209,
      "toolCalls": 1,
      "durationMs": 126892,
      "resultPreview": "Verified against the findings by re-derivation. The µs lists below are exact. Here is the plan.\n\n---\n\n# irapi Offline IR DB + Encoders — Implementation Plan\n\n## A) FINAL ARCHITECTURE\n\n**DB source to bundle:** a COMBO, decided.\n- **Primary raw data:** a curated subset of **Flipper-IRDB (CC0)** — only the brands the user owns (their TVs/AVR, plus `ACs/Danby` and `ACs/Midea`). AC records are already …"
    }
  ],
  "totalTokens": 235249,
  "totalToolCalls": 74
}