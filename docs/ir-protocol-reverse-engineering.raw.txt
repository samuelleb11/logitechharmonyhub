{
  "summary": "Statically reverse the remaining Harmony IR/LTCP protocol details (response body, learn/ir_cap, blob+ports, ir_test) from the extracted rootfs + decompiled Lua + hal strings",
  "agentCount": 5,
  "logs": [],
  "result": {
    "research": [
      {
        "key": "response",
        "summary": "hal's LTCP reply to a `command` (opcode 8) `/ir/ir_send` is a PRIMARY response packet followed by one or more SECONDARY packets carrying the JSON body. I confirmed from the C hal disassembly that the reply reuses the request's LTCP reqId with the 0x80 (isResp) bit set: reply[2] = request[2] | 0x80 (libhal_send_success_response @0x4189d0/0x4189e4). Numeric params are TLV: a length byte (masked 0x3F = width; 0 => NUL-terminated string) then `width` big-endian bytes. For a normal (no-checksum) small response the primary is 6 bytes `FF 08 (mid|0x80) 01 01 02` where the last byte (2) is the packet-count = numPackets, so `params[1]-1 = 1` secondary packet with the JSON follows. The observed 7-byte read is that 6-byte primary PLUS the leading sequence byte of the following secondary packet, coalesced into one TCP read — hal writes the primary, the secondary seq byte, the secondary length byte, and the payload as SEPARATE 1-byte/short socket writes (body_sender @0x41ba90, a2=1), so a single read() truncates mid-frame; the client MUST loop reads until it has consumed all `numPackets-1` secondaries. The JSON body is `{\"id\":<echoes request id>,\"code\":<int 200/500/503>,...}` (json_pack \"{s:O, s:i}\" keys \"id\",\"code\" @0x41f218), optionally followed by a binary tail.",
        "confirmed": [
          "reqId echo with isResp bit: the reply's 3rd byte = request's reqId | 0x80. C hal: libhal_send_success_response reads ctx[2] (`lbu $a3,2($a1)` @0x4189d0) and ORs 0x80 (`ori $a1,$a3,0x80` @0x4189e4), writing it as primary byte[2] (`sb $a1,0xd($t1)` @0x418a14). parse_primary stores the incoming reqId masked with 0x7f (`andi $s6,$t8,0x7f; sb $s6,2($s0)` @0x4183a4). Matches ltcp_decompiled_reference.lua:334 `self.isResp = band(reqId,128)==128` and :333 `self.reqId = reqId`.",
          "Primary header layout = [serviceId=0xFF, command, reqId|0x80, numParamsByte]. send_success_response prologue writes 0xFF (`sb $t2(-1),0xb($t1)` @0x418a00), command=ctx[0] (@0x418a08), reqId|0x80 (@0x418a14); numParamsByte written by the per-command switch. Matches ltcp .lua processPrimaryPacket:274-292 (serviceId/command/reqId/numParam=band(byte4,63)).",
          "numParamsByte: bit0x80 = checksum-present flag, low bits = param count. parse_primary: `srl $ra,$s2,7; sw $ra,8($s0)` (@0x4183c8 request[8]=bit7) and `andi $s3,$s2,0x7f; sw $s3,4($s0)` (@0x4183cc request[4]=count). Reply numParamsByte for command=8 success = 1, or 0x81 if checksum negotiated (`movz $t9,$a3(1),$ra(ctx[0x50]); ... =0x81` @0x418c54-5c). Decoder masks it: ltcp.lua:292 `band(val:byte(4),63)`.",
          "Numeric params are TLV [lenByte, big-endian bytes]; lenByte&0x3F = width (1/2/4), lenByte&0x3F==0 => NUL-terminated string. encode_param_word @0x41894c writes exactly [0x02, msb, lsb] (width=2). Decoder ltcp.lua:304-324: `len=band(byte,63)`; if 0 -> read to NUL (string) else read `len` bytes -> getNumber big-endian.",
          "The single primary param is the packet COUNT = numPackets (total packets incl. primary). decode_command_handler computes numPackets = 1 + ceil(bodyLen/maxSec) (`divu`/`mflo`/`addiu +1` @0x41d6ac-0x41d6cc) and stores it at ctx[4] (`sw $v0,0x94($sp)` @0x41d6d4). send_success command=8 case reads ctx[4] (`lw $a1,4($s0)` @0x418c58) and encodes it. Matches ltcp.lua:336 `self.packets = params[1]-1` (= number of secondary packets).",
          "Count is width-encoded by magnitude: <256 => 1 byte [0x01,count]; <65536 => word [0x02,hi,lo]; else dword [0x04,...]. send_success: `sltu $t8,$a1,0x10000` (@0x418c70), `sltiu $t6,$a1,0x100` (@0x418e64) -> byte path @0x418e6c writes lenByte=1 then count; word path calls encode_param_word @0x418ef8. For a small ir_send JSON, numPackets=2 -> byte-encoded -> primary is 6 bytes FF 08 (mid|80) 01 01 02.",
          "Checksum is per-request-negotiated and OFF for our client. ctx[0x50]=request[8]=(reqNumParamsByte>>7). Our Rust sends numParams=0x01 (bit7=0) so ctx[0x50]=0. send_primary_fn @0x418830 appends a trailing XOR-checksum byte ONLY if ctx[0x50]!=0 (`lw $v0,0x50($a1); beqz $v0,skip; sb $a0(cksum),0xb($v1)` @0x418840-0x41886c). So our reply has NO checksum byte. CHECKSUM_SUPPORT=false in ltcp_decompiled_reference.lua:13.",
          "The whole response is sent as many small separate writes, not one buffer. Primary: one libhal_write_socket of `len` bytes (@0x41888c). Each secondary: a 1-byte seq write (a2=1 @0x41bb88), a 1-byte length write (a2=1 @0x41bbd8/0x41bc08), then a payload write (a2=payloadLen @0x41bc4c) — body_sender @0x41ba90. Both ends set TCP_NODELAY (hal_main_decompiled_reference.lua:30; ir.rs:117), so segments arrive coalesced arbitrarily; a single read() can return 6 (primary), 7 (primary+seq), or the whole thing.",
          "Secondary packet framing = [seqByte, lenByte, payload]. lenByte&0x40 => extended: read 1 more byte, len=((lenByte&0x3F)<<8)|next (14-bit). Else len=lenByte&0x3F. body_sender checks `andi $t7,$s3,0x40` (@0x41bb98) and builds a 14-bit length (`andi $a3,$s1,0x3fff` @0x41bbac). Matches ltcp.lua processSecondaryPacket:220-247. The 0x80 base bit on the header bytes is ignored by the decoder (masks 0x3F).",
          "Response body is JSON via jansson: json_loadb (parse request @0x41f0ec) -> json_unpack (@0x41f158) -> libhal_hbus_process (run cmd @0x41f1e8) -> json_pack(\"{s:O, s:i}\", \"id\", <req id obj>, \"code\", <int>) (@0x41f218; fmt @0x449860, keys 'id'@0x443134,'code'@0x4449d4) -> json_object_set_new_nocheck -> json_dumps (@0x41f2dc) -> strlen/realloc/memcpy into the out buffer. So the C hal reply JSON = {\"id\":<echoes request id>,\"code\":<int>,...}. irsender_decompiled_reference.lua:60-64 reads response.code (200 OK, 500 'RF timed out', 503 'RF link lost').",
          "reqId==0xFF (255) is emitted by libhal_send_error_response @0x418f10 which hardcodes primary byte[2]=0xFF (`sb $t4(-1),0xd($t3)` @0x418f58) for transport-level errors (checksum mismatch, invalid service id, bad/oversized param, etc. — the ltcpErrMsg table ltcp.lua:27-42). ltcp.lua:282-286 treats reqId==255 as 'ltcp error message' (returns false).",
          "reqId==0xFE (254) is an EOF/no-more-data marker for streamed READ responses. send_success read-case tests it: `xori $a1,$a3,0xfe; movz $t4,$t9(1),$a1` (@0x418b9c) — when reqId==0xFE it emits only 1 param (handle) and skips the length param. ltcp.lua:287-291 treats reqId==254 as 'eof'. Not emitted for /ir/ir_send (opcode 8 command)."
        ],
        "uncertain": [
          "EXACT identity of the observed 7th byte: from the write ORDER in body_sender (seq write @0x41bb84 precedes length write @0x41bbd4) it is the secondary SEQUENCE byte, value (counter+1)|0x80 (`or $t3,$t4,$fp(-0x80)` @0x41bb6c) i.e. 0x81,0x82,... NOT the JSON. TEST: on the device, loop-read the socket after send until no more data and dump the full hex — you should see `FF 08 (mid|80) 01 01 02` then `<seqByte> <lenByte> {\"id\":...,\"code\":200...}`.",
          "Whether ir_send's reply body ever contains errorCode/errorString or a nested 'data' object beyond {id,code}: the base pack is {\"id\",\"code\"} and json_object_set_new_nocheck adds one more key I did not fully resolve. The connectserver layer (ltcpserverconnector, port 16717) is what adds data.errorCode/errorString — our client hits 16716 (C hal) directly. TEST: capture the full JSON body live and inspect keys; parse defensively for both `code` and `errorCode`/`errorString`.",
          "maxSec chunk size (62 vs 16382) depends on a hal socket-handle flag s0[8] (`lw $t5,8($s0); beqz` @0x41d68c -> s3=0x3e or 0x3ffe). This only affects how many secondary packets a LARGE body splits into (i.e. the count value). For small ir_send acks count=2 regardless. TEST: send a multi-KB blob and read the count byte(s).",
          "Whether count could ever be word-encoded (making the primary a genuine 7 bytes without checksum): only if numPackets>=256, i.e. a body needing >=256 secondary packets (~16KB-4MB). Implausible for ir_send but possible for large reads. The parser handles it via the param lenByte width."
        ],
        "byte_spec": "WIRE FORMAT of hal's LTCP reply (big-endian numbers, no checksum in our config).\n\n=== PRIMARY response packet (one socket write) ===\nOffset Byte\n 0     0xFF                      serviceId (SERVICE_ID); reject if != 0xFF\n 1     cmd                       echoes request opcode (0x08 for `command`)\n 2     reqId                     = (request primary byte[2]) | 0x80\n                                   isResp = (reqId & 0x80)!=0\n                                   reqId==0xFF => LTCP error packet\n                                   reqId==0xFE => EOF marker (streamed reads only)\n 3     numParamsByte             bit0x80 = checksumPresent; low6 (&0x3F) = paramCount (<=3)\n 4..   params[paramCount]        each = TLV:\n                                    lenByte; w = lenByte & 0x3F\n                                    if w==0: NUL-terminated string (read bytes until 0x00)\n                                    else:    w big-endian bytes => unsigned number\n [last]  checksum                present ONLY if checksumPresent; 1 XOR byte (ignore)\n\nFor a `command`(8) SUCCESS the single param is the packet COUNT (=numPackets, total incl. primary).\nConcrete no-checksum small reply (numPackets=2):\n   FF 08 (mid|0x80) 01 01 02        <-- 6 bytes\n   secondaryCount = params[0] - 1 = 1\n\n=== SECONDARY packet(s) — repeat `secondaryCount` times ===\n 0     seqByte                   sequence id; may have 0x80 set; ignore (mask 0x3F if needed)\n 1     lenByte                   if (lenByte & 0x40): EXTENDED\n                                     2  extraByte; len = ((lenByte & 0x3F)<<8) | extraByte\n                                   else: len = lenByte & 0x3F\n ...   payload[len]              body fragment\n [checksum]                      present only if checksumPresent (ignore)\nConcatenate all payloads => BODY.\n\n=== BODY ===\nBODY = <JSON text> [ <binary tail> ]\nJSON = {\"id\":<echoes request JSON id>, \"code\":<int>, ...}   (code 200=OK, 500/503=RF error)\nSplit point = end of the top-level JSON object; bytes after it = binaryTail\n(hbus_decompiled_reference.lua:120 binaryData = data[jsonLen+1:]).\n\n=== WHAT THE 7-BYTE READ IS ===\nThe primary is 6 bytes (FF 08 mid|80 01 01 02). hal then writes the secondary as separate tiny\nwrites (seq, len, payload). One read() coalesced the 6-byte primary + the secondary's 1st (seq)\nbyte => 7 bytes. It is NOT the whole reply: keep reading to get lenByte + the JSON.\n(If you ever see byte[3]==0x81, that instead is a checksum-negotiated 7-byte primary\n FF 08 mid|80 81 01 02 <xor>; our client sends numParams=0x01 so this won't happen.)\n\n=== RUST PARSER (loop reads; do NOT rely on one read) ===\nstruct Reply { req_id:u8, is_resp:bool, is_error:bool, is_eof:bool, msg_type:u8,\n               count:u64, json:Vec<u8>, binary:Vec<u8> }\n\nfn read_reply<R:Read>(s:&mut R) -> Result<Reply,IrError> {\n    let mut b1=|s:&mut R|->io::Result<u8>{let mut b=[0u8;1]; s.read_exact(&mut b)?; Ok(b[0])};\n    // --- primary header ---\n    let mut h=[0u8;4]; s.read_exact(&mut h)?;\n    if h[0]!=0xFF { return Err(IrError::Protocol(format!(\"bad serviceId {:#x}\",h[0]))); }\n    let msg_type=h[1]; let req_id=h[2]; let np_byte=h[3];\n    let is_resp=req_id&0x80!=0; let is_error=req_id==0xFF; let is_eof=req_id==0xFE;\n    let has_cksum=np_byte&0x80!=0; let nparams=(np_byte&0x3F) as usize;\n    // --- primary params ---\n    let mut nums=Vec::new();\n    for _ in 0..nparams {\n        let lb=b1(s)?; let w=(lb&0x3F) as usize;\n        if w==0 { loop { if b1(s)?==0 {break;} } }        // NUL-terminated string\n        else { let mut v=0u64; for _ in 0..w { v=(v<<8)|b1(s)? as u64; } nums.push(v); }\n    }\n    if has_cksum { let _=b1(s)?; }\n    if is_error { return Err(IrError::Backend(\"ltcp error packet (reqId=0xFF)\".into())); }\n    // count = first numeric param; secondaries = count-1\n    let count=*nums.get(0).unwrap_or(&1);\n    let sec = count.saturating_sub(1);\n    // --- secondary packets ---\n    let mut body=Vec::new();\n    for _ in 0..sec {\n        let _seq=b1(s)?; let lb=b1(s)?;\n        let len = if lb&0x40!=0 { (((lb&0x3F) as usize)<<8)|(b1(s)? as usize) }\n                  else { (lb&0x3F) as usize };\n        let mut buf=vec![0u8;len]; s.read_exact(&mut buf)?; body.extend_from_slice(&buf);\n        if has_cksum { let _=b1(s)?; }\n    }\n    // --- split JSON | binary ---\n    let jend = json_end(&body);                 // index just past top-level {...}\n    let (json,binary)=body.split_at(jend);\n    Ok(Reply{req_id,is_resp,is_error,is_eof,msg_type,count,json:json.to_vec(),binary:binary.to_vec()})\n}\n// then: code = json[\"code\"] (200=>ok). errorCode/errorString if present (or code!=200) => error.\n// correlate: (req_id & 0x7F) == the mid you put in your request primary byte[2].\n\nNotes: wrap the TcpStream in a BufReader and use read_exact so partial/coalesced segments are handled;\nthe current ir.rs send_raw (single 512-byte read @ir.rs:155-166) is the bug — it stops after the\n7-byte coalesced chunk and never reads the JSON body.",
        "sources": [
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/ltcp_decompiled_reference.lua — processPrimaryPacket (l.269-338: serviceId/command/reqId checks, reqId 255/254 markers l.282-291, numParam=band(byte4,63), TLV param decode l.299-324, self.isResp/self.reqId/self.packets=params[1]-1 l.332-336), processSecondaryPacket (l.212-267: 2-byte hdr, 0x40 extended, len&0x3F), recvResponse (l.359-384), getNumber (l.406-412), CHECKSUM_SUPPORT=false (l.13)",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/ltcpserverconnector_decompiled_reference.lua — handleCommandCallback (l.46-102) builds {id,code=200,data={errorCode,errorString}} + optional binary (the 16717/connectserver layer, reference for response shape); json.decode+binary split (l.129-130)",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/hbus_decompiled_reference.lua — command() (l.69-123): json.decode of hal reply then binaryData=data[jsonLen+1:] (l.116-120); handleCallback type==8 isResp (l.32-38)",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/irsender_decompiled_reference.lua — startIrCommand (l.40-68): response.code 200/500/503 handling",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/hal_main_decompiled_reference.lua — startTcpConnection (0x01 hello l.39, TCP_NODELAY l.30), recvLtcp loop (l.44-66)",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/src/ir.rs — LtcpBackend::send_raw single-read bug (l.149-174); build_command_frame primary framing (l.198-221)",
          "/tmp/harmony_rootfs/usr/bin/hal (ELF32 MSB MIPS, stripped; dynsyms present). Disassembled via capstone (scratchpad disasm*.py). Key functions: libhal_send_success_response @0x418990 (primary header + per-command param switch; jump table @0x448880, cmd8 case @0x418c4c, byte-count path @0x418e64), libhal_send_error_response @0x418f10 (reqId=0xFF), libhal_encode_param_word @0x41894c (word param [0x02,msb,lsb]), libhal_encode_init @0x4188e4 (ctx[0]=req[0],ctx[2]=req[2],ctx[0x50]=req[8]), send_primary_fn @0x418830 (one write; optional checksum), body_sender @0x41ba90 (per-secondary 1-byte writes, 0x40 extended len), libhal_parse_primary_packet @0x4181b0 (req[2]=in&0x7f, req[8]=in>>7, req[4]=in&0x7f), libhal_decode_command_handler @0x41d5c0 (numPackets=1+ceil(bodyLen/maxSec)), libhal_hbus_command @0x41ef84 (json_loadb/json_unpack/json_pack \"{s:O,s:i}\" keys id/code/json_dumps)"
        ]
      },
      {
        "key": "learn",
        "summary": "The IR-learn path in stock `hal` is exposed as the LTCP resource `/ir/ir_cap` (method name `ir.cap`), routed through a fixed dispatch table at 0x452840. Unlike `/ir/ir_send`, ir_cap is NOT a simple JSON opcode-8 command: its table entry only populates the OPEN(1), DEVCTL(6), and command/notify(w11=0x2c) handler slots — there is no plain READ slot — and the actual capture-and-return is done by the DEVCTL handler `libhal_ir_cap_raw_read` (0x4247e4), which ACKs, blocks in `ir_read()` reading raw IR edges from the receiver device, converts the edges to an array of 16-bit microsecond durations, and streams that array back over the socket via `libhal_write_socket`. OPEN (`libhal_ir_cap_open`, 0x424728) just `calloc`s a 4100-byte (0x1004) capture buffer into the handle. The captured blob is a header + alternating mark/space durations (µs, big-endian uint16); to replay it verbatim through `/ir/ir_send` you rebuild the exact compiled-IR blob that on-device Lua `generateFromRawIr`+`appendDurationToByteArray` produce (fully decompiled — format nailed below). The remaining unknown is purely the client-side wire choreography (does the official client reach this via a raw OPEN+DEVCTL sequence, or via an opcode-8 \"ir.cap\" hbus command that the generic layer expands into open+devctl and streams back as the response), plus the capture-window/port parameter keys and the OPEN->handle plumbing — all three need one live-device capture to pin down.",
        "confirmed": [
          "Dispatch table at va 0x452840 maps path '/ir/ir_cap' (0x4433b0) -> method 'ir.cap' (0x4433bc); entries are 0x38 bytes (14 words): [w0 path, w1 method, ... handler function pointers ...]. Verified by dumping 0x452808..0x452ae0 (all IR/sys/rf entries decode cleanly).",
          "ir_cap entry populates exactly 3 handler slots: +0x0c=0x424728 (OPEN), +0x20=0x4247e4 (DEVCTL), +0x2c=0x4246ac (the opcode-8/hbus-command slot, same offset as ir_send's JSON handler). No READ(+0x18) or CLOSE(+0x24) slot is set (those words are 0 at 0x452854/0x452864).",
          "libhal_ir_cap_open @0x424728 (OPEN handler): logs 'libhal_ir_cap_open: ir_cap - open file command invoked' (fmt at 0x44a1b0, name at 0x44a298), then calloc(0x1004=4100, 1) and stores the buffer pointer at handle+0x0c; returns 0, or logs 'Error ir_cap open callback' (0x44a1dc) and returns -1 on calloc failure. Confirmed via calls resolved through MIPS GOT: -0x78ac($gp)=calloc, -0x7c54($gp)=libhal_log.",
          "libhal_ir_cap_raw_read @0x4247e4 (DEVCTL handler, slot +0x20): receives (socket_or_ctx s2 = a0, request s0 = a1); calls libhal_send_success_response(s2) FIRST (early ACK) [-0x7a54($gp)]; then ir_read(*(a1->0x5c)->0x0c, *(s2)) [-0x7f44($gp)]; memsets a 0x40 stack buffer; builds 64-byte LTCP-style packets (buf[0]=0x90=0x80|0x10, first 6 payload bytes copied from the capture buffer header, buf[0x3f]=8) and streams them out with libhal_write_socket(s2, buf, 0x40) [-0x7c98($gp)] in a loop; logs 'libhal_ir_cap_raw_read: read %d ir packets' (fmt 0x44a200, name 0x44a280).",
          "The ir_cap opcode-8/command-slot handler @0x4246ac: calloc(0x1004,1); ir_read(buf, *(handle+0x28)); stores the ir_read return (entry count) at handle+0x20 and 2*count at handle+0x24; returns 0. i.e. capture size in BYTES = 2*count (each captured interval is one uint16). It does NOT json_unpack — the capture parameter at handle+0x28 is pre-populated by the generic hbus/LTCP layer.",
          "ir_read @0x423fa4: pthread_create's ir_read_thread (thread fn base+0x3994), waits, then post-processes the thread's global result struct (at *(gp global)+0x4de4 / +0x4e00) into the caller's uint16 output array: durations are computed as (sample_count * device_clock)/1000 using the magic-number division 0x10624dd3 + (mfhi>>6) (== divide by 1000 -> microseconds). out[0],out[1]=derived header durations, out[2]=*(ctx+8) symbol count, then alternating mark/space durations, each a big-endian uint16 (sh). Flags 0x802/0x8000 are passed to the per-symbol formatter (base+0x3108).",
          "ir_read_thread @~0x423a00 loop caps accumulated signal time against s7 = 0x30d40 = 200000 (200000 microseconds = 200 ms capture window / limit) and uses libhal_is_read_sock_available (-0x7ef8($gp)) to detect end-of-signal / socket abort; it reads edges from a device via a vtable read at *(dev+0x38) and exits with pthread_exit.",
          "ir_send comparison confirms slot semantics: /ir/ir_send entry slots are WRITE(+0x14)=0x424550 (realloc handle->0x0c + memcpy the trailing IR blob in), DEVCTL(+0x28-region byte-dispatcher)=0x4245ec (switch on a control byte: 0x81->ir_send, 0x82->ir_send_cancel, 0x87->ir_send_repeat), and the JSON handler=0x424430 which json_unpack's '{s:b, s?i, s?i}' (0x44a140) with keys enable/keyLatency(0x44a150)/ports(0x44a15c) then calls ir_send/ir_send_repeat/ir_send_cancel. This 0x424430 handler is what the existing working Rust ir_send (opcode 8 + JSON) actually drives.",
          "Replay format (decompiled on-device Lua, exact): generateFromRawIr (irmanager.lua) builds the compiled-IR blob byte layout: [0]=0xFF (raw-timing marker; keycode mode uses 0x01), [1..4]=period_ns uint32 BE = floor(1e9/carrierHz), [5]=0x32(=50), [6..7]=0x0000 preSilence, [8]=0x00, [9]=0x00, [10..11]=0x0000, [12..13]=repeatLocation-1 (uint16 BE, =16 for raw), [14..15]=0x0000, [16..17]=repeatLength uint16 BE = (numDurationBytes)/2 - 1, [18..]=duration pairs. The raw string it parses is 'F<hexHz> P<hexUs> S<hexUs> ...' (F=carrier once, P=mark/pulse, S=space).",
          "Duration encoding (utility.lua appendDurationToByteArray/convertToBytes, exact): each interval µs value -> uint16 BE: hi=(val>>8)&0xff, lo=val&0xff; if it is a PULSE/mark (intervalType==1) set hi|=0x80. Values >32767 are emitted as repeated chunks (0x7FFF for space, 0xFFFF for mark) subtracting 32767 each, remainder clamped to a 30µs minimum. So bit7 of the high byte is the mark/space flag — the same convention the capture presumably returns.",
          "hbus.lua (on-device client) treats ir.cap as multi-phase: command() sets self.prevCmd='ir.cap' inside the hal_transaction, and a follow-up command reuses the same client/response via ltcp.formCommandRequest with ltcpPktSent=true and ltcp.resetResponse — i.e. the captured payload is expected to arrive as an asynchronous continuation on the same TCP connection, consistent with the DEVCTL streaming observed in hal.",
          "LTCP opcode set (ltcp reference, lines 15-26): open=1, seek=2, write=3, read=4, flush=5, devctl=6, close=7, command=8, notify=9, ping=0. CHECKSUM_SUPPORT=false. Primary packet = [0xFF service, opcode, msgid, numParams(|0x80 if checksum), params...]; string param = [0x80][bytes][0x00]; number param = [len][big-endian bytes]."
        ],
        "uncertain": [
          "PRIMARY UNKNOWN — the exact client wire choreography. Two viable models: (A) raw file-IO: OPEN(opcode 1, param = path string '/ir/ir_cap') -> get handle H from the open response, then DEVCTL(opcode 6, param = H [+ maybe capture time/port]) which ACKs then streams the uint16 duration array back as LTCP data packets, then CLOSE(opcode 7). (B) hbus command: a single opcode-8 frame cmd='/ir/ir_cap' with JSON data (as ir_send does) that the generic layer expands into open+devctl and returns the blob as the (possibly second, streamed) response body. LIVE TEST: connect to 127.0.0.1:16716, send 0x01 hello, then try model B first (it mirrors the proven ir_send path) with data {\"time\":5000,\"ports\":1}; if no streamed body, do model A: send OPEN with the path param, read the handle, send DEVCTL with the handle, and dump every byte received for ~10s while pressing a remote at the IR receiver window.",
          "The capture-window/port parameter: hal's ir_cap command handler reads *(handle+0x28) as the ir_read arg but does not itself json_unpack it, so the JSON key name is not provable statically. Best guesses from the sibling ir_test handler ('{s:i, s?i}' with keys 'time','ports', and 'invalid arguments: give values for time and ports' at 0x44a238) are keys 'time' (capture timeout, ms or the 200000µs cap seen in ir_read_thread) and 'ports' (which IR receiver). TEST: vary time and observe how long DEVCTL blocks; vary ports and see if capture succeeds only on the built-in receiver.",
          "The returned capture blob's exact header: hal copies the first 6 bytes of the capture buffer into the response 64-byte frame header (buf[2..7]) and ir_read writes out[0],out[1]=header durations and out[2]=symbol count. Whether the carrier frequency is returned explicitly (like the ir_loopback command which json_pack's '{s:o}' with a freq/duration int) or must be inferred from the mark durations is unconfirmed. TEST: capture a known 38 kHz remote and inspect out[0..2] and whether any field ~= a duration equals ~38000 or the period.",
          "How OPEN returns the handle to the client (which response param carries the fd/index that DEVCTL must echo) — this is generic LTCP file-IO plumbing I did not fully reverse. TEST: capture the OPEN response bytes and decode via the processPrimaryPacket rules (params[i]).",
          "Cancel/abort semantics: strings 'Aborted the ir learing flow' (0x44a014) and 'Timeout occurred - no heartbeat or cancel' (0x449eb0) exist in libir.c and ir_read_thread checks libhal_is_read_sock_available for an abort signal, implying the client can cancel by closing the socket or via a devctl/heartbeat, but the exact cancel packet is unconfirmed. TEST: during an armed DEVCTL, send a CLOSE or drop the socket and confirm hal logs the abort and frees the handle.",
          "Whether a captured blob streamed back is ALREADY in the ir_send-compatible compiled layout ([0xFF][period]...) or is a bare duration array that the client must wrap. If bare, rebuild with the decompiled generateFromRawIr+appendDurationToByteArray algorithm before sending to /ir/ir_send. TEST: capture then immediately replay the raw bytes verbatim via the existing build_command_frame(JSON + blob) and see if the TV responds; if not, wrap per the compiled-IR format."
        ],
        "byte_spec": "CONNECTION (same as ir_send, already working in ir.rs): TCP 127.0.0.1:16716; send single byte 0x01 hello; then LTCP frames. No checksum. Per-packet msgid = 0..63 counter. hbus JSON 'id' = 1..65535 counter.\n\nLTCP PRIMARY PACKET = [0xFF, OPCODE, msgid, numParams, <params...>].  OPCODE: open=1, write=3, read=4, devctl=6, close=7, command=8. String param = [0x80, bytes..., 0x00]. Number param = [len_byte(1..3), big-endian value bytes].\n\n=== RECOMMENDED CAPTURE SEQUENCE (test model A, then B) ===\nMODEL A (raw file-IO, matches hal handler wiring):\n1) OPEN: primary [0xFF, 0x01, mid, 0x01, <string param '/ir/ir_cap'>] i.e. numParams=1, param = [0x80]+b\"/ir/ir_cap\"+[0x00]. Read response; decode params via processPrimaryPacket; extract HANDLE (a number param).  [hal: libhal_ir_cap_open -> calloc(4100)]\n2) DEVCTL (arm+capture+stream): primary [0xFF, 0x06, mid, numParams, <HANDLE number param> (+ optional time/ports number params)]. hal responds: first a success ACK (libhal_send_success_response), then one-or-more data packets carrying the capture. Read until socket idle or CLOSE.  [hal: libhal_ir_cap_raw_read blocks in ir_read ~<=200 ms window]\n3) CLOSE: primary [0xFF, 0x07, mid, 0x01, <HANDLE number param>].\n\nMODEL B (hbus command, mirrors the proven ir_send framing): reuse build_command_frame with usr = JSON only (no trailing blob):\n  usr = b'{\"id\":<rid>,\"cmd\":\"/ir/ir_cap\",\"data\":{\"time\":5000,\"ports\":1}}'\n  frame = [0xFF,0x08,mid1,0x01,0x01,noPkt] + per-secondary [mid,0x80|len (or 0xC0|(len>>8&0x3F),len&0xFF if len>63)] + usr bytes. Then read streamed response(s) exactly like recvResponse (processPrimaryPacket then processSecondaryPacket).\n\n=== CAPTURED BLOB (what ir_read produces / what to store) ===\nArray of big-endian uint16 microsecond values: out[0],out[1] = header durations, out[2] = interval count N, then N alternating mark/space durations. Total bytes = 2 * (return count). Duration = round(sample_count * device_clock / 1000).\n\n=== STORE FOR VERBATIM REPLAY (rebuild the /ir/ir_send compiled-IR blob) ===\nblob layout (0-based bytes, big-endian):\n [0]     = 0xFF                       (raw-timing marker)\n [1..4]  = period_ns (u32) = floor(1_000_000_000 / carrier_Hz)   (carrier_Hz ~= 38000 typical)\n [5]     = 0x32                        (=50)\n [6..7]  = 0x0000                      (preSilence)\n [8]     = 0x00\n [9]     = 0x00\n [10..11]= 0x0000\n [12..13]= 0x0010                      (repeatLocation-1 = 16)\n [14..15]= 0x0000\n [16..17]= repeatLength (u16) = (len(durationBytes))/2 - 1\n [18..]  = duration pairs, each interval encoded as u16 BE:\n             hi = (us>>8)&0xFF; lo = us&0xFF; if PULSE/mark: hi |= 0x80;\n             if us>32767 emit chunks 0xFFFF(mark)/0x7FFF(space) subtracting 32767 until <=32767; clamp remainder to >=30us.\nFire it with the EXISTING working path: LtcpBackend.build_ir_send_frame -> usr = JSON{\"cmd\":\"/ir/ir_send\",\"data\":{enable:true,keyLatency:500,ports:MASK}} .. blob, opcode 8.\n\n=== RUST STATE MACHINE (learn_start/poll/stop) ===\nenum Learn { Idle, Armed{handle:Option<u32>, started:Instant, deadline}, Captured(Vec<u16>), Expired }\nlearn_start(port,timeout_s): connect+hello; MODEL A: send OPEN('/ir/ir_cap'); parse handle; send DEVCTL(handle, time=timeout_s*1000, ports=1<<port); set Armed. (Fallback MODEL B: send opcode-8 command frame.) Do NOT block the caller — keep the socket and poll.\nlearn_poll(): non-blocking read; feed bytes into an LTCP reassembler (processPrimaryPacket/processSecondaryPacket clone); on a completed data body -> parse uint16[] durations -> IrCode{ encoding:\"ltcp_blob\", carrier_hz: from header or default 38000, blob: rebuilt compiled-IR bytes, ports:vec![port], repeat:1 } -> Captured. On deadline with no body -> Expired.\nlearn_stop(): send CLOSE(handle) (model A) or just drop/reconnect the socket (this also serves as cancel; ir_read_thread aborts on socket unavailability).",
        "sources": [
          "/tmp/harmony_rootfs/usr/bin/hal (MIPS BE, stripped) — dispatch table at va 0x452840 (path '/ir/ir_cap' 0x4433b0, method 'ir.cap' 0x4433bc); handlers libhal_ir_cap_open @0x424728, libhal_ir_cap_raw_read @0x4247e4, ir_cap command-slot @0x4246ac, ir_read @0x423fa4, ir_read_thread @~0x423a00, ir_send @0x423c84, ir_send handlers @0x424430/0x424550/0x4245ec; strings 'ir_cap - open file command invoked'(off 303540), 'Error ir_cap open callback'(303584), 'read %d ir packets'(303620), 'Aborted the ir learing flow'(303124), 'Timeout occurred - no heartbeat or cancel'(302768), libhal_ir_cap_open/raw_read symbol strings (303744/303768). Disassembled with capstone 5 (scripts in /tmp/luadec/: disasm_hal.py, annot2.py, table2.py, findtab.py, mipsgot.py) and GOT/dynsym-resolved externals (calloc/ir_read/libhal_send_success_response/libhal_write_socket/libhal_log/json_unpack/pthread_*).",
          "/tmp/luadec/learn_irmanager.lua — decompiled tasks/harmonyengine/core/irmanager.lua: generateFromRawIr (raw 'F/P/S' string -> compiled-IR blob, lines 130-198) and the send-side generateCommandData.",
          "/tmp/luadec/learn_utility.lua — decompiled tasks/harmonyengine/core/utility.lua: appendDurationToByteArray/convertToBytes (duration->uint16 encoding with 0x80 mark bit, 32767 chunking, 30us clamp; lines 9-42).",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/ltcp_decompiled_reference.lua — fileIoCommands opcodes, formPrimaryPacket/formParameter/getNumber, processPrimaryPacket/processSecondaryPacket/recvResponse (the reassembler to clone in Rust).",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/hbus_decompiled_reference.lua — command()'s prevCmd='ir.cap' streaming/continuation logic (multi-phase) and handleCallback (type 8 command vs type 9 notify).",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/hal_main_decompiled_reference.lua — the 0x01 hello and hal_transaction recv loop; ltcpserverconnector_decompiled_reference.lua — server-side response framing (formCommandResponse).",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/src/ir.rs — existing LtcpBackend: connect()+0x01 hello, build_command_frame/build_ir_send_frame (proven opcode-8 path to extend for learn_start/poll/stop)."
        ]
      },
      {
        "key": "blob",
        "summary": "The /ir/ir_send blob is NOT Pronto and NOT a carrier-embedded opaque capture — it is a fully documented, big-endian, 16-byte-header + timing-word structure built entirely in pure Lua (irmanager.lua), which hal (libir.c) parses field-for-field. The header carries carrier period (ns), a constant 50% duty byte, presilence, min-repeat count, and three uint16 byte-offset pointers to start/repeat/finish sequences. Each sequence is a uint16 word-count followed by that many 16-bit words; each word = (markbit<<15)|duration, where bit15=1 means carrier-ON (mark) and 0 means OFF (space), and the low 15 bits are the duration in microseconds (min 30, chunked at 32767). This means we can SYNTHESIZE codes from scratch, not just replay. The 'ports' field is confirmed a single INTEGER bitmask (hal json_unpack {s:b,s?i,s?i}), default 7 (=0b111, all three emitters); the current Rust port_mask() OR-of-(1<<p) is correct for port ids p in {0,1,2}, but the bit->physical-emitter map (built-in blaster vs the two 3.5mm jacks) is not in any firmware string and needs a live blink test.",
        "confirmed": [
          "BLOB HEADER IS 16 BYTES, BIG-ENDIAN. All multi-byte writes are high-byte-first: setShort/addShort do addChar(rshift(val,8)) then addChar(val); addInteger = two addShorts high-half first. Evidence: /tmp/luadec/blob_irmanager.lua:30-48.",
          "Header layout (offsets 0-15), from generateFromKeyCode (blob_irmanager.lua:205-216) and cross-checked field-by-field by utility.byteArraysEqual (blob_utility.lua:152-183): [0]=type byte; [1..4]=period_ns uint32; [5]=duty_cycle; [6..7]=preSilence uint16; [8]=minRepeats; [9]=reserved 0; [10..11]=startLocation; [12..13]=repeatLocation; [14..15]=finishLocation.",
          "byte[0] type: 0x01 = compiled-from-keycode (addChar(1), blob_irmanager.lua:205); 0xFF = raw (addChar(255), blob_irmanager.lua:131). byteArraysEqual calls a[1] the 'ir type' (blob_utility.lua:152-155).",
          "period field (bytes 1-4) = carrier PERIOD in NANOSECONDS = floor(1e9/carrier_freq_hz). protocol.period computed as band(1000000000/CarrierFrequency,-1) (blob_protocol.lua:36); raw path period=band(1000000000/val,-1) where val is the 'F' hex = frequency in Hz (blob_irmanager.lua:186-188). hal debug prints 'period_ns = %d' (hal offset 302464/302656).",
          "byte[5] = duty cycle = constant 0x32 (decimal 50) in BOTH paths (addChar(50) at blob_irmanager.lua:207 and :133). byteArraysEqual labels a[6] 'duty cycle' (blob_utility.lua:160-163). hal prints 'duty_cycle = %d' (hal 302484). So duty is 50%, hardcoded — NOT the 33 in our Rust IrCode.duty (which is informational only).",
          "bytes[6..7] = preSilence uint16 (addShort(device.preSilence), blob_irmanager.lua:208; device.preSilence default 0, blob_device.lua:35). hal prints 'presilence = %d' and 'updated presilence(ns)...' (hal 302508/302556/302592) — hal converts blob presilence into ns, implying the stored unit is NOT ns (consistent with microseconds).",
          "byte[8] = minRepeats (addChar(minRepeats), blob_irmanager.lua:209). Default 3, overridden by device.pressMinRepeats if >=0 (blob_irmanager.lua:201-204); raw path uses 0 (blob_irmanager.lua at addChar(0) after preSilence). hal prints 'min_repeat = %d' (hal 302532).",
          "byte[9] = reserved 0 (addChar(0), blob_irmanager.lua:210).",
          "bytes[10..11]/[12..13]/[14..15] = startLocation/repeatLocation/finishLocation, each a uint16 = the 0-indexed BYTE OFFSET of that sequence's size field, or 0 if absent. generateSequence returns sequenceSizeIndex-1 (blob_irmanager.lua:295) and it is stored via setShort (lines 220,224,228). getSequence reads size at bytes[location+1] i.e. treats location as 0-indexed byte offset (blob_utility.lua:226-233). compareSequences reads these at Lua indices 11,13,15 (blob_utility.lua:172-183).",
          "SEQUENCE format: at the location offset there is a uint16 wordCount (= number of 16-bit duration words), then wordCount*2 bytes of words. generateSequence sets the field to sequenceSize/2 (bytes/2) at blob_irmanager.lua:294; getSequence slices bytes[location+3 .. location+3+sizeInWords*2] (blob_utility.lua:228-229).",
          "DURATION WORD = uint16 big-endian, bit15 = mark/space flag, bits14..0 = duration magnitude. convertToBytes: hb=rshift(band(value,0xFF00),8); if intervalType==1 then hb=bor(hb,128) (set bit15); lb=band(value,0xFF) (blob_utility.lua:35-42). Decoder confirms: sign=rshift(ss,15), duration=band(ss,32767) (blob_utility.lua:211-212).",
          "MARK vs SPACE: intervalType 1 = mark/PULSE (carrier ON) -> bit15 SET; intervalType 0 = space/gap (carrier OFF) -> bit15 CLEAR. Raw grammar maps prefix 'P'->intervalType 1, 'S'->0 (blob_irmanager.lua:190-193).",
          "Duration chunking + clamp (appendDurationToByteArray, blob_utility.lua:9-33): while val>32767 emit a full chunk word (0x7FFF for space, 0xFFFF for mark) and subtract 32767; then if remaining val<30 clamp to 30; emit remainder word. So max magnitude per word is 32767; minimum emitted duration is 30.",
          "RAW-path whole-waveform placement: type=0xFF, startLocation=0, repeatLocation=0x0010 (16), finishLocation=0, and the entire mark/space stream lives in the 'repeat' sequence starting at byte offset 16 (blob_irmanager.lua:131-197). Raw command-string grammar: first token 'F<hexHz>' sets carrier, then alternating 'P<hexMicros>'/'S<hexMicros>' tokens (regex [PS]?%x+, blob_irmanager.lua:148-149; '-' separators stripped, :147).",
          "hal parses the SAME header: its libir.c debug strings expose exactly period_ns, duty_cycle, presilence, min_repeat (hal 302464-302532, file marker 'src/libhal/ir/libir.c' at 302948), and the carrier is generated via I2S ('opening /dev/i2s' 302972; pulse_bclk/pulse_bits math 302656-302692). So hal consumes the identical byte layout irmanager emits.",
          "PORTS IS AN INTEGER, not an array. hal json_unpack format for /ir/ir_send data is '{s:b, s?i, s?i}' (bool enable + optional int keyLatency + optional int ports) at hal offset 303424, immediately followed by literals 'keyLatency' (303440) and 'ports' (303452).",
          "ports value = device.ports, default 7 (blob_device.lua:41). Sourced from config: device.ports = item.Device.ControlPort (blob_userconfigreader.lua:614) and round-tripped configDevice.ControlPort = device.ports (blob_config.lua:200). Passed straight through with no transformation: irManager.startIr/fireIr(..., self.ports) (blob_device.lua:241,261,276) -> irsender:startIr(data,keyLatency,ports) -> hbus:command(path,{enable,keyLatency,ports}) (irsender_decompiled_reference.lua:40-58).",
          "Default 7 = 0b111 => the Hub drives 3 IR outputs (built-in front blaster + two rear 3.5mm mini-jack extenders) and by default fires all three. One bit per emitter. Our Rust IrCode.port_mask() = fold OR (1<<p) yields 7 for ports=[0,1,2], matching the stock all-emitters default; a single p yields 1/2/4.",
          "ports == 0 is SPECIAL (not 'no output'): irsender routes ports==0 to the RF path ('/rf/hot/1/ir/ir_send' when paired/mode==3, else '/ir/ir_send') and does NOT set the 'fire_command' LED; nonzero ports means fire the local IR emitters and lights the LED (irsender_decompiled_reference.lua:43-53).",
          "keyLatency is 500 (ms) for press/fire on real device (blob_device.lua:241,276 pass 500) and is optional int in hal — our Rust hardcoding keyLatency=500 matches.",
          "The ENCODE path is 100% Lua (irmanager.lua + utility.lua + protocol.lua); the C module irdecoder (luaworks.so: luaopen_irdecoder, irdecoder_decode_irpacket, ir_carrier_* symbols) is only the DECODE/learn direction (useCompiledIR=false by default, blob_irmanager.lua:20). So codes can be synthesized entirely in Rust with no C dependency."
        ],
        "uncertain": [
          "Duration UNIT is microseconds: HIGH-CONFIDENCE INFERENCE, not a cited constant. Supports: 'F' token is frequency in Hz (period=1e9/F), 15-bit range + 32767-chunking fits long IR gaps in us (e.g. 40 ms gap => 40000 us needs chunking; in carrier-cycles it would not), min-clamp 30, and Logitech's cloud rawIR XML convention uses us. TEST: on live hal, send a synthesized single mark of a known duration (e.g. one 9000-unit mark) and scope the emitter / compare against a captured known NEC 9ms header; if 9000 units == 9 ms it is microseconds.",
          "bit->physical-emitter MAP is UNKNOWN (no 'emitter/blaster/jack/gpio' port-bit string exists in hal; only 'ir_send - devctrl default %02x' at hal 303500). NEEDS LIVE TEST: fire ports=1, then 2, then 4 (and 7) individually and watch which physical output blinks (built-in front blaster vs jack A vs jack B). Likely bit0=built-in, bit1/bit2=the two 3.5mm jacks, but order of the two jacks is a guess.",
          "Playback semantics of the three sequences (start once / repeat while held / finish on release) and how min_repeat interacts with a lone enable=true are INFERRED from field names + hal strings ('send_repeat == -1', heartbeat/cancel/'Timeout occurred - no heartbeat or cancel' at hal 302764/302904). Our Rust send_ir sends only enable=true and never the enable=false cancel; stock fireIr does startIr(enable=true) then immediately cancelIr(enable=false) (irmanager.lua:63-71, device.lua:261 path). TEST: send one code with minRepeats=1..3, observe whether it fires the expected count and whether hal holds/repeats until a follow-up enable=false is sent.",
          "byte[5] duty '50' is interpreted by hal as 50 percent: the numeric byte is CONFIRMED 50, but that it means literally 50% duty (vs some scaled unit) rests on hal's 'duty_cycle=%d ... %0.0f%%' pulse-bits math. TEST: scope carrier duty cycle on a live blast; expect ~50% high time per carrier period.",
          "preSilence unit (uint16) is assumed microseconds like durations (hal converts it to ns via 'updated presilence(ns)'). For synthesis, copying the stock device value (often 0) is safe; a nonzero value's exact unit should be scope-verified."
        ],
        "byte_spec": "/ir/ir_send DATA = JSON envelope bytes immediately followed by the raw IR BLOB (no checksum, no length prefix between them; hal json_decode's the leading object and treats trailing bytes as the blob). JSON = {\"id\":N,\"cmd\":\"/ir/ir_send\",\"data\":{\"enable\":true,\"keyLatency\":500,\"ports\":<int mask>}}.\n\nBLOB (all fields BIG-ENDIAN):\nOffset Size Field\n0      1    type            0x01 = compiled/keycode, 0xFF = raw (both share this exact layout)\n1      4    period_ns       uint32 = floor(1_000_000_000 / carrier_freq_hz)  (e.g. 38 kHz -> 26315)\n5      1    duty_cycle      constant 0x32 (=50 -> 50%)\n6      2    preSilence      uint16 (microseconds; 0 is fine)\n8      1    minRepeats      uint8  (keycode: device.pressMinRepeats, default 3; raw: 0)\n9      1    reserved        0x00\n10     2    startLocation   uint16 = 0-indexed byte offset of the start-sequence size field, 0 if none\n12     2    repeatLocation  uint16 = same, for repeat sequence\n14     2    finishLocation  uint16 = same, for finish sequence\n16..   ...  sequence data (referenced by the *Location offsets above)\n\nSEQUENCE (located at offset L, i.e. one of start/repeat/finish points here):\n  L+0  2  wordCount  uint16 = number of 16-bit duration words that follow\n  L+2  wordCount*2 bytes: the duration words\n\nDURATION WORD (uint16 BE):\n  bit15      = 1 -> MARK (carrier ON / pulse) ; 0 -> SPACE (carrier OFF / gap)\n  bits14..0  = duration magnitude in MICROSECONDS (min 30). For a duration D>32767:\n               emit floor chunks of 32767 as full words (0x7FFF for space, 0xFFFF for mark),\n               subtracting 32767 each time, then one final word for the remainder\n               (remainder clamped up to 30 if <30). i.e. word = (mark?0x8000:0) | min(D,32767).\n\nMINIMAL ONE-SHOT SYNTHESIS RECIPE (mirrors the stock RAW path, all in start-of-slot 16):\n  bytes = [0xFF]                              # type raw\n        + u32be(floor(1e9/freq_hz))           # period_ns\n        + [0x32]                              # duty 50\n        + u16be(0)                            # preSilence\n        + [0x00]                              # minRepeats (raise to 1..3 if a lone enable w/o cancel)\n        + [0x00]                              # reserved\n        + u16be(0)                            # startLocation = 0\n        + u16be(16)                           # repeatLocation = 0x0010  (points at the size field below)\n        + u16be(0)                            # finishLocation = 0\n        + u16be(N)                            # wordCount = number of mark/space words\n        + [ (mark?0x8000:0)|clampchunk(us) ... ]   # N words, alternating mark(P)/space(S) in microseconds\n  Then set data.ports = mask (see below), enable=true. To stop, send a second command enable=false\n  (stock does startIr(enable=true) then cancelIr(enable=false)); with minRepeats>=1 the frame plays\n  at least that many times regardless.\n\nPORTS ENCODING:\n  data.ports is a single INTEGER BITMASK (hal json_unpack '{s:b, s?i, s?i}'; optional int).\n  Default on stock = 7 = 0b111 = all three IR outputs. One bit per emitter.\n  ports = 0 is SPECIAL: routes to the RF/hub path instead of the local IR blaster (do NOT use 0 to\n  mean 'off' — use it only for RF). To fire the local emitters, use a nonzero mask.\n  Our Rust port_mask() = OR over ports of (1<<p) is CORRECT for port ids p in {0,1,2}:\n     ports=[0,1,2] -> 7 (stock 'all'), [0]->1, [1]->2, [2]->4.\n  Bit->physical map (built-in front blaster vs the two 3.5mm mini-jack extenders) is UNVERIFIED:\n  fire masks 1,2,4 individually on a live Hub and watch which output blinks to pin it down.",
        "sources": [
          "/tmp/luadec/blob_irmanager.lua (decompiled tasks/harmonyengine/core/irmanager.lua): byte helpers setChar/addChar/addShort/addInteger/setShort/setInteger (22-48); generateFromKeyCode header+sequence build (200-230); generateFromRawIr raw header + 'F/P/S' grammar (130-198); generateSequence size field = sequenceSize/2, returns offset (254-296); fireIr/startIr pass ports (58-84)",
          "/tmp/luadec/blob_utility.lua (decompiled tasks/harmonyengine/core/utility.lua): appendDurationToByteArray chunk+clamp (9-33); convertToBytes mark-bit=0x80 on high byte (35-42); byteArraysEqual field map type/period/duty/presilence/minrepeats (141-185); convertBytesToDurations sign=ss>>15,dur=ss&0x7FFF (202-224); getSequence size-at-location+words (226-233)",
          "/tmp/luadec/blob_protocol.lua (decompiled tasks/harmonyengine/model/protocol.lua): period = floor(1e9/CarrierFrequency) (36); segment/atoms -> durations via appendDurationToByteArray (107-127)",
          "/tmp/luadec/blob_device.lua (decompiled tasks/harmonyengine/model/device.lua): default ports=7 (41); fire/press/hold/release calls passing self.ports and keyLatency=500 (241,261,276)",
          "/tmp/luadec/blob_userconfigreader.lua (decompiled tasks/harmonyengine/loader/userconfigreader.lua): device.ports = item.Device.ControlPort (614)",
          "/tmp/luadec/blob_config.lua (decompiled tasks/harmonyengine/apihandler/config.lua): configDevice.ControlPort = device.ports (200)",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/irsender_decompiled_reference.lua: startIrCommand builds hbus data {enable,keyLatency,ports}; ports==0 -> RF path + no LED (40-58, 43-53)",
          "/tmp/harmony_rootfs/usr/bin/hal (strings -t d): libir.c IR codec params period_ns/duty_cycle/presilence/min_repeat (offsets 302428-302592), pulse_bclk/pulse_bits/I2S carrier gen (302656-302972), ir_send json_unpack '{s:b, s?i, s?i}' + keyLatency/ports (303424-303452), 'ir_send - devctrl default %02x' (303500), file marker src/libhal/ir/libir.c (302948)",
          "/tmp/harmony_rootfs/opt/lib/luaworks.so (strings): irdecoder / ir_carrier_* symbols = decode/learn direction only (luaopen_irdecoder, irdecoder_decode_irpacket, ir_carrier_data_section_process, etc.)",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/src/ir.rs: current IrCode/port_mask() and build_ir_send_frame under review (lines 27-33, 180-192)"
        ]
      },
      {
        "key": "selftest",
        "summary": "hal exposes four IR commands over LTCP — /ir/ir_send, /ir/ir_cap, /ir/ir_loopback, /ir/ir_test (each with a short alias ir.send/ir.cap/ir.loopback/ir.test) — dispatched from a 0x38-byte-record table in .data at VMA 0x4627d0+. The command's execute callback lives at record offset +0x2c. Two of these need NO learned blob: /ir/ir_test emits a hardcoded 40 kHz/50%-duty test waveform that hal keeps as 22 static bytes in .data (0x463470), and /ir/ir_loopback emits a second built-in 62.5 kHz pattern (0x463458) while a capture thread receives it and validates the carrier frequency (a true RF-path self-test). Both call the exact same low-level ir_send() (0x423c84) that the already-working /ir/ir_send path uses, so if ir_test emits, real IR sends work. Because ir_send() only prepends 8 bytes (keyLatency, ports) to the payload, those 22 static bytes ARE a valid /ir/ir_send LTCP blob, giving a second no-blob-needed option that reuses the proven ir.rs path. LED cue: /sys/led {\"action\":\"fire_command\"} drives the front-LED \"IrFired\" animation, but the lua sets it before sending — hal's /ir/ir_send does NOT set it automatically, and it is only a UI hint, not proof of RF emission.",
        "confirmed": [
          "Full IR command surface exists as path+alias string pairs in a dispatch table of 0x38-byte records in .data: /ir/ir_cap+ir.cap (record VMA 0x462840), /ir/ir_send+ir.send (0x462878), /ir/ir_loopback+ir.loopback (0x4628b0), /ir/ir_test+ir.test (0x4628e8). Evidence: strings at file offsets 275376/275396/275416/275444 and python-resolved record dump showing path ptr at +0x00, alias ptr at +0x04, handler fn ptrs at +0x0c/+0x14/+0x18/+0x20/+0x28/+0x2c.",
          "In every IR record the +0x2c slot is the main execute handler: irsend +2c=0x424430, ircap +2c=0x4246ac, irloop +2c=0x424adc, irtest +2c=0x424c18. ir.rs already invokes /ir/ir_send which reaches irsend +2c, so invoking /ir/ir_test the same way reaches irtest +2c.",
          "/ir/ir_send execute (0x424430) calls json_unpack(req+0xc, \"{s:b, s?i, s?i}\", \"enable\",&b, \"keyLatency\",&i, \"ports\",&i) then ir_send(blob=req+0x18, len=req+0x1c, 500000, keyLatency, ports&7). Format string 0x44a140='{s:b, s?i, s?i}', keys 'enable'(0x449684) 'keyLatency'(0x44a150) 'ports'(0x44a15c). The binary blob is carried separately from JSON at request-struct +0x18(ptr)/+0x1c(len).",
          "/ir/ir_test execute (0x424c18) calls json_unpack(req+0xc, \"{s:i, s?i}\", \"time\",&time, \"ports\",&ports) with ports pre-defaulted to 7, then ir_send(buf@0x463470, 22, time*0xF4240, 0, ports&7). Format string 0x44a220='{s:i, s?i}', key 'time'(0x44a22c), key 'ports'(0x44a15c), error 0x44a234='invalid arguments: give values for time and ports'. Constant 0xF4240=1000000. time is REQUIRED (s:i), ports OPTIONAL default 7 (all ports).",
          "An alternate string-form ir_test entry at record slot +0x14 (0x424cd8) parses the request body as a \"TIME, PORTS\" ASCII string via strtok(delim=\", \" @0x44a26c)/strtol(base10), then makes the identical ir_send(buf@0x463470, 22, time*1000000, 0, ports&7) call.",
          "The ir_test/loopback payloads are REAL 22-byte IR waveforms baked into .data, NOT zeros (my first read was a segment-mapping bug: seg2 file = vaddr-0x410000, not -0x400000). ir_test buf @0x463470 (fileoff 0x53470) = 01 00 00 61 a8 32 00 00 04 00 00 00 00 10 00 00 00 02 82 ee 02 ee. loopback buf @0x463458 (fileoff 0x53458) = 01 00 00 3e 80 32 00 00 05 00 00 00 00 10 00 00 00 02 84 e2 04 e2. ir_init (0x424244) does not overwrite them.",
          "Low-level ir_send (dynsym 0x423c84) signature ir_send(blob_ptr, blob_len, count, keyLatency, ports): it alloc(len+8), memcpy(newbuf+8, blob, len), writes newbuf[0]=keyLatency, newbuf[4]=ports, then tail-calls the enqueue/thread-signal fn at 0x422dd0 with a0=max(count,1), a1=newbuf. So the 22-byte payload is exactly what a /ir/ir_send LTCP blob must contain (ir_send only prepends the 8-byte keyLatency/ports header).",
          "IR blob header layout, from the send-thread parser at 0x422410 reading newbuf[9..0x10] (=blob[1..8]): blob[0]=type=0x01; blob[1..4] big-endian = period_ns (ir_test 25000 = 40.0 kHz, loopback 16000 = 62.5 kHz); blob[5]=duty_cycle percent, clamped to <=50 (both =50); blob[6..7] BE * 1000 = presilence ns (both =0); blob[8]=min_repeat (test 4, loop 5). Logged via formats period_ns(0x449d80)/duty_cycle(0x449d94). blob[9..21] is the pulse/space symbol table.",
          "period_ns is range-checked at 0x422438: valid window [0x7d0, 0x7d0+0x1aa38] ns = [2000, 111112] ns (carrier ~9 kHz..500 kHz); out of range logs 'IR frequency out of range %d' (0x449d40) and aborts. 40 kHz(25000) and 62.5 kHz(16000) are both in range.",
          "/ir/ir_loopback execute (0x424adc) calls ir_loopback_test (dynsym 0x423dd8) then json_pack a response. ir_loopback_test IGNORES its request argument (uses fixed globals): pthread_mutex_lock(0x464dbc), pthread_create(capture thread fn @0x422264), ir_send(buf@0x463458, 22, 0, 0, ports=7), ir_send_cancel, then returns captured data. So loopback needs NO params and NO blob.",
          "The loopback is a genuine emit+receive RF self-test: it validates the received carrier via 'loopback frequency %d' (0x449fc8) / 'loopback frequency out of range %d' (0x449fe4). ir_read/ir_read_thread on /dev/rfspi (opens via calloc 0x1004 in ircap path) capture what was emitted.",
          "/ir/ir_cap (execute 0x4246ac, open 0x424728) is the IR LEARN path: it opens the capture device and does ir_read of received IR packets ('ir_cap - open file command invoked' 0x44a1b0, 'read %d ir packets' 0x44a208, libhal_ir_cap_open/libhal_ir_cap_raw_read). It RECEIVES, so it needs no blob but does not emit.",
          "LED feedback: hal command /sys/led (alias sys.led, string 275808) takes {\"action\":<str>}; action 'fire_command' (string @298096) triggers the LED state-machine 'IrFired' transitions (led_Idle_IrFired/led_Start_IrFired/led_UsbPower_IrFired, 'IrFired' @274056); unknown action -> 'Invalid action' (274084). hal_utils_decompiled_reference.lua setLedAction() sends hbus:command('sys.led',{action=arg}); irsender_decompiled_reference.lua calls halUtils.setLedAction('fire_command') just before the ir_send (lines 52,91). hal's /ir/ir_send does not set the LED itself.",
          "The binary is not fully stripped: it retains .dynsym with function names/addresses (SYMTAB 0x401908, STRTAB 0x403768, 486 syms) and MIPS GOT metadata (GOT 0x463530, gp 0x46b520, LOCAL_GOTNO=12, GOTSYM=11). GOT local anchors: local3=0x440000, local4=0x450000(rodata), local5=0x460000(.data/.bss), local7=0x420000(.text). This let every gp-relative call/string be resolved to real symbols (json_unpack, strtok, strtol, ir_send, ir_loopback_test, pthread_*, snprintf, libhal_log)."
        ],
        "uncertain": [
          "Exact unit/semantics of ir_test 'time': it is multiplied by 1,000,000 and passed as ir_send()'s 3rd arg (the same slot /ir/ir_send hardcodes to 500000), which feeds the enqueue fn 0x422dd0 as a count/duration (min(1)). It could be a repeat count or an emission duration; and ir_send starts a thread that repeats until cancel or 'Timeout occurred - no heartbeat or cancel'. TEST LIVE: fire /ir/ir_test {\"time\":100} vs {\"time\":5000,\"ports\":7} and measure emission length on an IR receiver/scope; if it runs continuously, stop it with /ir/ir_send {\"enable\":false} (or ir.send cancel).",
          "Whether /ir/ir_test needs an enable/heartbeat/cancel lifecycle like ir_send. irsender sends periodic {enable=true} heartbeats and a cancel. TEST LIVE: fire ir_test once and observe whether it self-terminates; if it hangs the emitter on, issue the ir_send cancel path.",
          "Exact bit-to-port mapping of the ports mask (masked &7). Likely bit0=port1, bit1=port2, bit2=port3 of the hub's 3 IR blaster ports; 7=all. TEST LIVE: fire ir_test with ports=1,2,4 separately and watch which physical emitter/port lights on an IR camera.",
          "Full encoding of blob[9..21] (the pulse/space symbol table after the 9-byte header). Header fields (period_ns/duty/presilence/min_repeat) are confirmed; the trailing 13 bytes (ir_test: 00 00 00 00 10 00 00 00 02 82 ee 02 ee) are a symbol/timing list not fully traced. Not needed to replay verbatim, but needed to synthesize new patterns. TEST/REVERSE: trace the send-thread loop past 0x4224f8 (reads newbuf 0x12..0x17) which branches on blob[10..11].",
          "ir_loopback return payload semantics: it json_pack's a '{s:o}' response containing an object (likely captured-count or measured carrier freq) and pass/fail is implied by the 'loopback frequency out of range' check. TEST LIVE: run /ir/ir_loopback and inspect the returned JSON to learn the pass/fail field before relying on it as an automated self-test oracle.",
          "Standalone (unpaired) mode should use /ir/ir_test and /ir/ir_send directly; the paired variant is /rf/hot/1/ir/ir_send (routes through the RF dongle 'hot' path, irsender picks it when prefMgr.mode==3 or ports==0). Assumed the direct /ir/* path is correct for this no-cloud appliance, but not verified that /rf/hot isn't required on this specific unit. TEST LIVE: confirm /ir/ir_test returns success (not code 500 'RF timed out'/503 'RF link lost')."
        ],
        "byte_spec": "RECOMMENDED NO-BLOB VALIDATION (fire from Rust over the existing LTCP connection: 0x01 hello byte, then a command frame opcode=8, CHECKSUM_SUPPORT=false, same framing ir.rs already uses for /ir/ir_send).\n\n=== OPTION A (PRIMARY, simplest — hal supplies the waveform) ===\nCommand path: \"/ir/ir_test\"   (alias \"ir.test\")\nJSON params (request body, NO binary blob; blob length = 0):\n   {\"time\": 1000, \"ports\": 7}\n - \"time\": REQUIRED integer. hal computes time*1000000 and passes it to ir_send() as the repeat/duration count (see UNCERTAIN — start ~1000, or lower for a single short burst).\n - \"ports\": OPTIONAL integer bitmask, default 7 (all 3 IR blaster ports). Masked with &7. Use 7 to maximize the chance an emitter is visible.\nEffect: hal emits its built-in 40.0 kHz / 50% duty test pattern (the 22 bytes at .data 0x463470) via the identical ir_send()/dev-i2s/dev-rfspi path used by real sends. Observe with a phone/USB camera pointed at the front IR window (the blaster LED will flicker). To stop if it runs continuously, send /ir/ir_send {\"enable\": false}.\n\n=== OPTION B (fallback — reuse the PROVEN ir.rs /ir/ir_send path with a known-good blob) ===\nCommand path: \"/ir/ir_send\"   (exactly what LtcpBackend already builds)\nJSON params: {\"enable\": true, \"keyLatency\": 0, \"ports\": 7}\nBinary blob (22 bytes, verbatim — this is hal's own built-in ir_test waveform, a valid ir_send payload):\n   01 00 00 61 A8 32 00 00 04 00 00 00 00 10 00 00 00 02 82 EE 02 EE\nThis drives the exact code path ir.rs is confirmed to work with, emitting the 40 kHz pattern. (Loopback's built-in 62.5 kHz payload, if you want a second known blob: 01 00 00 3E 80 32 00 00 05 00 00 00 00 10 00 00 00 02 84 E2 04 E2.)\n\n=== OPTION C (RF PATH SELF-TEST, needs no camera — closed-loop emit+receive) ===\nCommand path: \"/ir/ir_loopback\"   (alias \"ir.loopback\")\nJSON params: none required ({} is fine); no blob.\nEffect: hal starts an IR capture thread, emits the built-in 62.5 kHz pattern on all ports, receives it back, and validates the measured carrier (\"loopback frequency\" range check). Returns a JSON object. This is the manufacturing RF-path self-test and confirms the emitter physically works WITHOUT any external IR receiver. (Parse/pass-criterion: see UNCERTAIN — inspect the returned JSON once live.)\n\n=== OPTIONAL VISUAL CUE (matches stock firmware behavior) ===\nBefore/around the send, optionally fire: path \"/sys/led\" (alias \"sys.led\"), params {\"action\": \"fire_command\"} to flash the front LED \"IrFired\" animation. NOTE this is only a UI hint (the stock lua sets it manually); it is NOT proof of RF emission. Real proof = camera (A/B) or loopback carrier check (C).\n\n=== IR BLOB WIRE FORMAT (payload that follows the 8-byte keyLatency/ports header ir_send prepends internally) ===\nConfirmed header (big-endian), parser at hal 0x422410:\n  off 0     u8   type/version         = 0x01\n  off 1..4  u32  period_ns            (carrier period; 25000=40kHz, 16000=62.5kHz); VALID RANGE [2000, 111112] ns else \"IR frequency out of range\"\n  off 5     u8   duty_cycle_percent   (clamped to <=50; both built-ins = 50)\n  off 6..7  u16  presilence_units     (multiplied by 1000 -> ns; both built-ins = 0)\n  off 8     u8   min_repeat           (built-ins: ir_test=4, loopback=5)\n  off 9..   symbol/timing table (pulse/space list) — replay verbatim; full encoding not yet traced.\nRequest-struct note: over LTCP the JSON params and the binary blob are delivered separately; hal reads JSON at request+0x0c and the blob at request+0x18(ptr)/+0x1c(len). For ir_test the blob is empty.",
        "sources": [
          "/tmp/harmony_rootfs/usr/bin/hal (ELF32 MIPS big-endian, dynsym retained). Key functions: ir_send low-level @0x423c84 (blob->packet builder, tail-calls enqueue @0x422dd0); ir_loopback_test @0x423dd8 (fixed-buffer emit+capture self-test); ir_init @0x424244; ir_read @0x423fa4; send-thread blob-header parser @0x422410 (period_ns/duty/presilence/min_repeat range checks).",
          "hal dispatch handlers (static, between ir_init end 0x424424 and libhal_dongle_info_get 0x425094): /ir/ir_send execute 0x424430 (json_unpack {s:b,s?i,s?i}); /ir/ir_test execute 0x424c18 (json_unpack {s:i,s?i}) and string-form 0x424cd8 (strtok/strtol); /ir/ir_loopback execute 0x424adc; /ir/ir_cap 0x424728/0x4247e4/0x4246ac.",
          "hal .data command dispatch table: 0x38-byte records at VMA 0x462808(ota),0x462840(/ir/ir_cap),0x462878(/ir/ir_send),0x4628b0(/ir/ir_loopback),0x4628e8(/ir/ir_test); path ptr +0x00, alias +0x04, exec handler +0x2c.",
          "hal built-in IR test waveforms in .data: ir_test @VMA 0x463470 (fileoff 0x53470), loopback @VMA 0x463458 (fileoff 0x53458), 22 bytes each (mapping seg2 file=vaddr-0x410000).",
          "hal .rodata format/key strings: 0x44a140 '{s:b, s?i, s?i}', 0x449684 'enable', 0x44a150 'keyLatency', 0x44a15c 'ports', 0x44a220 '{s:i, s?i}', 0x44a22c 'time', 0x44a234 error, 0x44a26c ', ' (strtok delim), 0x449d80 'period_ns = %d', 0x449d94 'duty_cycle = %d', 0x449fc8 'loopback frequency %d', 0x449fe4 'loopback frequency out of range %d'; LED: 275808 '/sys/led', 298096 'fire_command', 274056 'IrFired', 274084 'Invalid action'.",
          "hal dynsym (objdump -T): ir_send 0x423c84, ir_send_repeat 0x423d9c, ir_send_cancel 0x423db8, ir_loopback_test 0x423dd8, ir_read 0x423fa4, ir_init 0x424244, libhal_rf_test 0x42580c; MIPS GOT meta GOT=0x463530 gp=0x46b520 LOCAL_GOTNO=12 GOTSYM=11.",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/irsender_decompiled_reference.lua (startIrCommand/sendIrHeartBeat/cancelIr: hbus:command('/ir/ir_send',{enable,keyLatency,ports},data); setLedAction('fire_command') at lines 52,91; paired path /rf/hot/1/ir/ir_send).",
          "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/hal_utils_decompiled_reference.lua (setLedAction -> hbus:command('sys.led',{action=arg}); ledPath 'sys.led' or 'hot.1.sys.led').",
          "Analysis scripts written to /tmp: /tmp/resolver.py (gp-offset->dynsym/local resolver), /tmp/bat.py & /tmp/bat2.py (capstone MIPS disassembler with GOT/string annotation), /tmp/init.py. Capstone 5.0.7 used (Apple llvm-objdump lacks the MIPS target)."
        ]
      }
    ],
    "spec": "# IR CLIENT IMPLEMENTATION SPEC — service/irapi/src/ir.rs\n\nCoding target: extend `LtcpBackend`. The current `send_raw` (ir.rs:149-174) does ONE 512-byte read and returns — that is the bug: it stops after the coalesced 6/7-byte primary and never reads the JSON body. Replace with a proper LTCP reassembler. All facts below are CONFIRMED from the C hal disassembly + decompiled ltcp.lua unless marked ⚠UNCERTAIN.\n\n---\n\n## 1) RESPONSE PARSING\n\n### Wire format (no checksum in our config; hal writes primary + secondaries as many tiny socket writes)\n\nPRIMARY (offsets): `[0]=0xFF serviceId` · `[1]=cmd (echoes 0x08)` · `[2]=reqId = requestReqId|0x80` · `[3]=numParamsByte (bit0x80=checksumPresent, low6=paramCount)` · `[4..]=params`. Each param is TLV: `lenByte; w=lenByte&0x3F; if w==0 → NUL-terminated string; else w big-endian bytes → unsigned number`. Trailing XOR checksum byte ONLY if `numParamsByte&0x80` (never, for us — we send numParams=0x01).\n\nFor a `command`(8) success the single param is the packet COUNT = numPackets (total incl. primary). Small ir_send ack = `FF 08 (mid|0x80) 01 01 02` (6 bytes), so `secondaryCount = param0 - 1 = 1`.\n\nSECONDARY (repeat secondaryCount times): `[0]=seqByte (may have 0x80 set — ignore)` · `[1]=lenByte; if (lenByte&0x40): extended → read 1 more byte, len=((lenByte&0x3F)<<8)|next; else len=lenByte&0x3F` · `[2..]=payload[len]`. Concatenate all payloads → BODY.\n\nBODY = `<JSON text>[<binary tail>]`. JSON = `{\"id\":<echoes request id>,\"code\":<int>,...}` (code 200=OK, 500=\"RF timed out\", 503=\"RF link lost\"). Split point = end of top-level `{...}`; bytes after = binaryTail.\n\nreqId markers: `0xFF` (255) = transport-level LTCP error packet → treat as error. `0xFE` (254) = EOF marker for streamed reads (not emitted by ir_send; matters for ir_cap streaming — see §2). `(reqId & 0x80)` = isResp; correlate `(reqId & 0x7F) == mid` you put in request primary byte[2].\n\n### Rust algorithm\n\nWrap the stream in `BufReader` and use `read_exact` everywhere — never trust one `read()`. Read byte-by-byte for headers/params, `read_exact(&mut vec![0;len])` for payloads.\n\n```\nstruct Reply { req_id:u8, is_resp:bool, is_error:bool, is_eof:bool,\n               msg_type:u8, count:u64, json:Vec<u8>, binary:Vec<u8> }\n\nfn read_reply<R:Read>(s:&mut R) -> Result<Reply,IrError> {\n    let b1 = |s:&mut R| -> io::Result<u8> { let mut b=[0u8;1]; s.read_exact(&mut b)?; Ok(b[0]) };\n    let mut h=[0u8;4]; s.read_exact(&mut h)?;                       // primary header\n    if h[0]!=0xFF { return Err(IrError::Protocol(format!(\"bad serviceId {:#x}\",h[0]))); }\n    let (msg_type, req_id, np) = (h[1], h[2], h[3]);\n    let (is_resp, is_error, is_eof) = (req_id&0x80!=0, req_id==0xFF, req_id==0xFE);\n    let has_cksum = np&0x80!=0;\n    let mut nums=Vec::new();\n    for _ in 0..(np&0x3F) {                                          // primary params (TLV)\n        let lb=b1(s)?; let w=(lb&0x3F) as usize;\n        if w==0 { while b1(s)?!=0 {} }                              // NUL-terminated string\n        else { let mut v=0u64; for _ in 0..w { v=(v<<8)|b1(s)? as u64; } nums.push(v); }\n    }\n    if has_cksum { let _=b1(s)?; }\n    if is_error { return Err(IrError::Backend(\"ltcp error packet (reqId=0xFF)\".into())); }\n    let count = *nums.get(0).unwrap_or(&1);\n    let sec = if is_eof {0} else { count.saturating_sub(1) };        // eof carries no body\n    let mut body=Vec::new();\n    for _ in 0..sec {                                                // secondary packets\n        let _seq=b1(s)?; let lb=b1(s)?;\n        let len = if lb&0x40!=0 { (((lb&0x3F) as usize)<<8)|(b1(s)? as usize) }\n                  else { (lb&0x3F) as usize };\n        let mut buf=vec![0u8;len]; s.read_exact(&mut buf)?; body.extend_from_slice(&buf);\n        if has_cksum { let _=b1(s)?; }\n    }\n    let jend = json_end(&body);                                     // index past top-level {...}\n    let (json,binary)=body.split_at(jend);\n    Ok(Reply{req_id,is_resp,is_error,is_eof,msg_type,count,\n             json:json.to_vec(), binary:binary.to_vec()})\n}\n```\n\n`json_end`: brace-depth scan honoring string literals + backslash escapes (start at first `{`, return index just past matching `}`). Surface `{reqId, code, errorCode, errorString, binary}` by parsing the JSON slice: read `code` (i64); read optional `errorCode`/`errorString` if present. Success = `code==200` AND no `errorCode`. Note ⚠UNCERTAIN: the C hal at :16716 packs base `{\"id\",\"code\"}` plus possibly one more key; the `data.errorCode/errorString` shape belongs to the 16717 connectserver layer we do NOT hit. Parse defensively for both `code` and `errorCode`/`errorString`; never hard-require keys beyond `code`.\n\nRead-loop timeout: keep the socket's `set_read_timeout`; on WouldBlock/TimedOut mid-frame return `IrError::Timeout` (a partial frame means hal is still writing or died). Replace `send_raw` body with `write_all(frame); flush(); read_reply(&mut BufReader::new(&s))`.\n\n---\n\n## 2) LEARN (/ir/ir_cap)\n\nhal wires ir_cap with only OPEN(+0x0c=0x424728), DEVCTL(+0x20=0x4247e4), and command/notify(+0x2c=0x4246ac) — NO plain READ slot. OPEN callocs a 4100-byte capture buffer into the handle; DEVCTL `libhal_ir_cap_raw_read` sends an early success ACK, then blocks in `ir_read()` (≤200 ms window, `s7=0x30d40`), then STREAMS the captured uint16 duration array back as 64-byte LTCP data frames (`buf[0]=0x90`) via `libhal_write_socket`. The command-slot handler reads its capture arg from `handle+0x28` (pre-populated by the generic layer — not json_unpack'd there).\n\n### State machine\n\n```\nenum Learn { Idle,\n             Armed { handle:Option<u32>, started:Instant, deadline:Instant },\n             Captured(Vec<u16>), Expired }\n```\n\n`learn_start(port, timeout_s)`: `connect()` (0x01 hello). Then **try MODEL B first** (mirrors the proven opcode-8 path, least new code):\n\n- ⚠UNCERTAIN(primary unknown): send a `command`(8) frame via existing `build_command_frame` with `usr` = JSON only, no blob:\n  `{\"id\":<rid>,\"cmd\":\"/ir/ir_cap\",\"data\":{\"time\":5000,\"ports\":<1<<port>}}`\n- Then read streamed replies with `read_reply` in a loop until EOF/idle. Set `Armed`.\n\nIf MODEL B yields no streamed body, fall back to **MODEL A** (raw file-IO, matches hal handler wiring exactly):\n1. OPEN: primary `[0xFF,0x01,mid,0x01, <string param>]` where string param = `[0x80] + b\"/ir/ir_cap\" + [0x00]`. `read_reply`; extract HANDLE from the first numeric param. ⚠UNCERTAIN(unknown #4): which response param carries the handle — decode all params and log them; the fd/index is a small int.\n2. DEVCTL (arm+capture+stream): primary `[0xFF,0x06,mid,numParams, <HANDLE num param>(+optional time/ports num params)]`. hal ACKs, then streams data packets. Read until idle or EOF(reqId 0xFE).\n3. CLOSE: primary `[0xFF,0x07,mid,0x01,<HANDLE num param>]`.\n\n`learn_poll()`: non-blocking read; feed bytes into the same LTCP reassembler; on a completed data body → parse to `Vec<u16>` (big-endian) → build `IrCode`. Deadline with no body → `Expired`.\n\n`learn_stop()`: MODEL A → send CLOSE(handle); MODEL B → just drop/reconnect the socket. Dropping the socket also cancels: `ir_read_thread` aborts on `libhal_is_read_sock_available` going false.\n\n### Captured → replayable IrCode\n\nThe streamed array is `out[0],out[1]`=header durations, `out[2]`=interval count N, then N alternating mark/space durations (each big-endian uint16, µs). ⚠UNCERTAIN: whether the streamed blob is ALREADY the ir_send-compiled layout or a bare duration array. **Assume bare** and rebuild the compiled blob per §3 (`type=0xFF` raw path): `[0xFF] + u32be(period_ns) + [0x32] + u16be(0) + [0x00] + [0x00] + u16be(0)startLoc + u16be(16)repeatLoc + u16be(0)finishLoc + u16be(N) + [ (mark?0x8000:0)|clampchunk(us) ... ]`. Store as `IrCode{ encoding:\"ltcp_blob\", carrier_hz: 38000 (or derived), blob, ports:vec![port], repeat:1 }`.\n\n### Live confirmations required (precise tests)\n\n- **Choreography (B vs A)** — connect, 0x01 hello, send MODEL B ir_cap command with `{\"time\":5000,\"ports\":1}`; press a remote at the IR window; dump every received byte for ~10 s. If a streamed uint16 body arrives → B works. Else run MODEL A (OPEN→handle→DEVCTL) and dump bytes.\n- **Param keys** — vary `time` and confirm DEVCTL block duration tracks it; vary `ports` and confirm capture only on the built-in receiver. Sibling `ir_test`/`ir_loopback` use keys `time`/`ports`, so those are the best guesses.\n- **Handle plumbing (A)** — decode the OPEN response params and identify which numeric param DEVCTL must echo.\n- **Blob shape** — capture then immediately replay the raw received bytes verbatim through `build_ir_send_frame`; if the TV responds, the stream is already compiled; if not, wrap per §3.\n- **Carrier in header** — capture a known 38 kHz remote; inspect `out[0..2]` for a field ≈ 38000 or the period, to decide whether carrier is returned or must default to 38000.\n\n---\n\n## 3) PORTS + BLOB\n\n**`ports` is a single INTEGER bitmask** — CONFIRMED (hal `json_unpack '{s:b, s?i, s?i}'`, masked `&7`), NOT an array. Default stock = 7 = 0b111 = fire all three IR outputs (front blaster + two rear 3.5 mm extender jacks). One bit per emitter. `ports==0` is SPECIAL — it routes to the RF/hub path, NOT \"off\"; always use a nonzero mask to fire local emitters.\n\n**Current `port_mask()` (ir.rs:30-32) is CORRECT** for port ids p∈{0,1,2}: `[0,1,2]→7`, `[0]→1`, `[1]→2`, `[2]→4`. Keep it. ⚠UNCERTAIN: bit→physical-emitter map (no firmware string). Test: fire masks 1, 2, 4 individually on the live Hub, watch with a phone camera which output blinks. Likely bit0=built-in front, bit1/bit2=the two jacks (jack order a guess).\n\n**The blob** is a fully documented big-endian structure (NOT Pronto, NOT opaque), 16-byte header + timing words:\n`[0]type (0x01 keycode / 0xFF raw)` · `[1..4]period_ns=floor(1e9/carrier_hz)` · `[5]duty=0x32(50%)` · `[6..7]preSilence(µs)` · `[8]minRepeats` · `[9]0` · `[10..11]startLoc` · `[12..13]repeatLoc` · `[14..15]finishLoc`, each Loc = 0-indexed byte offset of a sequence's uint16 wordCount field (0 if absent). Sequence = `u16 wordCount` then `wordCount` words. Duration word = uint16 BE: `bit15=1→mark(carrier ON)/0→space`, `bits14..0=µs (min 30)`; for D>32767 emit `0xFFFF`(mark)/`0x7FFF`(space) chunks subtracting 32767, remainder clamped ≥30. This means we can SYNTHESIZE codes, not just replay. `duty` field in `IrCode` (informational) does not matter — hal hardcodes 50%. ⚠UNCERTAIN(high-confidence): duration unit is microseconds; resolve by scoping a synthesized 9000-unit mark ≈ 9 ms.\n\n---\n\n## 4) SELF-TEST — fastest proof of real IR emission, no blob\n\n**Best single command: `/ir/ir_test`.** hal supplies its built-in 40 kHz waveform (22 static bytes at .data 0x463470) through the identical `ir_send()` path the working /ir/ir_send uses — if it emits, real sends work. Send an opcode-8 command frame (same framing as ir_send, empty blob):\n\n```\n{\"id\":<rid>,\"cmd\":\"/ir/ir_test\",\"data\":{\"time\":1000,\"ports\":7}}\n```\n\n`time` REQUIRED int (hal computes `time*1000000`); `ports` optional, default 7. Observe the front IR window with a phone camera. ⚠UNCERTAIN: `time` semantics (count vs duration) — if it emits continuously, stop with `/ir/ir_send {\"enable\":false}`.\n\n**Fallback (reuses the proven ir.rs /ir/ir_send path with a known-good blob):** `/ir/ir_send` with `{\"enable\":true,\"keyLatency\":0,\"ports\":7}` + verbatim 22-byte blob:\n`01 00 00 61 A8 32 00 00 04 00 00 00 00 10 00 00 00 02 82 EE 02 EE`\n\n**No-camera RF self-test:** `/ir/ir_loopback` (no params, no blob) — hal emits its 62.5 kHz pattern, captures it back, validates carrier. Returns JSON; ⚠UNCERTAIN pass/fail field — inspect the returned JSON once live before trusting as an oracle.\n\n---\n\n## 5) ORDERED NEXT ACTIONS (serial device work)\n\n1. **Implement `read_reply` + `json_end`** in ir.rs; rewrite `send_raw` (ir.rs:149-174) to write-then-`read_reply` over a `BufReader`. Return the parsed `{code, errorCode, errorString, binary}`, not raw bytes. This alone fixes the \"reads 7 bytes and stops\" bug.\n2. **Add a `selftest` CLI verb** that sends `/ir/ir_test {\"time\":1000,\"ports\":7}` and prints the parsed reply. Build for MIPS (service/build-mips.sh), deploy with `tools/netdeploy.py`, run on device with a phone camera on the front IR window. **This is the fastest proof of real emission** — do it before anything TV-specific.\n3. **Prove a real TV command end-to-end:** first learn one button (§2) OR, faster, replay a captured/known TV power blob through the existing `send_ir`. Point the Hub at the user's TV, fire, confirm the TV reacts. If `/ir/ir_test` blinked but the TV ignores a real code, the issue is blob format (§2 wrap) or ports mapping (step 4), not the transport.\n4. **Pin the ports→emitter map:** fire `send_ir` with `ports` masks 1, 2, 4, 7 individually; watch camera + TV to learn which bit is the front blaster vs jacks. Update the doc comment at ir.rs:28-29 / retire the \"unconfirmed\" note.\n5. **Implement LEARN:** wire `learn_start/poll/stop` (ir.rs:234-243) using MODEL B first, MODEL A fallback (§2). Deploy, run the choreography test (dump bytes while pressing a remote), decode the streamed uint16 array, rebuild the blob, and replay it to confirm round-trip against the user's TV.\n6. **Confirm `code` semantics live:** verify success replies carry `code:200` and that RF errors surface as 500/503, so higher layers can branch. Capture one full JSON body to settle whether `errorCode`/`errorString` ever appear at :16716.\n\nKey uncertainties to resolve on-device, ranked: (a) ir_cap choreography B-vs-A + param keys; (b) captured-blob shape (bare vs compiled); (c) ports bit map; (d) `ir_test` time semantics. Everything in §1 (response parsing) and §3 (ports=int bitmask, blob layout) is static-confirmed and safe to code now."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Research"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "research:response-format",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a80890263fffb0dae",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783018670594,
      "queuedAt": 1783018670549,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "hal's LTCP reply to a `command` (opcode 8) `/ir/ir_send` is…",
      "promptPreview": "RESOURCES (all offline, read-only — do NOT touch the live device):\n- Extracted stock rootfs: /tmp/harmony_rootfs  (515 files). If missing, regenerate:\n    python3 /Volumes/MacEXT/code/logitechharmonyhub/backups/sqextract-atheros-lzma.py /Volumes/MacEXT/code/logitechharmonyhub/backups/mtd3.bin /tmp/harmony_rootfs\n- hal binary (C, MIPS big-endian, stripped): /tmp/harmony_rootfs/usr/bin/hal\n    Inspe…",
      "lastProgressAt": 1783019986391,
      "tokens": 166090,
      "toolCalls": 33,
      "durationMs": 1315796,
      "resultPreview": "{\"summary\":\"hal's LTCP reply to a `command` (opcode 8) `/ir/ir_send` is a PRIMARY response packet followed by one or more SECONDARY packets carrying the JSON body. I confirmed from the C hal disassembly that the reply reuses the request's LTCP reqId with the 0x80 (isResp) bit set: reply[2] = request[2] | 0x80 (libhal_send_success_response @0x4189d0/0x4189e4). Numeric params are TLV: a length byte …"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "research:learn-ircap",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "aa68f82dfd31e7264",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783018670601,
      "queuedAt": 1783018670549,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The IR-learn path in stock `hal` is exposed as the LTCP res…",
      "promptPreview": "RESOURCES (all offline, read-only — do NOT touch the live device):\n- Extracted stock rootfs: /tmp/harmony_rootfs  (515 files). If missing, regenerate:\n    python3 /Volumes/MacEXT/code/logitechharmonyhub/backups/sqextract-atheros-lzma.py /Volumes/MacEXT/code/logitechharmonyhub/backups/mtd3.bin /tmp/harmony_rootfs\n- hal binary (C, MIPS big-endian, stripped): /tmp/harmony_rootfs/usr/bin/hal\n    Inspe…",
      "lastProgressAt": 1783019661487,
      "tokens": 135032,
      "toolCalls": 45,
      "durationMs": 990886,
      "resultPreview": "{\"summary\":\"The IR-learn path in stock `hal` is exposed as the LTCP resource `/ir/ir_cap` (method name `ir.cap`), routed through a fixed dispatch table at 0x452840. Unlike `/ir/ir_send`, ir_cap is NOT a simple JSON opcode-8 command: its table entry only populates the OPEN(1), DEVCTL(6), and command/notify(w11=0x2c) handler slots — there is no plain READ slot — and the actual capture-and-return is …"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "research:blob-and-ports",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "acc6a411e3c78a701",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783018670601,
      "queuedAt": 1783018670549,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The /ir/ir_send blob is NOT Pronto and NOT a carrier-embedd…",
      "promptPreview": "RESOURCES (all offline, read-only — do NOT touch the live device):\n- Extracted stock rootfs: /tmp/harmony_rootfs  (515 files). If missing, regenerate:\n    python3 /Volumes/MacEXT/code/logitechharmonyhub/backups/sqextract-atheros-lzma.py /Volumes/MacEXT/code/logitechharmonyhub/backups/mtd3.bin /tmp/harmony_rootfs\n- hal binary (C, MIPS big-endian, stripped): /tmp/harmony_rootfs/usr/bin/hal\n    Inspe…",
      "lastProgressAt": 1783019087769,
      "tokens": 63054,
      "toolCalls": 18,
      "durationMs": 417168,
      "resultPreview": "{\"summary\":\"The /ir/ir_send blob is NOT Pronto and NOT a carrier-embedded opaque capture — it is a fully documented, big-endian, 16-byte-header + timing-word structure built entirely in pure Lua (irmanager.lua), which hal (libir.c) parses field-for-field. The header carries carrier period (ns), a constant 50% duty byte, presilence, min-repeat count, and three uint16 byte-offset pointers to start/r…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "research:ir_test-and-surface",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a7c9d0db97d537c50",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783018670601,
      "queuedAt": 1783018670549,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "hal exposes four IR commands over LTCP — /ir/ir_send, /ir/i…",
      "promptPreview": "RESOURCES (all offline, read-only — do NOT touch the live device):\n- Extracted stock rootfs: /tmp/harmony_rootfs  (515 files). If missing, regenerate:\n    python3 /Volumes/MacEXT/code/logitechharmonyhub/backups/sqextract-atheros-lzma.py /Volumes/MacEXT/code/logitechharmonyhub/backups/mtd3.bin /tmp/harmony_rootfs\n- hal binary (C, MIPS big-endian, stripped): /tmp/harmony_rootfs/usr/bin/hal\n    Inspe…",
      "lastProgressAt": 1783019928359,
      "tokens": 142676,
      "toolCalls": 54,
      "durationMs": 1257757,
      "resultPreview": "{\"summary\":\"hal exposes four IR commands over LTCP — /ir/ir_send, /ir/ir_cap, /ir/ir_loopback, /ir/ir_test (each with a short alias ir.send/ir.cap/ir.loopback/ir.test) — dispatched from a 0x38-byte-record table in .data at VMA 0x4627d0+. The command's execute callback lives at record offset +0x2c. Two of these need NO learned blob: /ir/ir_test emits a hardcoded 40 kHz/50%-duty test waveform that h…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "synthesize:ir-spec",
      "phaseIndex": 2,
      "phaseTitle": "Synthesize",
      "agentId": "a4f14568de12ba04f",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1783019986415,
      "queuedAt": 1783019986414,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/Volumes/MacEXT/code/logitechharmonyhub/service/irapi/src/i…",
      "promptPreview": "RESOURCES (all offline, read-only — do NOT touch the live device):\n- Extracted stock rootfs: /tmp/harmony_rootfs  (515 files). If missing, regenerate:\n    python3 /Volumes/MacEXT/code/logitechharmonyhub/backups/sqextract-atheros-lzma.py /Volumes/MacEXT/code/logitechharmonyhub/backups/mtd3.bin /tmp/harmony_rootfs\n- hal binary (C, MIPS big-endian, stripped): /tmp/harmony_rootfs/usr/bin/hal\n    Inspe…",
      "lastProgressAt": 1783020073132,
      "tokens": 47377,
      "toolCalls": 1,
      "durationMs": 86716,
      "resultPreview": "# IR CLIENT IMPLEMENTATION SPEC — service/irapi/src/ir.rs\n\nCoding target: extend `LtcpBackend`. The current `send_raw` (ir.rs:149-174) does ONE 512-byte read and returns — that is the bug: it stops after the coalesced 6/7-byte primary and never reads the JSON body. Replace with a proper LTCP reassembler. All facts below are CONFIRMED from the C hal disassembly + decompiled ltcp.lua unless marked ⚠…"
    }
  ],
  "totalTokens": 554229,
  "totalToolCalls": 151
}